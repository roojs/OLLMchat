This paragraph has **bold spanning
a newline** (known parser limitation).

Round-trip is expected to lose or mangle the bold and following text until the parser supports emphasis across soft line breaks (see plan 1.8).
