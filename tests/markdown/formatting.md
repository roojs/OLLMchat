# Simple formatting

Plain text with **bold** and *italic* and ***bold italic*** and ___also bold italic___ and `code` and ~~strikethrough~~ and _underline_.

***Bold + Italic*** on its own line (not a horizontal rule).

Mixed **bold *and* italic** and `code with **ignored** formatting`.

Inline link: [example](https://example.com) and autolink <https://example.org>.
