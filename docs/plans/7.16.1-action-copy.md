# 7.16.1 — Copy execution into `Action.*` (no wiring)

**Status:** in progress (copy done; awaiting review)

**Parent:** [`7.16-iterative-task-execution-loop.md`](7.16-iterative-task-execution-loop.md)

**Depends on:** nothing (first sub-plan)

**Blocks:** [`7.16.1b-action-wire.md`](7.16.1b-action-wire.md)

**Pointer:** `.cursor/rules/CODING_STANDARDS.md` — Checklist for all plans · **Layout:** `docs/guide-to-writing-plans.md`

> **Note:** `docs/plans/1.0-summary.md` is not updated until the **7.16.1** pair (copy + wire) is done and archived.

---

## Purpose

- **🔷** **Copy** (not refactor) today’s task execution from **`Task.Details`** into **`liboccoder/Action/`** runners.
- **🔷** Follow existing code as closely as possible — **no creative restructuring**.
- **🚫** **No wiring** — **`Details.run_exec`** unchanged; no **`run_action`** dispatch yet.
- **🚫** **No** **`meson.build`** / build changes — Action sources are present but **not compiled** until **7.16.1b**.
- **🚫** **No helpers** on **`Action.Base`** — duplicated bodies per runner where **`Details`** had one shared method.
- **🚫** **No** **`Action.Iterate`** in this sub-plan.

---

## Rule: copy, don’t extract

- **🔷** Lift **`run_exec`** / **`run_post_exec`** / **`post_exec_prompt`** bodies verbatim; replace **`this.`** with **`this.task.`** where the field lived on **`Details`**.
- **🔷** Keep comments from **`Details.vala`** (including commented-out blocks).
- **🚫** Do not deduplicate **`ExamSequence`** vs **`RunTools`** vs **`RefOnly`** vs **`WriteExec`** — they share the same **`run_exec`** body today; each runner gets its own copy for dispatch clarity at wire time.
- **🚫** Do not add shared helpers on **`Base`**.

---

## Layout on disk

```text
liboccoder/Action/
  Base.vala
  ExamSequence.vala
  PostExamMerge.vala
  RefOnly.vala
  WriteExec.vala
  RunTools.vala         # removed in 7.16.3
```

- **ℹ️** Namespace **`OLLMcoder.Action`** — flat; **🚫** not under **`Task/`**.

---

## Runners (what was copied)

### `Base`

- **`protected Task.Details task`**
- **`public abstract async void run () throws Error`**

### `PostExamMerge`

- Copied from **`Details.run_post_exec`** + **`Details.post_exec_prompt`** (private on this class).

### `ExamSequence` — path **A**

- Copied from **`Details.run_exec`** (exam queue built by **`build_run_queue`** before dispatch — still on **`Details`**).

### `RunTools` — path **B** (legacy)

- Same **`run_exec`** copy as **`ExamSequence`**; dispatches to **`PostExamMerge`** when **`tools().size > 1`**.

### `RefOnly` — path **C**

- Same **`run_exec`** copy; happy path is single lone **`exec`** row.

### `WriteExec` — path **D**

- Same **`run_exec`** copy; **`write_file`** behaviour stays in **`Tool.run`**.

---

## Known wiring blockers (for 7.16.1b — not fixed here)

- **💩** **`PostExamMerge`** calls **`yield this.task.fill_model ()`** — **`fill_model`** is **`protected`** on **`OLLMchat.Agent.Base`**; wiring may need **`internal`** visibility or a thin **`Details`** delegate — **no helper layer on `Action.Base`**.
- **💩** Same for any other **`protected`** **`Details`** / **`Agent.Base`** members the copied bodies touch.
- **ℹ️** **`build_run_queue`** stays on **`Details`** until wire review.

---

## Acceptance (this sub-plan only)

- **🔷** All six Action files exist under **`liboccoder/Action/`**.
- **🔷** Bodies match **`Details.run_exec`** / **`run_post_exec`** / **`post_exec_prompt`** (modulo **`this` → `this.task`**).
- **🔷** **`Details.vala`** and **`meson.build`** untouched.
- **🔷** Live behaviour unchanged (Action code not linked).

---

## Next

- **🔷** Review Action classes → approve → [`7.16.1b-action-wire.md`](7.16.1b-action-wire.md).

---

## LLM notes

- **🚫** Do not wire **`run_action`** here.
- **🚫** Do not add Action files to **`meson.build`** here.
- **🚫** Do not add **`Iterate.vala`** here.
