# Plan 1.23.30: Reference content length validation

**Source:** [1.23.20 DONE](done/1.23.20-DONE-post-testing-changes.md) §1 (deferred from 1.23.18).

**Coding standards:** `.cursor/rules/CODING_STANDARDS.md`.

## Status

TODO — not started.

## Goal

- **`validate_task_list_reference_lengths` (Runner):** After task list parse (or per task), resolve reference content the same way as `Details` / precursor expansion; if any resolved block exceeds a configured max length, return an issues string so the creator can trim or split references. **`send_async`** (task list create) would append to `parser.issues` and retry like other validation failures.
- **http(s) validation (optional):** Sanity-check `http://` / `https://` targets (format-only and/or reachability) during LIST-stage validation if desired — was a secondary bullet on 1.23.20.

## Scope

- **`liboccoder/Skill/Runner.vala`** — new helper + call site after parse, before accepting task list.
- **`liboccoder/Task/Details.vala`** — reuse or mirror resolved reference length computation (same paths as `link_content` / precursor injection).
- **Config:** max length constant or settings key.

## Related

- **1.23.20** (done): HTTP stage behaviour implemented elsewhere; length + HTTP checks left here.
- **1.23.31:** File `#anchor` / AST validation (separate plan).
