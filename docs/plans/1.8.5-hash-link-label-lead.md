# 1.8.5 — Hash-link label lead (`[#` vs `[user`)

**Status:** **✔️** done — `FormatMap` `[#?` link lead + prompt examples use `[#user-N]`

**Parent:** [`1.8-markdown-outstanding-issues.md`](1.8-markdown-outstanding-issues.md)

---

## Fix (`libocmarkdown/FormatMap.vala`)

```vala
mp.set("[#", FormatType.INVALID);
mp.set("[#?", FormatType.LINK);
```

Same pattern as `` [`? `` for backtick link text.

---

## Verify

```bash
printf '%s\n' '[#user-54](#user-54)' > /tmp/t.md
build/examples/oc-markdown-doc-test /tmp/t.md   # links.size = 1
```
