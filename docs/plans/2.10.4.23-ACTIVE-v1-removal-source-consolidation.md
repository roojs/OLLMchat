# 2.10.4.23 — V1 removal, source consolidation, Meson cleanup (ACTIVE)

> **Do not update `docs/plans/1.0-summary.md` for this plan.**

**Status:** **ACTIVE** — Meson V2 cutover (**2.10.4.21**–**28**) ✅; this plan finishes the job: **one tree**, **no `v2testing`**, **no duplicate V1/V2 files**

**Pointer:** `docs/guide-to-writing-plans.md` · **`docs/build-rules.md`**

**Parent:** [`2.10.4.0-summary.md`](2.10.4.0-summary.md)

**Prerequisite (done):**

- [`done/2.10.4.21-DONE-meson-v2-cutover.md`](done/2.10.4.21-DONE-meson-v2-cutover.md) — **`v2testing`** source swaps; default **`true`**
- [`done/2.10.4.22-DONE-app-rpc-daemon-startup.md`](done/2.10.4.22-DONE-app-rpc-daemon-startup.md) — **`ollmapp/V2/Window.vala`**
- [`done/2.10.4.24-DONE-v2-build-gaps.md`](done/2.10.4.24-DONE-v2-build-gaps.md) — green V2 build
- [`done/2.10.4.27-DONE-rpc-client-cleanup-and-wire-registration.md`](done/2.10.4.27-DONE-rpc-client-cleanup-and-wire-registration.md)
- [`done/2.10.4.28-DONE-rpc-client-write-queue.md`](done/2.10.4.28-DONE-rpc-client-write-queue.md)

**Blocks until done (vector):**

- [`2.10.4.29-ACTIVE-vector-cli-rpc-migration.md`](2.10.4.29-ACTIVE-vector-cli-rpc-migration.md) — RPC for **`oc-vector-index`**, **`oc-vector-search`**, **`CodebaseSearch`** tool; then delete V1 **`libocvector`** (Phase 1d)

**Related:**

- [`done/8.1-DONE-bin-protocol-libocrpc.md`](done/8.1-DONE-bin-protocol-libocrpc.md) — bin wire (production path)
- [`done/2.10.4.18-DONE-vector-search-rpc.md`](done/2.10.4.18-DONE-vector-search-rpc.md) — **`Codebase.search`** **✅**; remainder in **2.10.4.29**

---

## Purpose

**2.10.4.21** flipped Meson to compile **V2 copies** under parallel paths (`libocfiles/V2/*`, `liboccoder/V2/*`, `*V2/` tool dirs, `ollmapp/V2/Window.vala`) while **shipping V1** remained on disk for **`-Dv2testing=false`**. That was intentional scaffolding.

**🔷** This plan **removes scaffolding**:

1. **Promote V2 sources** to their **canonical paths** (no more `V2/` / `*V2/` suffix dirs for production code).
2. **Delete all unused V1** sources (in-process SQLite PM, **`Sandbox/*`**, shipping **`Window.vala`**, full **`libocvector`** FAISS tree, …).
3. **Remove the `v2testing` Meson option** and every `if get_option('v2testing')` branch.
4. **Install as `libocfiles.so`** again (not **`libocfiles2.so`** alongside v1).
5. **App / CLI / packaging cleanup** — dead options, docs, debian split names if needed.

**ℹ️** After this plan, there is **no** way to build the old in-process file/index stack from this repo.

---

## Inventory — what moves vs what deletes

### `libocfiles/` — promote `V2/*` → root; delete V1

| Action | Path |
|--------|------|
| **Move** | `V2/File.vala` → `File.vala` (replace) |
| **Move** | `V2/FileBase.vala` → `FileBase.vala` |
| **Move** | `V2/FileAlias.vala` → `FileAlias.vala` |
| **Move** | `V2/FileWithHistory.vala` → `FileWithHistory.vala` |
| **Move** | `V2/FileHistory.vala` → `FileHistory.vala` |
| **Move** | `V2/FileBuffer.vala` → `FileBuffer.vala` |
| **Move** | `V2/Folder.vala` → `Folder.vala` |
| **Move** | `V2/FolderFiles.vala` → `FolderFiles.vala` |
| **Move** | `V2/ProjectFile.vala` → `ProjectFile.vala` |
| **Move** | `V2/ProjectFiles.vala` → `ProjectFiles.vala` |
| **Move** | `V2/ProjectList.vala` → `ProjectList.vala` |
| **Move** | `V2/ProjectManager.vala` → `ProjectManager.vala` |
| **Move** | `V2/DeleteManager.vala` → `DeleteManager.vala` |
| **Move** | `V2/ReviewFiles.vala` → `ReviewFiles.vala` |
| **Move** | `V2/Tree.vala` → `Tree.vala` |
| **Move** | `V2/TreeBase.vala` → `TreeBase.vala` |
| **Keep** | `Copyable.vala`, `BufferProviderBase.vala`, `DummyFileBuffer.vala`, `FileChange.vala`, `GitProviderBase.vala`, `Diff/*`, `SQT/VectorMetadata.vala` |
| **Keep** | `ollmfilesd/CallParam.vala` in Meson list (shared wire params) |
| **Delete** | entire `Sandbox/` tree (`Bubble.vala`, `RunSeccomp.vala`, `Scan.vala`, `Overlay.vala`, `BubbleWindows.vala`, …) |
| **Delete** | `ProjectMigrate.vala` (V1 SQLite migration in app process) |
| **Delete** | empty `V2/` directory after moves |

### `liboccoder/` — promote `V2/*` → root / `Task/`

| Action | Path |
|--------|------|
| **Move** | `V2/FileDropdown.vala` → `FileDropdown.vala` |
| **Move** | `V2/SourceView.vala` → `SourceView.vala` |
| **Move** | `V2/AgentFactory.vala` → `AgentFactory.vala` |
| **Move** | `V2/Approvals.vala` → `Approvals.vala` |
| **Move** | `V2/Task/ValidateLink.vala` → `Task/ValidateLink.vala` |
| **Move** | `V2/Task/ResolveLink.vala` → `Task/ResolveLink.vala` |
| **Move** | `V2/Task/WriteChange.vala` → `Task/WriteChange.vala` |
| **Keep** | shared Skill/, Task/ (non-swapped), Action/, List/, Gtk*, BufferProvider, … |
| **Delete** | empty `V2/` after moves |

### `liboctools/` — promote `*V2/` → replace shipping tool impl

| Action | Path |
|--------|------|
| **Replace** | `ReadFileV2/Request.vala` + `ReadFileV2/Summarize.vala` → `ReadFile/Request.vala`, `ReadFile/Summarize.vala` (delete old V1 bodies) |
| **Replace** | `EditModeV2/Request.vala` + `EditModeV2/Stream.vala` → `EditMode/Request.vala`, `EditMode/Stream.vala` |
| **Replace** | `WriteFileV2/Request.vala` → `WriteFile/Request.vala` |
| **Replace** | `RunCommandV2/Request.vala` → `RunCommand/Request.vala` |
| **Keep** | `FileVerification.vala`, `Child/*`, `WebFetch/*`, `GoogleSearch/*`, `SessionFetch/*`, `Registry.vala`, … |
| **Delete** | `ReadFileV2/`, `EditModeV2/`, `WriteFileV2/`, `RunCommandV2/` dirs after merge |

**ℹ️** `Tool.vala` wrappers under `ReadFile/`, `EditMode/`, … stay; only **Request** (and EditMode **Stream**) swap to V2 bodies.

### `libocvector/` — **✔️ deleted** (Phase 1d, 2.10.4.29)

| Action | Path |
|--------|------|
| **Deleted** | entire `libocvector/` FAISS tree (`Index.vala`, `Database.vala`, `Indexing/*`, `Search/*`, `Tool/*`, …) |
| **Moved** | `faiss_c_wrapper.{cpp,h}` → `libocvector2/` (were symlinks to deleted tree) |
| **App** | `codebase_search` via `liboctools/CodebaseSearch` + daemon RPC; `vector_registry` removed |

### `ollmapp/` — promote V2 window

| Action | Path |
|--------|------|
| **Replace** | `V2/Window.vala` → `Window.vala` (RPC boot, no in-process **`BackgroundScan`**) |
| **Delete** | shipping `Window.vala` (V1) before replace |
| **Delete** | `V2/` dir |

### Already gone

- **`libocfiles/RpcClient.vala`** — deleted in **2.10.4.27** ✅

---

## Phase 1 — Git moves (one library at a time)

**🔷** Use **`git mv`** so history follows canonical paths. **One phase → `ninja -C build` → smoke** before the next.

**Suggested order:**

1. **`libocfiles`** — move all `V2/*.vala` to root; delete V1 counterparts + `Sandbox/` + `ProjectMigrate.vala`
2. **`liboctools`** — merge `*V2/` into tool dirs; delete `*V2/` dirs
3. **`liboccoder`** — move `V2/*` and `V2/Task/*`
4. **`libocvector`** — stub `Registry.vala` only; delete FAISS tree
5. **`ollmapp`** — `V2/Window.vala` → `Window.vala`

**⏳** After each step: fix **imports only if** paths changed (namespaces unchanged — **`OLLMfiles`**, **`OLLMcoder`**, … stay the same).

---

## Phase 2 — Meson: remove `v2testing`

### 2.1 `meson_options.txt`

**Delete** the **`v2testing`** option row entirely.

### 2.2 Root `meson.build`

| Change | Detail |
|--------|--------|
| **Library names** | `ocfiles_lib = 'ocfiles'` (drop `ocfiles2` ternary); same for **`occoder`**, **`octools`**, **`ocmcp`** |
| **Examples** | Remove `and not get_option('v2testing')` guards — decide per-example: **update** to RPC/daemon or **drop** from `executables_info` |
| **Tests** | Remove `v2testing=false` requirement in `tests` option error; wire **`tests/rpc/*`** as primary harness |
| **CLI** | Always build **`ollmchat-cli`** if still wanted |
| **Comment** | Fix stale “JSON-RPC wire types” comment on **`libocrpc`** subdir |

### 2.3 `libocfiles/meson.build`

**Replace** the whole `if get_option('v2testing')` / `else` block with **one** `ocfiles_src` list (the current V2 list, paths without `V2/` prefix after Phase 1).

**Remove:**

- `sandbox_src` / `ocfiles_extra_sources` seccomp C glue (only used by V1 **`Sandbox`**)
- `if not get_option('v2testing')` typelib branch — **decide**: generate GIR for new thin client or keep `vala_gir: []` (document choice in changelog)

**Deps:** keep **`libocrpc`**; drop seccomp/linux extras unless another consumer needs them.

### 2.4 `liboctools/meson.build`, `liboccoder/meson.build`, `libocvector/meson.build`, `libocmcp/meson.build`, `ollmapp/meson.build`

- Collapse to **single** source list (post–Phase 1 paths).
- Remove all `get_option('v2testing')` branches.
- **`ollmapp`:** `window_src = 'Window.vala'` always — delete `if get_option('v2testing')` pick.

---

## Phase 3 — Delete dead app / packaging surface

### 3.1 `ollmapp/Application.vala` — remove `--disable-indexer`

**Why:** V2 **`Window`** has **🚫** no in-process **`BackgroundScan`**; flag is dead.

#### Remove — static field

```vala
		public static bool opt_disable_indexer = false;
```

#### Remove — option table row

```vala
			{ "disable-indexer", 0, 0, OptionArg.NONE, ref opt_disable_indexer, "Disable background semantic search indexing", null },
```

#### Remove — reset in `command_line`

```vala
			opt_disable_indexer = false;
```

**⏳** Grep **`OllmchatApplication.opt_*`** — remove any option with zero callers.

### 3.2 Docs / debian

| Target | Action |
|--------|--------|
| **`README.md`** | Drop **`-Dv2testing=false`** shipping-v1 instructions |
| **`docs/build-rules.md`** | Remove **`v2testing`** section |
| **`debian/split/control`** | Drop **`libocfiles2`** / dual-package rows if present; single **`libocfiles1`** again |
| **`meson.build` comment** | Binary RPC, not JSON-RPC |

### 3.3 Examples and tests

| Item | Action |
|------|--------|
| **`examples/oc-test-files`**, **`oc-test-bubble`**, **`oc-vector-index`**, **`oc-vector-search`** | Update to RPC client + daemon, or remove from Meson until rewritten |
| **`tests/`** (markdown, ollamaweb, …) | Re-enable without `v2testing=false` guard where still valid |
| **`tests/rpc/*`** | Primary regression for file daemon |

---

## Phase 4 — Wire / type hygiene (follow-up in same milestone)

**Parent:** [`done/8.1-DONE-bin-protocol-libocrpc.md`](done/8.1-DONE-bin-protocol-libocrpc.md) — bin cutover ✅

### Display vs domain split

- **Daemon (`ollmfilesd/`)** — derived checks as **methods**, not GObject computed **properties**
- **Client (`libocfiles/`)** — keep computed **properties** for Gtk binding (`path_basename`, `icon_name`, …)

| Surface | Client | Daemon | Wire |
|---------|--------|--------|------|
| `path_basename` | computed property | method / inline | **🚫** not sent |
| `icon_name` | computed property | **🚫** not on type | **🚫** not sent |
| `display_with_indicators` | computed property | **🚫** not on type | **🚫** not sent |
| `is_alias` | computed property | **⏳** method | **🚫** not sent |

**⏳** Strip dead **`bin_write_prop`** / **`bin_read_prop`** omit cases that reference client-only **`ParamSpec`** names on daemon types.

---

## Suggested order (full plan)

1. **⏳** Phase 1a — **`libocfiles`** moves + V1/Sandbox delete → green build
2. **⏳** Phase 1b — **`liboctools`** merge `*V2/` → green build
3. **⏳** Phase 1c — **`liboccoder`** moves → green build
4. **⏳** Phase 1d — **`libocvector`** stub + delete FAISS — **after 2.10.4.29**
5. **⏳** Phase 1e — **`ollmapp/Window.vala`** promote → green build + app smoke (open project, file dropdown, tool call)
6. **⏳** Phase 2 — Meson: remove **`v2testing`**, single lib names, collapse branches
7. **⏳** Phase 3 — app options, docs, debian, examples/tests audit
8. **⏳** Phase 4 — daemon derived-property → method pass (optional split to **2.10.4.29** if large)

---

## Verification

- **`ninja -C build`** — no **`v2testing`** option; default config green
- **`build/ollmchat.bin --debug`** — spawns **`ollmfilesd`**, opens project, loads file, no write-queue warnings
- **`tests/test-rpc.sh`** — passes
- **`rg 'v2testing|V2/|ReadFileV2|libocfiles2'`** — no hits in Meson or production sources (plans/docs OK)

---

## Changelog

| Date | Change |
|------|--------|
| 2026-07-05 | Expanded from app-option backlog to full V1 removal + source consolidation plan (user: scaffolding still in tree after **2.10.4.21**–**28**) |
