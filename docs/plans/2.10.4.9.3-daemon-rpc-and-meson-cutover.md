# 2.10.4.9.3 — Daemon RPC and Meson cutover

**Status:** **PARTIAL** — §1 `CallParam` + §2 relays ✅ ([`done/2.10.4.9.3.1`](done/2.10.4.9.3.1-DONE-daemon-folder-file-relays.md)); **⏳** Meson §4 · e2e checklist.

**Parent:** [`2.10.4.9-v2-wiring-and-cutover.md`](2.10.4.9-v2-wiring-and-cutover.md)

**Depends on:** [`done/2.10.4.9.1-DONE-design-project-files-and-client-api.md`](done/2.10.4.9.1-DONE-design-project-files-and-client-api.md) ✅

**Blocks:** [`2.10.4.9.2`](2.10.4.9.2-reshape-callers.md) (callers) · app Meson cutover

---

## Purpose

- **🔷** Add **`Folder.*`** and **`File.fetch`** wire methods on the daemon — same objects as client API ([`done/2.10.4.9.1`](done/2.10.4.9.1-DONE-design-project-files-and-client-api.md)) ✅
- **🔷** Finish **`File.*`** relays ([`2.10.4.6`](done/2.10.4.6-DONE-file.md)); handlers read daemon `project_files`, not thin PM delegates.
- **🔷** Meson cutover last; then end-to-end checklist.

---

## Wire to implement (🔷)

- **`Folder.fetch`** — folder row at path (replaces `ProjectManager.get_folder_at_path`)
- **`File.fetch`** — file in project (`path` + `project_path`)
- **`Folder.contains_folder`** — directory membership under project
- **`Folder.fetch_file_list`** — dropdown snapshot
- **`Folder.fetch_pending_approvals`** — approvals snapshot
- **`File.register`** — existing; backs client `insert_file`

**🚫** Do not add new `ProjectManager.get_folder_at_path` relay.

Update [`2.10.4.1`](2.10.4.1-ollmfilesd-rpc-api.md) when handlers land.

---

## Daemon rules (🔷)

- **🔷** Handler logic on **`ollmfilesd/Folder.vala`** / **`ollmfilesd/File.vala`** — read local `project_files`, not cross-delegate through PM wrappers.
- **🔷** `OLLMrpc.Request.register` in `ollmfilesd/Application.vala` dispatches **`Folder.*`** to a relay singleton ✅ ([`done/2.10.4.9.3.1`](done/2.10.4.9.3.1-DONE-daemon-folder-file-relays.md))
- **⏳** Strip UI-only code from `ollmfilesd/File.vala` per [`2.10.4.6`](done/2.10.4.6-DONE-file.md).

---

## Meson cutover (last)

- **⏳** App → `V2/*.vala` + `RpcClient` / `RpcClientBoot`
- **⏳** Spawn `ollmfilesd`
- **🚫** No in-place edits to shipping `libocfiles/Folder.vala`

---

## End-to-end checklist

1. Client `Folder` has no `project_files`.
2. Dropdown via `fetch_file_list` RPC.
3. Tools via `project.fetch_file`; no GUI `child_map`.
4. New file: disk → `insert_file` / `File.register`.
5. Search: no client `get_ids`.
6. Session: paths + `fetch_file`, not index `get_active_file()`.

---

## Concrete code proposals

### 1. `ollmfilesd/CallParam.vala` — params for scoped Folder/File RPC ✅

**Delivered:** `FileParams.project_path`, `FolderParams` in `ollmfilesd/CallParam.vala`; registered in daemon startup.

<details>
<summary>Original fences (archive)</summary>

#### Add — `project_path` on `FileParams`

Placement: inside `FileParams`, immediately after the `path` property.

```vala
		public string project_path { get; set; default = ""; }
```

#### Add — `FolderParams` class

Placement: after closing `}` of `FileParams`, before `VectorParams`.

```vala
	/** {@code Folder.*} request params (project-scoped). */
	public class FolderParams : OLLMrpc.CallParam
	{
		public string project_path { get; set; default = ""; }
		public string path { get; set; default = ""; }
	}
```

</details>

### 2. Daemon relays ✅ — [`done/2.10.4.9.3.1-DONE-daemon-folder-file-relays.md`](done/2.10.4.9.3.1-DONE-daemon-folder-file-relays.md)

All **`Folder.*`** and **`File.fetch`** Vala fences implemented in the child plan.

### 3. Remove `ProjectManager.get_folder_at_path` daemon relay ✅

**N/A** — no `ProjectManager.get_folder_at_path` RPC relay was ever registered; internal `get_folder_at_path` kept for `Folder.fetch`.

### 4. Meson / spawn

**⏳** Hunks deferred until wire tests pass — see [`done/2.10.4.0-DONE-libocfiles-v2-and-rpc-layout.md`](done/2.10.4.0-DONE-libocfiles-v2-and-rpc-layout.md).

---

## Stage 3 done when

- ✅ [`done/2.10.4.9.3.1`](done/2.10.4.9.3.1-DONE-daemon-folder-file-relays.md) implemented; T1.6 updated for `Folder.fetch`
- ✅ Client hunks from [`done/2.10.4.9.1`](done/2.10.4.9.1-DONE-design-project-files-and-client-api.md) in tree
- **⏳** Meson cutover + checklist § above
