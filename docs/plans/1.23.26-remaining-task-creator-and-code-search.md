# Plan 1.23.26: Remaining items (task creator guidance, code search target)

**Source:** Remaining points from [Plan 1.23.23](done/1.23.23-DONE-task-iterator-skills-grep.md) Phase 6 and Phase 9. That plan is done; this plan carries the leftover work.

## Status

TODO — not started.

## Items

### 1. Task creator — prompt guidance (from Phase 6)

**Goal:** Ensure the task creation prompt tells the creator to produce **initial tasks only** and that the list will be **refined after each step** (iterator adds later tasks as needed). No "Recommended next steps" section in the output format.

**Scope:** `resources/skill-prompts/task_creation_initial.md` — verify or add guidance that the creator should break down into steps and that after each step progress will be reviewed, so it does not need to list every future task.

---

### 2. Code search target file (from Phase 9)

**Goal:** Code search tool should support targeting a **specific file or path scope** so "search in file X" is possible. (1.23.22 §1.)

**Scope:** `libocvector/Tool/RequestCodebaseSearch.vala` (and tool API if needed). Add a path/file or path_scope parameter so the LLM can restrict codebase_search to a given file or directory. Currently `RequestCodebaseSearch` has query, language, element_type, category, max_results — no path scope.

---

## Related plans

- **1.23.23** (done): Task iterator, task creator scope, skill split, Grep skill — this plan continues the remaining Phase 6 and Phase 9 items.
- **1.23.22** (done): [Testing issues](done/1.23.22-DONE-testing-issues.md); code search targeting from that plan.
