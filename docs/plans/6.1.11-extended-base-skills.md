# 6.1.11 Agent Pi — extended base skills

Status: ⏳ proposed

> **`docs/plans/1.0-summary.md` is not updated** for this sub-plan until it is done and archived.

ℹ️ Checklist: `docs/guide-to-writing-plans.md` — Checklist for plans (emoji provenance: **🔷** = user said it; **💩** = LLM suggestion).  
ℹ️ Parent: [6.1](6.1-agent-pi.md) / [6.1.2](done/6.1.2-DONE-pi-like-agent.md).  
ℹ️ Core pack (done): [done/6.1.3-DONE-base-skills.md](done/6.1.3-DONE-base-skills.md) — `resources/pi-skills/`.  
ℹ️ Offer / export UI: [6.1.10](6.1.10-skills-configuration.md).  
ℹ️ Upstream shortlist source: [ericgandrade/claude-superskills](https://github.com/ericgandrade/claude-superskills) (MIT) unless noted.

## Purpose

- 🔷 `⏳` Hold everything **left out** of the core base pack ([03](done/6.1.3-DONE-base-skills.md)) for later triage / vendoring as an **extended** set.
- 🔷 Same delivery shape as 03 when we ship any of these: Agent Skills folders under `resources/pi-skills/` (or a sibling tree), resource scan, optional export via [10](6.1.10-skills-configuration.md).
- 🔷 Prefer vendored / attributed upstream `SKILL.md` trees; thin human ports only when needed.
- ℹ️ Core pack stays the default; extended skills are available / offerable once locked — not dumped into the initial offer by default.

---

## Left out of 03 (candidates)

### Wanted / deferred from 03

- 🔷 `⏳` **`subagent-delegate`** — soft skill for delegating to a sub-agent; no good portable upstream yet (Claude Task / harness-bound playbooks). Hunt or thin-port when host API exists.
- 🔷 `⏳` **`git-hygiene`** — was “not for initial pack”; host/user layer owns git/file monitoring for v1. Revisit only if we want optional soft git playbooks later.
- 💩 `⏳` **`skill-creator` (markdown-only)** — authoring guidance without Python/eval harness; confirm before shipping.

### Superskills triage — skipped in 03

| Skill | Why left out in 03 | Extended status |
|-------|--------------------|-----------------|
| `grill-me` | Weird fit / Socratic interview | 🔷 `⏳` Reconsider if anyone wants it |
| `agent-skill-discovery` | Catalog already in prompt | 🔷 🚫 Still skip as-is; orchestrator uses catalog |
| `document-converter` | No real use then | 🔷 `⏳` Revisit if we gain converters / need |
| `skill-creator` (full + Python) | Harness baggage | 🔷 🚫 Python/evals; markdown concept under wanted above |
| `audio-transcriber` | Irrelevant then | 🔷 `⏳` Only if product needs audio |
| `youtube-summarizer` | Irrelevant then | 🔷 `⏳` Only if product needs it |
| `pptx-translator` | Irrelevant then | 🔷 `⏳` Only if product needs it |
| `storytelling-expert` | Irrelevant then | 🔷 `⏳` Optional creative pack |
| `mckinsey-strategist` | Irrelevant then | 🔷 `⏳` Optional biz pack |
| `us-program-research` | Irrelevant then | 🔷 `⏳` Optional / niche |
| enuno `skill-orchestrator` | Too complicated | 🔷 🚫 Prefer superskills orchestrator (already in core) |

### Explicitly not a gap in 03

- 🔷 🚫 **`document` (product docs)** — not needed as a skill; leave out of extended unless product asks later.

---

## Delivery notes (when implementing any candidate)

- ℹ️ Same mechanical adapt as 03: yank `evals/`, Agent Pi tools, ask in chat, say what to use (don’t name absent upstream tools).
- ℹ️ New folders join the same `/pi-skills` scan (or a dedicated extended root if we want to keep core vs extended physically split — **💩** lock then).
- ℹ️ [6.1.10](6.1.10-skills-configuration.md) decides what enters the **initial offer**; extended can be available-only by default.

---

## Suggested order

1. 🔷 `⏳` Decide which candidates are “yes later” vs permanent 🚫 (especially media / biz skills).
2. 🔷 `⏳` **`subagent-delegate`** when host sub-agent API exists (or a portable soft brief).
3. 💩 `⏳` Markdown-only **skill-creator** if we want in-app authoring.
4. 🔷 `⏳` Cherry-pick any remaining superskills includes into the pack + gresource.
5. ℹ️ Wire offer defaults in [10](6.1.10-skills-configuration.md) so extended stays off the initial menu unless chosen.

---

## LLM notes

- ℹ️ Core pack is archived at [done/6.1.3-DONE-base-skills.md](done/6.1.3-DONE-base-skills.md) — do not re-vendor those here.
- 🚫 Do not silently expand the **initial offer** when adding extended skills — that is [10](6.1.10-skills-configuration.md).
- 🚫 Do not ship skill-creator Python/eval harness or enuno orchestrator without a new explicit decision.
