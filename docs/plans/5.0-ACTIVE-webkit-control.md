# 5.0 — WebKit browser tool (accessibility)

> **Do not update `docs/plans/1.0-summary.md` for this plan.**

**Status:** **ACTIVE** — library + spike app first; chat tool wiring later

**Pointer:** `docs/guide-to-writing-plans.md` — **Checklist for plans**; proposed Vala follows **`docs/coding-standards.md`**

**Related:**

- ℹ️ Precedent help shape: [`resources/tools/huggingface-help.md`](../../resources/tools/huggingface-help.md), [`liboctools/HuggingFace/Tool.vala`](../../liboctools/HuggingFace/Tool.vala)
- ℹ️ CLI spike pattern (elsewhere in tree): [`examples/oc-hf.vala`](../../examples/oc-hf.vala), [`examples/oc-test-fetch.vala`](../../examples/oc-test-fetch.vala), [`examples/TestAppBase.vala`](../../examples/TestAppBase.vala) — **this** library’s app lives under **`libocwebkit/examples/`**, not top-level `examples/`
- ℹ️ Existing HTTP tools (may later defer to / coexist with this): `google_search`, `web_fetch`
- ℹ️ Snappr2 WebKit patterns (port select pieces): `/home/alan/gitlive/app.Snappr2` — `Browser.vala`, `Rpc/Cloudflare.vala`, `BrowserStack.vala`; Windows browse via sibling **`webview2-gtk`** (a11y later)
- ℹ️ Windows a11y track: [`5.0.1-windows-webkit-accessibility.md`](5.0.1-windows-webkit-accessibility.md)
- ℹ️ Android track: [`5.0.2-android-webkit-control.md`](5.0.2-android-webkit-control.md)
- ℹ️ Android backlog W1/W2 previously assumed DOM/JS SERP scrape — **5.0** supersedes the approach; **Android port** is **5.0.2** ([`docs/bugs/2026-07-18-android-poc-completion.md`](../bugs/2026-07-18-android-poc-completion.md))

---

## Purpose

- **🔷** New library **`libocwebkit`** — browser host **and** the OLLMchat **browser tool** (hand in hand). Not born inside chat UI chrome; chat registers/uses the library.
- **🔷** Library depends on **OLLMchat tool interfaces** (`BaseTool`, request types, etc.) so the tool is a first-class citizen of the lib.
- **🔷** Early work: **`oc-test-webkit`** under **`libocwebkit/examples/`** (see **Phase 3**) before globe UI in the main app.
- **🔷** Tool help is **action-based** (overview + per-action topic pages); browsing is accessibility-driven, not JS SERP scraping.
- **🔷** Each **chat instance owns one `BrowserStack`** (with a primary **`Browser`**); cookies are **session-scoped to that chat**.
- **🔷** Tool wire name: **`browser`** (title **Browser**).
- **🔷** Actions: **`help`**, **`fetch`**, **`search`** (Google only for now), **`press`**, **`download`** (URL), **`where`** (“where am I?”).
- **🔷** **`fill` is an argument**, not an action — pass a fill **object** (press-ref id → text) on a call (typically with **`action: press`**) to type into fields then activate.
- **🔷** Tool replies return page content (default a11y markdown with inline press refs + References) so the model can continue with **`press`** (+ optional **`fill`**).
- **🔷** UI: **globe toggle** chat ↔ browser on **desktop** so the user can see the live page when needed; default stays on chat.
- **🔷** Port Snappr **target-site Cloudflare detection + UI handoff**; ignore Snappr login / PMAN auth.
- **🔷** **Platform for this plan’s spike:** **Linux** (WebKitGTK) only. **Windows** a11y → **[`5.0.1`](5.0.1-windows-webkit-accessibility.md)**. **Android** → **[`5.0.2`](5.0.2-android-webkit-control.md)**.
- **⏳** Phases 2–3 prove a11y loop → then cookies/CF → Registry → globe → download (see **Phases**).

---

## Library — `libocwebkit`

- **🔷** Own Meson subdir / shared library (same family as `libochf`, `liboctools`).
- **🔷** Holds **both**:
  - **`Browser` / `BrowserStack`** (Snappr names): WebView, load settle, a11y → markdown, fill/press, session cookies, CF block/cleared, stack ownership for show/hide
  - **OLLMchat tool** implementing `OLLMchat.Tool.BaseTool` (and request deserialize / execute) — same library, not a thin wrapper left in `liboctools`
- **🔷** **Depends on `libollmchat`** for tool interfaces (unlike `libochf`, which stays catalog-only with the Hub tool living in `liboctools`).
- **🔷** **Does not** own chat UI / globe toggle (app shell).
- **🔷** Help markdown pages ship **with the library** (`libocwebkit` GResource) — overview + per-action topics; not top-level `resources/tools/`.
- **💩** `liboctools` / Registry only **registers** the tool from `libocwebkit` (link + `register_tool`), does not re-own the Tool class.
- **🔷** Namespace **`OLLMwebkit`**, Meson lib / pkg **`ocwebkit`**.
- **💩** Windows backend (when **5.0.1** lands) lives in this library + **webview2-gtk**, not a second OLLM lib.
- **🔷** Library-local **`libocwebkit/examples/`** — do **not** put this spike in top-level `examples/` (even though other libs historically did).

---

## Platforms (5.0 / 5.0.1 / 5.0.2)

- **🔷** **5.0:** **Linux** WebKitGTK — library, `oc-test-webkit`, then Linux chat tool + globe.
- **🔷** Snappr already browses on **Windows** via **webview2-gtk**, but that path is **not** assumed to support a11y dump/press yet → extend in **[`5.0.1`](5.0.1-windows-webkit-accessibility.md)** (spike + webview2-gtk changes).
- **🔷** **Android** WebView backend + bottom-bar globe → **[`5.0.2`](5.0.2-android-webkit-control.md)**.
- **💩** Keep one tool wire name and markdown contract across platforms; platform backends differ inside the library.

---

## Tool identity

- **🔷** Help opens like Hub: this is a **general-purpose web browser** — search the internet, open pages, download, follow links by pressing refs.
- **🔷** Wire name: **`browser`** — confirmed. Title: **Browser**.
- **🔷** Short tool `description` steers to **`action: "help"`** (overview) when missing from context, and **`action: "help"` + `topic`** before using an unfamiliar action.
- **🔷** Operational details live in library help pages (overview + topics), not in the short `@param` blurb.
- **🔷** Tool class lives in **`libocwebkit`**, implementing OLLMchat tool interfaces.

---

## Actions (v1)

### `help` (action)

- **🔷** **`action: "help"`** — not a boolean-only Hub-style `help: true` as the primary API (may still accept `help: true` as alias for overview — **💩**).
- **🔷** **Overview:** `{"action": "help"}` — short index: what the tool is, default `format=a11y` dump shape, list of actions, “call help for an action name before using it”, prefer `press` over inventing URLs.
- **🔷** **Per-action page:** `{"action": "help", "topic": "<action>"}` (or equivalent arg name) — e.g. `topic: "press"`, `topic: "search"`. Also allow **`topic: "fill"`** and **`topic: "format"`** even though those are arguments, not actions.
- **🔷** Must re-call overview help when that text is missing from context (same rule as Hub). Must call the **topic** help when about to use an action whose page is not in context.
- **🔷** Overview and topic pages are **for the model** — omit engine internals (settle timers, Cloudflare machinery). Light note that the chat keeps one browser session is OK if useful; no CF essay.

### `fetch`

- **🔷** Load a URL in the chat’s WebKit instance.
- **🔷** Wait for load settle (see **Settle**).
- **🔷** Return page content in the requested **output format** (default: accessibility markdown).

### `search`

- **🔷** Args: **`url`** — Google search URL **including the query** (no separate `query` / `engine` in v1).
- **🔷** Use **accessibility** to fill/submit when needed — **not** Snappr’s `search-google.js` / DOM scrape.
- **ℹ️** Under the hood related to **`action: press`** + **`fill`** on homepage controls; exact path is Phase 3.
- **💩** Optional **`engine`** later — omit from schema for now.
- **🔷** After settle, return in the requested **output format** (default: a11y markdown).

### `press` (action)

- **🔷** **`action: "press"`** — activate a press-ref from the last a11y dump.
- **🔷** Which control: argument **`press`** (integer) — the ref from content markers / **References**.
- **🔷** Optional argument **`fill`** may be present on the same call (see below).
- **🔷** Order when both: apply **`fill`** first, then press, then settle, then return **`format`**.
- **🔷** Help recommends **`press`** over **`fetch`** when following an on-page result.
- **ℹ️** Press ids come from the a11y tree even if a later reply used `html` / `markdown` format.

### `fill` (argument only — not an action)

- **🔷** **`fill` is never an `action` value.** It is a tool **argument**: a JSON **object** (associative map) of press-ref id → text.
- **🔷** Shape: `{ "1": "typed text", "3": "other field" }` — keys are press-ref ids (strings in JSON); one text per ref.
- **🔷** Used with **`action: "press"`** so one call can type into fields and then activate (e.g. submit).
- **💩** Whether `fill` is allowed on other actions (e.g. dump-only after fill without press) — optional later; v1 = with **`press`**.
- **🔷** No `{n, value}` object array — avoids awkward/`value` naming and duplicate refs.
- **🔷** Apply all fills via accessibility (not JS form scrape).

### `download` (action)

- **🔷** **`action: "download"`** with a **URL** — download that resource through the browser session (cookies / auth of this chat’s WebKit).
- **💩** Save path / progress / approval UX — follow existing Hub download patterns where it fits; spike or later wiring.
- **ℹ️** Distinct from `fetch` (which returns page content in `format`).

### `where` (action) — “where am I?”

- **🔷** One-word action **`where`** — same idea as Unix **`whoami`** / “who am I?”, but for the browser: **where am I?**
- **🔷** Return the current page in the requested **output format** — no navigation.
- **🔷** Use after challenges, or when the model needs a fresh dump before choosing `press`.

---

## Output formats

Shared option on page-bearing actions (`fetch` / `search` / `press` / current-page) and on `oc-test-webkit` (`--format` / stdin).

- **🔷** **`a11y`** (default) — markdown projection of the accessibility tree (see contract below). Primary format for browse / `press` (+ `fill`).
- **🔷** **`html`** — page HTML via **JavaScript injection** (`outerHTML` / equivalent after settle). Not a11y.
- **🔷** **`markdown`** — HTML→Markdown conversion of that HTML (same idea as `web_fetch` cleanup). May or may not prove useful; keep the option.
- **💩** Other formats later if spike needs them (e.g. raw a11y JSON, plain text only) — do not invent until evidence.
- **💩** Parameter name `format` with values `a11y` | `html` | `markdown` — confirm spelling in help.

---

## Accessibility markdown contract (`format=a11y`)

Two parts: **content first** (structure + inline press pointers), then a **reference index** of those targets.

```markdown
# Page
- URL: …
- Title: …

## Content
… structured projection of the a11y tree …
… e.g. headings, lists, table-ish row/column when the tree suggests it …
… position / layout hints when available (exact wording from spike) …

Click [^press:3] here for the docs, or search [^press:1].

## References
[^press:1]: [textbox] Search — (no url)
[^press:3]: [link] Example result — https://example.com/…
```

- **🔷** **Body first** — readable content with a **standard inline marker** for anything you can `press` (not a separate interactive list at the top).
- **🔷** **References at the bottom** — index of those markers: role, name/label, and **link URL when it is a link** (what the control does / where it goes).
- **🔷** `press` takes the same id as the marker (integer or token — spike picks one stable form).
- **💩** Exact marker syntax (`[^press:N]` vs `[press:N]` vs other) — confirm after seeing real a11y dumps; keep one convention in help.
- **💩** Row/column / position text — emit when the tree gives useful structure; do not invent a fake grid.
- **⏳** Spike (5.0): confirm what **WebKitGTK** yields for Google SERP; tune body + reference shape from evidence.
- **⏳** Windows WebView2 a11y spike → **5.0.1**. Android → **5.0.2**.
- **🚫** Do **not** port Snappr `resources/search-google.js` SERP extraction.
- **🚫** Do **not** lead with a standalone “Interactive 1…N” catalogue ahead of the content.

---

## Settle (implementer only — not model help)

- **ℹ️** Snappr `Browser.await_load` / settle polling uses load progress, `readyState`, CF block, and **per-domain timers** from domain settings — we have **no** domain settings here.
- **🔷** **v1 settle:** after navigation / press, wait a **default settle time** (about **2 seconds**), then dump. Simple fixed delay for the spike and first tool wiring.
- **💩** Exact default `2000` ms — tune from `oc-test-webkit` if pages are still mid-render.
- **💩** Optional override (`settle_ms` / CLI `--settle`) for the test app — confirm if the tool exposes it or only the host/test app does.
- **⏳** Later (not v1): smarter settle — progress / `readyState` (Snappr-style), or a11y “still changing” monitoring. Original Snappr intent leaned that way; defer until fixed delay is painful.
- **🚫** Do not document settle mechanics in **`action: help`** pages.

---

## Ownership and cookies

- **🔷** **One browser per chat instance** (not shared across chats).
- **🔷** Cookies are **session-based for that chat** — WebKit cookie jar for the live view; optional in-memory host→cookie map for re-apply after CF (Snappr `site_cookies` pattern).
- **🔷** No cross-chat cookie sharing.
- **🚫** Do not persist site cookies to disk across app restarts in v1 (Snappr also deferred this).
- **🚫** Ignore Snappr PMAN `session_cookie` / login Cloudflare UI (`UI/Cloudflare.vala`, Auth).

---

## Cloudflare / challenge handoff (implementer only — not model help)

- **🔷** Port Snappr **target-site** detection: main-document response with Cloudflare server / `cf-ray` + challenge-ish status → mark blocked.
- **🔷** When blocked: **surface the WebKit UI** (same globe view) so the user can complete the challenge; resume tool flow after clear (or explicit continue).
- **🔷** After clear: harvest cookies into the session map (Snappr `harvest_site_cookies` / `apply_site_cookies`).
- **⏳** Full product polish of CF UX can come after the tool works; still **port the detection + handoff** early because it is useful.
- **🚫** Ignore Snappr gateway / Media Outreach login CF path.
- **🚫** Do not document Cloudflare detection in **`action: help`** pages. (`where` topic may say “use when unsure of the current page” without naming CF.)

**ℹ️** Snappr files to steal patterns from (not copy wholesale):

- `/home/alan/gitlive/app.Snappr2/src/Rpc/Cloudflare.vala`
- `/home/alan/gitlive/app.Snappr2/src/UI/Browser.vala` (load settle, cookie apply/harvest)
- `/home/alan/gitlive/app.Snappr2/src/UI/BrowserStack.vala` (promote visible on block — adapt to chat toggle)

---

## UI — globe toggle

- **🔷** Globe control toggles **chat view ↔ browser view** (not side-by-side in v1).
- **🔷** **Desktop Linux (5.0):** toggle in the GTK shell; no always-visible split pane yet.
- **🔷** **Windows desktop globe** once **5.0.1** a11y works; **Android bottom-bar globe:** **[`5.0.2`](5.0.2-android-webkit-control.md)**.
- **🔷** Browser view is also shown automatically on CF / user-assist challenges.
- **💩** Later: optional pane visible while the model runs — out of v1.

---

## Relationship to `google_search` / `web_fetch`

- **🔷** This tool is the **interactive browser** path (a11y + real WebKit session).
- **🔷** **Until `browser` is working, `google_search` and `web_fetch` coexist** (stay registered).
- **🔷** After `browser` proves out, we **may retire** those tools — not now.
- **ℹ️** Android W1/W2 assumed “WebKit replaces API search via DOM extract”; **5.0** replaces that approach with **a11y fill + a11y dump**; Android wiring is **5.0.2**.

---

## Help file location (library-owned resources)

**What Hub does today (the only in-tree “tool help” precedent):**

- ℹ️ Source: [`resources/tools/huggingface-help.md`](../../resources/tools/huggingface-help.md)
- ℹ️ Bundled into **`libollmchat`** via [`resources/gresources.xml`](../../resources/gresources.xml) prefix `/tools/` + [`libollmchat/meson.build`](../../libollmchat/meson.build) `glib-compile-resources`
- ℹ️ Loaded by Hub tool: `OLLMchat.Prompt.Template("huggingface-help.md") { source = "resource:///"; base_dir = "tools"; }`
- ℹ️ **`libochf`** has **no** help/gresource — catalog only; help rides the central app/chat resource blob

**What we want for Browser:**

- **🔷** Help sources live **inside `libocwebkit/resources/`** (overview + per-topic files — see **Help system**), not top-level `resources/tools/`.
- **🔷** Preferred packaging: **`libocwebkit` compiles its own GResource** into `libocwebkit.so` so chat app and `oc-test-webkit` get help by **linking the lib**.
  - **💩** `libocwebkit/gresources.xml` prefix `/ocwebkit/` listing `help.md`, `help-press.md`, …
  - **💩** Tool loads the matching resource for overview vs `topic`
- **ℹ️** No second copy in `ollmapp` / top-level `resources/` if linked into the `.so`.
- **💩** Fallback: sourcedir into libollmchat gresources (Hub-style) only if Phase 2 hits a wall.
- **🚫** Do not leave help only under top-level `resources/tools/`.

---

## Help system (overview + per-action pages)

- **🔷** Help is an **action**, not only a boolean.
- **🔷** **Overview** — `{"action": "help"}` — short index for the model.
- **🔷** **Topic page** — `{"action": "help", "topic": "press"}` (etc.) — how to use that action / argument.
- **🔷** Topics at least: `fetch`, `search`, `press`, `download`, `where`, plus **`fill`** and **`format`** (arguments).
- **🔷** Re-fetch overview when missing from context; re-fetch a **topic** page before using that action if that page is not in context.
- **🔷** Model-facing text: **no** settle-timer docs, **no** Cloudflare internals. Those stay in this plan / implementer notes only.
- **💩** Whether overview mentions “same chat keeps browser state” in one line — optional; omit cookie/CF essays.
- **💩** Arg name `topic` vs `name` — confirm at implement; meaning = which help page.
- **ℹ️** Hub still uses `help: true` + one big file — Browser deliberately splits pages.

### Resource files (library GResource)

- **🔷** `libocwebkit/resources/help.md` — overview
- **🔷** `libocwebkit/resources/help-fetch.md`
- **🔷** `libocwebkit/resources/help-search.md`
- **🔷** `libocwebkit/resources/help-press.md` (includes **`fill`** usage — fill is not a separate action)
- **🔷** `libocwebkit/resources/help-download.md`
- **🔷** `libocwebkit/resources/help-where.md`
- **💩** `help-format.md` if format details are long enough to split from overview
- **💩** Prefix e.g. `/ocwebkit/help.md`, `/ocwebkit/help-press.md`

### Overview draft (`help.md`)

```
BROWSER TOOL
---
General-purpose web browser for THIS chat session. Search the web, open pages,
fill forms, follow links via press-refs from the page dump, download files.

NOT a one-shot HTTP API. Prefer the accessibility dump + press/fill over
shell/curl browsing.

MUST — HELP WHEN MISSING
  If this overview is NOT already in your current context (including after a
  conversation summary), call {"action": "help"} first.
  Before using an action whose topic page is not in context, call
  {"action": "help", "topic": "<action>"} (e.g. topic "press", "search").
  A vague recollection is not enough.

ACTIONS (call help with topic for details)
  help      — this index, or topic page when "topic" is set
  fetch     — open a URL
  search    — Google search (v1)
  press     — activate a press-ref; optional fill object on the same call
  download  — download a URL through this browser session
  where     — where am I? dump current page (no navigation)

DEFAULT PAGE DUMP (format "a11y")
  # Page → URL / Title
  ## Content — structured text; press markers inline, e.g. [^press:3]
  ## References — [^press:N]: role, label, URL if link

  Prefer press on a ref from the last dump over fetch with a hand-copied URL
  when the control is already listed. fill is an ARGUMENT on press, never an
  action. For format "html" / "markdown", call {"action": "help", "topic": "format"}.

TYPICAL FLOW
  help → search or fetch → read Content + References →
  press (+ fill) → read dump → repeat → download if needed

OTHER TOOLS
  google_search / web_fetch may still be registered. Prefer this tool for
  multi-step browsing and press/fill. Do not use them to replace press/fill
  on this tool’s dumps.
```

### Topic drafts (summaries — full pages in resource files)

**`topic: fetch`**
- Args: `url` (required), `format` (optional, default a11y)
- When: known URL not already a press-ref
- Example: `{"action": "fetch", "url": "https://example.com/docs"}`
- Returns page dump; then use press/fill from References

**`topic: search`**
- Args: `url` (required Google URL including query), `format`
- Opens Google via accessibility (fill + submit under the hood) when needed
- Then press a result ref — do not invent SERP scraping
- Example: `{"action": "search", "url": "https://www.google.com/search?q=Vala+WebKitGTK"}`

**`topic: press`** (include **fill**)
- Args: `press` (required press id), `fill` (optional object), `format`
- `fill`: `{"1": "…", "3": "…"}` — NOT an action; only with press
- Order: fill entries → press → return dump
- Prefer over fetch when the link/control is in the last References
- Examples: press only; fill+press for forms/search boxes

**`topic: download`**
- Args: `url` (required)
- Saves through this chat’s browser session (distinct from fetch content)
- Prefer over run_command/wget for that URL
- Example: `{"action": "download", "url": "https://example.com/a.pdf"}`

**`topic: where`**
- “Where am I?” — dump current page, no navigation
- Args: `format` optional
- Use when unsure of the current page or after the UI was shown for a challenge
- Example: `{"action": "where"}`

**`topic: format`** (optional separate page)
- `a11y` (default) — Content + References; needed for press/fill
- `html` — page HTML; `markdown` — HTML→MD
- Press/fill need a prior a11y dump with References

**🚫 Model help must not document:** settle millisecond defaults, Cloudflare detection headers, cookie harvest APIs, Snappr internals.

---

## Phases

Class / file names below are the **working inventory** for implementers. Prefer **few classes**; a11y dump / format conversion stay on **`Browser`** / **`Tool`** unless a split is required.

**Naming (Snappr-aligned, locked):**

- **🔷** **`OLLMwebkit.Browser`** — one WebView unit (load, settle, a11y dump, fill/press, cookies). Same role as Snappr `Browser` — **not** called `Host`.
- **🔷** **`OLLMwebkit.BrowserStack`** — owns the **`Gtk.Stack`** (and construction of `Browser`), show/hide for globe + CF promote. Same role as Snappr `BrowserStack`. Chat owns one stack; v1 may only need **`primary`** (no crawl pool unless we add it later).
- **🔷** Tool wire name remains **`browser`** (lowercase) — distinct from the **`Browser`** class.

### Phase 1 — ✅ Design locks

- **✅** Current-page action: **`where`** (“where am I?” — one word, cf. `whoami`).
- **✅** Coexistence: **`google_search`** + **`web_fetch` stay** until `browser` works; **may retire later** if browser succeeds.
- **✅** Help lives under **`libocwebkit`** with library-owned GResource (not top-level `resources/tools/`).
- **✅** Class names: **`Browser`** + **`BrowserStack`** (not `Host`).
- **ℹ️** No implementation work left in this phase. Spike may still tweak press/fill wire field names.

### Phase 2 — ✔️ Scaffold `libocwebkit` + `oc-test-webkit` (Linux) — plan ✅ confirmed

Inventory below is **user-confirmed**. Agent marks **✔️**; user promotes to **✅**.

- **✔️** **🔷** Meson / tree
  - **✔️** `libocwebkit/meson.build` — shared lib `ocwebkit`, deps: gee, gio, glib, gtk4, webkitgtk-6.0 (or project’s WebKitGTK pkg), json-glib, **`libollmchat`**
  - **✔️** `libocwebkit/gresources.xml` + compile into the `.so` (library-owned help)
  - **✔️** `libocwebkit/resources/help.md` + `help-*.md` topic pages (see **Help system**)
  - **✔️** `libocwebkit/namespace.vala` — namespace **`OLLMwebkit`**
  - **✔️** `libocwebkit/examples/meson.build` — executable **`oc-test-webkit`**
  - **✔️** **🔷** Root [`meson.build`](../../meson.build) — `subdir('libocwebkit')` (after `libollmchat`, before or beside `liboctools`)
  - **✔️** **🔷** [`docs/meson.build`](../../docs/meson.build) — valadoc inputs when sources exist
  - **🚫** Do not add help markdown to top-level [`resources/gresources.xml`](../../resources/gresources.xml) as the primary home
- **✔️** **🔷** Core classes (stubs OK)
  - **✔️** `libocwebkit/Browser.vala` — class **`OLLMwebkit.Browser`**
    - owns `WebKit.WebView` (Linux); load URI, **~2s settle**, cookie map hooks (empty OK)
    - a11y dump / fill / press entry points (stub → Phase 3)
  - **✔️** `libocwebkit/BrowserStack.vala` — class **`OLLMwebkit.BrowserStack`**
    - owns `Gtk.Stack` + **`primary`** `Browser` (Snappr pattern; no crawl pool required in v1)
    - show/hide / promote for globe + CF (stub OK until Phase 4/6)
  - **✔️** `libocwebkit/Tool.vala` — class **`OLLMwebkit.Tool`** : `OLLMchat.Tool.BaseTool`
    - wire name **`browser`**, title **Browser**
  - **✔️** `libocwebkit/Request.vala` — class **`OLLMwebkit.Request`** : `OLLMchat.Tool.RequestBase`
    - fields: `action`, `topic` (for `action: help`), `url`, `press`, `format`, `fill` (object map)
- **✔️** **🔷** Example app
  - **✔️** `libocwebkit/examples/oc-test-webkit.vala` — class **`OcTestWebkitApp`**
  - GTK window embedding **`BrowserStack`** / primary WebView; CLI `--help` only is enough to prove link

### Phase 3 — ⏳ 🔷 Spike (`oc-test-webkit`)

- **⏳** **🔷** Prove on Linux: settle → `format=a11y` → Google via **`action=press` + `fill`** → `search` convenience → standalone `press` → try `html` / `markdown`
- **⏳** **🔷** Expand **`Browser`** (same file unless spike forces a split)
  - load + fixed settle (~2000 ms, optional `--settle`)
  - a11y tree → Content + References markdown
  - fill-by-ref + press-by-ref
  - JS `outerHTML` for `format=html`; HTML→MD for `format=markdown` (reuse `web_fetch` / markdown path if already available)
- **⏳** **🔷** Wire **`Tool` / `Request`** execute path for spike actions (`help` stub OK)
- **⏳** **🔷** Expand **`oc-test-webkit`**
  - CLI: `--fetch`, `--search`, `--fill`, `--press`, `--dump`, `--format`, `--settle`
  - **💩** optional stdin recipe lines / `libocwebkit/examples/recipes/*.txt`
- **ℹ️** Linux only — no webview2-gtk a11y work here

### Phase 4 — ⏳ 🔷 Session cookies + Cloudflare handoff (Linux)

- **⏳** **🔷** Cookies on **`Browser`** (and shared map on **`BrowserStack`** if we mirror Snappr crawl sharing later)
  - apply / harvest host→cookie map (Snappr `site_cookies` pattern)
  - WebKit cookie manager read/write
- **⏳** **🔷** Cloudflare
  - **💩** `libocwebkit/Cloudflare.vala` — class **`OLLMwebkit.Cloudflare`**
    - main-document response watch (Snappr `Rpc/Cloudflare.vala` pattern)
    - `is_blocked` / `cleared` (or equivalent signals)
  - **`Browser` / `BrowserStack`** integrate detect + pause; stack **promotes** WebView visible; **`oc-test-webkit`** shows it so user can clear challenge
- **ℹ️** Steal patterns from Snappr; do not port PMAN login CF

### Phase 5 — ⏳ 🔷 Help markdown + Registry wiring (Linux)

- **⏳** **🔷** Help
  - Finalize overview + topic markdown under **`libocwebkit/resources/`** in the library GResource
  - **`Tool`**: `action: help` → overview; `action: help` + `topic` → topic page (load from `/ocwebkit/…` in this `.so`)
- **⏳** **🔷** Registration (register only — no Tool class in `liboctools`)
  - **⏳** [`liboctools/Registry.vala`](../../liboctools/Registry.vala) — `register_tool(new OLLMwebkit.Tool(…))` in `fill_tools`
  - **⏳** [`liboctools/meson.build`](../../liboctools/meson.build) — link / vapidir **`libocwebkit`**
  - **⏳** App / chat bootstrap if tools are registered outside Registry too
  - **🔷** Keep registering **`google_search`** / **`web_fetch`** for now (coexist)
- **⏳** **🔷** Chat owns a **`BrowserStack`**
  - **💩** where it lives: field on chat session / `ChatWidget` / history chat object — pick the existing “one chat instance” owner and attach **`OLLMwebkit.BrowserStack`**
  - **`Tool`** uses that session’s stack / **`primary`** `Browser` (not a global singleton)

### Phase 6 — ⏳ 🔷 Desktop globe toggle + show-on-challenge (Linux)

- **⏳** **🔷** UI (app / `libollmchatgtk` — not inside `libocwebkit` beyond exposing the stack widget)
  - **💩** Globe control on composer / chat bar (next to tool selector patterns where they exist)
  - Chat shell toggles chat view ↔ **`BrowserStack`** (stack already owns visibility of the WebView)
  - show browser automatically when **`Cloudflare`** blocks / user-assist (**`BrowserStack`** promote, Snappr-style)
- **⏳** Files likely touched (exact names from tree at implement time)
  - **💩** [`libollmchatgtk/ChatWidget.vala`](../../libollmchatgtk/ChatWidget.vala) / [`ChatBar.vala`](../../libollmchatgtk/ChatBar.vala) / [`ChatInput.vala`](../../libollmchatgtk/ChatInput.vala)
  - **💩** [`ollmapp/Window.vala`](../../ollmapp/Window.vala) or pane that owns the active chat
- **🚫** No new helper class for “toggle logic” unless the user asks — inline in the shell widgets

### Phase 7 — ⏳ 🔷 `action: download` (URL)

- **⏳** **🔷** On **`Request` / `Tool`**
  - `action: "download"` + `url`
  - download through the chat **`Browser`** session (cookies / auth)
- **⏳** **🔷** On **`Browser`** (or thin download path using WebKit / soup with jar)
  - save file; **💩** path + progress + permission UX — align with Hub download / activity bar where practical
- **⏳** **🔷** Help + `oc-test-webkit` flag for download
- **ℹ️** Distinct from `fetch` (content in `format`)

### Phase 8 — ⏳ 🔷 Windows a11y

- **⏳** **🔷** Tracked in **[`5.0.1-windows-webkit-accessibility.md`](5.0.1-windows-webkit-accessibility.md)**
- **ℹ️** Expect **`webview2-gtk`** changes + **`Browser`** Windows backend; not part of Linux Phase 2–7 fences

### Phase 9 — ⏳ 🔷 Android

- **⏳** **🔷** Tracked in **[`5.0.2-android-webkit-control.md`](5.0.2-android-webkit-control.md)**
- **ℹ️** Android WebView + a11y + bottom-bar globe; same **`browser`** tool / **`Browser` + `BrowserStack`** API surface

---

## LLM notes

- Phase 1 design locks **✅**. Phase 2 scaffold **✔️** (agent built; await user **✅**).
- Tool **`browser`**, classes **`Browser` + `BrowserStack`**, **`Tool` / `Request`**, **`OcTestWebkitApp`** — locked.
- Next: **Phase 3** spike before chat globe (Phase 6).
- v1 settle is a **fixed ~2s delay** — do not port Snappr per-domain settle timers or a11y “still loading” heuristics until a later phase.
- Do **not** put settle / Cloudflare / cookie-harvest internals into **model** help pages — implementer-only (this plan + code).
- Do not put the Tool class in `liboctools` while leaving only a host in `libocwebkit` — user wants them together.
- Do not implement Snappr Google JS scrape under any name.
- Do not require Windows/`webview2-gtk` a11y in **5.0** — use **5.0.1** (Phase 8).
- Do not land Android WebView host work in **5.0** — use **5.0.2** (Phase 9).
- Do not add helper methods in Vala plans unless the user names them.
- Concrete **Remove** / **Replace with** / **Add** fences land under each phase after design sign-off — not in this draft.
