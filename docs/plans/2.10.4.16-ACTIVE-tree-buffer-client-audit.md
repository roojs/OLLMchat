# 2.10.4.16 — Tree client (ACTIVE)

> **Do not update `docs/plans/1.0-summary.md` for this plan.**

**Status:** step 1 **✔️** — **`V2/Tree*`** verbatim copy in tree; **⏳** reshape **`load_file_content`** next

**Pointer:** `docs/guide-to-writing-plans.md`

**Parent:** [`2.10.4.0-ACTIVE-summary.md`](2.10.4.0-ACTIVE-summary.md)

**Related:** [`2.10.4.9-ACTIVE-v2-caller-cutover.md`](2.10.4.9-ACTIVE-v2-caller-cutover.md) — **Backlog B-3, B-8** · [`done/2.10.4.6-DONE-file.md`](done/2.10.4.6-DONE-file.md) — **🚫** no **`tree_factory`** on V2 **`ProjectManager`**

---

## Purpose

- **🔷** **`libocfiles/V2/`** only — **📋** copy **`TreeBase.vala`**, **`Tree.vala`** from shipping, then reshape on the **V2** copies
- **🔷** Tree works on **in-memory data** only — content must come via **RPC** (**`yield file.read()`**) before parse
- **🔷** **🚫** no edits to shipping **`libocfiles/*.vala`**
- **⏳** verbatim **📋** copy checkpoint, then reshape **`V2/TreeBase.load_file_content`**
- **🚫** **`FileHistory`**, **`Diff/*`**, **`libocvector`** — not V2 copies; out of scope here
- **🚫** **`ProjectManager.tree_factory`** / **`tree_cache`** on V2 PM — per [`done/2.10.4.6`](done/2.10.4.6-DONE-file.md); callers use **`new Tree(file)`** (or equivalent) on the **V2** tree class after copy

---

## Principle (🔷 user)

- **🔷** Tree code does **not** touch the filesystem — only **in-memory data**
- **🔷** Client-side tree-sitter is fine when that data arrived via **RPC**
- **🔷** **🚫** not “move AST to daemon” by default
- **🔷** If shipping **`Tree*`** must change → **📋** copy into **`V2/`** and reshape there; shipping stays frozen

---

## Tree — agreed (🔷) vs backlog (⏳)

**🔷 User / prior cutover decision**

- **🔷** Parse on **client** using **`libocfiles/V2/Tree*`** after copy
- **🔷** **`V2/TreeBase.load_file_content`** — in-memory content from **`file.read()`** only; **🚫** no **`buffer.read_async()`** disk path
- **🚫** no **`tree_factory`** on **`V2/ProjectManager`** ([`done/2.10.4.6`](done/2.10.4.6-DONE-file.md))

**🔷 `⏳` Implementation (user-approved workflow: copy → reshape)**

1. **✔️** **📋** Verbatim copy **`TreeBase.vala`**, **`Tree.vala`** → **`libocfiles/V2/`** (commit checkpoint)
2. **⏳** Reshape **`V2/TreeBase.load_file_content`** on the copy only

**ℹ️** All work is on **V2 / `*V2` source copies** in the tree — **🚫** not Meson-linked; shipping **`meson.build`** lists stay frozen until a separate cutover step ([`2.10.4.9`](2.10.4.9-ACTIVE-v2-caller-cutover.md)).

**💩 `⏳` Caller copies (not stated in chat — confirm before build)**

- **💩** **`liboctools/ReadFileV2/Request.vala`** — after step 2, **`new Tree(this.file)`** instead of **`project_manager.tree_factory(this.file)`** (edit the **`*V2`** file only)
- **💩** **`ReadFile/Summarize.vala`** — still shipping; **`ReadFileV2`** uses it today — **🚫** no shipping edit; copy under **`ReadFileV2/`** only if Summarize must move with V2 **`TreeBase`**

**🚫** **`FileChange.resolve_ast_path`** — shipping **`FileChange.vala`**; not this plan

---

## Concrete code proposals

**⏳** After **📋** copy checkpoint — first **`###`**: **`V2/TreeBase.vala`** **`load_file_content`**.
