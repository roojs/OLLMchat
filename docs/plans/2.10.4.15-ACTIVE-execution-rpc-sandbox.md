# 2.10.4.15 — Execution via RPC / sandbox in daemon (ACTIVE)

> **Do not update `docs/plans/1.0-summary.md` for this plan.**

**Status:** not started — wire drafted below; **blocked on [`2.10.4.17`](2.10.4.17-ACTIVE-libocbwrap-sandbox-library.md)** (`libocbwrap` must exist first)

**Pointer:** `docs/guide-to-writing-plans.md` — checklist for plans; proposed Vala follows `docs/coding-standards.md`

**Parent:** [`2.10.4.0-ACTIVE-summary.md`](2.10.4.0-ACTIVE-summary.md)

**Depends on:** [`2.10.4.17-ACTIVE-libocbwrap-sandbox-library.md`](2.10.4.17-ACTIVE-libocbwrap-sandbox-library.md) — daemon links **`libocbwrap`**, **🚫** no sandbox copy under **`ollmfilesd/`**

**Related:** [`2.10.4.9-ACTIVE-v2-caller-cutover.md`](2.10.4.9-ACTIVE-v2-caller-cutover.md) · [`2.10.4.1-ollmfilesd-rpc-api.md`](2.10.4.1-ollmfilesd-rpc-api.md)

---

## Purpose

- **🔷** **`Exec.can_wrap`** + **`Exec.run`** RPC on **`ollmfilesd`** — sandboxed one-shot commands for tools when the app uses the daemon.
- **🔷** Daemon delegates bwrap/overlay/seccomp to **`libocbwrap`** ([`2.10.4.17`](2.10.4.17-ACTIVE-libocbwrap-sandbox-library.md)); overlay → project index via daemon **`FileVerification`** impl.
- **🔷** **`liboctools/RunCommand`** may call **`Exec.run`** when RPC is up; until then keeps **`libocbwrap`** in-process (same library, no RPC).
- **🔷** **`libocmcp/Client/Stdio`** stays on **`libocbwrap`** in the app until Phase B (stdio session RPC — **💩** defer).
- **ℹ️** Orthogonal to V2 caller cutover — **`Exec.*`** can land after **`libocbwrap`** extract.

---

## 🚫 Superseded approach (do not implement)

- **🚫** Copy **`libocfiles/Sandbox/*`** → **`ollmfilesd/Sandbox/`** + renamespace **`OLLMfiles` → `OLLMfilesd`**
- **Reason (🔷):** shared **`libocbwrap`** library replaces duplication; see [`2.10.4.17`](2.10.4.17-ACTIVE-libocbwrap-sandbox-library.md)

---

## Who calls sandbox today

- **`liboctools/RunCommand/Request.vala`** — **`OLLMfiles.Sandbox.Bubble`** (→ **`OLLMbwrap.Bubble`** after **2.10.4.17**)
- **`libocmcp/Client/Stdio.vala`** — bwrap argv + **`RunSeccomp`**
- **`examples/oc-test-bubble.vala`** — manual test

**After `libocbwrap`:** callers link **`libocbwrap`**; **`libocfiles`** supplies **`OverlayVerification`** only.

---

## Daemon pieces (after `libocbwrap`)

### Link shared library

- **`ollmfilesd/meson.build`** — **`ocbwrap_vapi_dep`**, **`--pkg=ocbwrap`** (Linux full / Windows stub — same as app)

### New in `ollmfilesd/`

- **`OverlayVerification.vala`** — **`OLLMfilesd.OverlayVerification : OLLMbwrap.FileVerification`**
  - daemon project-files / DB sync (port of today’s **`Scan.handle_*`** for **`OLLMfilesd.*`**)
- **`Exec.vala`** — RPC relay singleton
  - **`can_wrap`** → **`OLLMbwrap.Bubble.can_wrap()`**
  - **`run`** → build **`SandboxProfile`** from **`project_path`** + params → **`Bubble.exec`** with daemon verification

### Register in `Application.vala`

- **`OLLMrpc.Request.register("Exec", …)`** + **`ExecParams`** in **`CallParam.vala`**

---

## RPC wire — `Exec.*`

**🔷** Object name **`Exec`** — [`2.10.4.1`](2.10.4.1-ollmfilesd-rpc-api.md).

### Phase A — one-shot command (**🔷** — **`RunCommand`** via RPC)

#### `Exec.can_wrap`

```json
{"method":"Exec.can_wrap","params":{}}
```

**Result:** `{ "available": true, "reason": "" }`

#### `Exec.run`

```json
{
  "method": "Exec.run",
  "params": {
    "project_path": "/home/alan/proj/myapp",
    "command": "make test",
    "working_dir": "",
    "network": false,
    "allow_write": ["project"]
  }
}
```

**Params (`ExecParams`)**

- **`project_path`** (string) — empty = no-project mode
- **`command`** (string) — `sh -c` shell string
- **`working_dir`** (string) — absolute or empty; **`playground`** normalized like **`RunCommand`**
- **`network`** (bool)
- **`allow_write`** (string[])

**Result**

```json
{
  "output": "merged stdout/stderr (+ exit hint if non-zero)",
  "exit_code": 0,
  "seccomp_network": "",
  "seccomp_fs": ""
}
```

**Errors:** **`-32001`** project not found · **`-32003`** I/O / spawn · **`-32004`** sandbox unavailable

**🚫 Not on wire:** **`run_as_root`/`sudo`**, client unsandboxed Flatpak/Windows fallback

### Phase B — MCP stdio session (**💩** defer)

- **`Exec.spawn_stdio`**, session write/read, **`event.exec.output`** — see prior sketch; MCP keeps **`libocbwrap`** in-process until approved

---

## Caller changes

### Phase A

- **`RunCommand/Request.vala`** — when daemon connected: **`Exec.run`** RPC; else **`OLLMbwrap.Bubble`** in-process (same **`libocbwrap`**)
- **`run_as_root`** — unchanged (client subprocess)

### Phase B

- **`Stdio.vala`** — optional RPC session; default stays **`libocbwrap`** spawn

---

## Suggested order

1. **⏳** [`2.10.4.17`](2.10.4.17-ACTIVE-libocbwrap-sandbox-library.md) — extract library + **`libocfiles/OverlayVerification`**
2. **⏳** **`ollmfilesd/OverlayVerification.vala`** + link **`libocbwrap`**
3. **⏳** **`Exec.vala`** + **`ExecParams`** + register
4. **⏳** RPC test tranche T3
5. **⏳** **`RunCommand`** RPC path (optional alongside in-process)
6. **⏳** Phase B MCP session — if needed

---

## Still to do ⏳

1. **⏳** **`libocbwrap`** (prerequisite — [`2.10.4.17`](2.10.4.17-ACTIVE-libocbwrap-sandbox-library.md))
2. **⏳** Daemon **`OverlayVerification`**
3. **⏳** **`Exec.can_wrap`** + **`Exec.run`**
4. **⏳** T3 harness
5. **⏳** **`RunCommand`** RPC caller path

---

## Concrete code proposals

**⏳** After **`libocbwrap`** lands — first **`###`**: **`ollmfilesd/CallParam.vala`** **`ExecParams`**, then **`Exec.vala`**, then **`OverlayVerification.vala`**.
