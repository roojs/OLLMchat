# Plan 1.24: OpenAI API chat and embed compatibility

## Overview

Move from Ollama-specific API calls to the OpenAI-compatible endpoints for **chat** and **embed**. Ollama supports the OpenAI API standard for chat (`/v1/chat/completions`) and embeddings (`/v1/embeddings`). Using these endpoints improves portability and allows the app to work with other OpenAI-compatible servers (e.g. OpenRouter, Together, or direct OpenAI) without changing call sites.

**Reference implementation:** `libollmchat/Call/Models.vala` — already uses the OpenAI-compatible pattern: `is_openai = true`, `url_endpoint = "v1/models"`, same connection URL as Ollama-native calls; `build_url()` strips `/api` and appends the endpoint. New chat/embed classes should follow this pattern.

**References:**
- [Ollama OpenAI compatibility](https://docs.ollama.com/api/openai-compatibility)
- [Ollama native /api/chat](https://docs.ollama.com/api/chat)
- OpenAI Chat Completions and Embeddings API (platform.openai.com)

## Status

Phase 1 implemented — new OpenAI API classes and SSE streaming for `exec_stream()` are in place. Outstanding: tests and Phase 2 integration (see §6). Phase 2 per-library change lists are in sub-plans **1.24.1**–**1.24.6** (see §7).

## Coding standards

Proposed code in this plan follows **`.cursor/rules/CODING_STANDARDS.md`**: brace placement (line break for namespaces/classes/methods, inline for control structures; no one-line if with body), `this.` for instance members, `continue` in loops to keep body flat, multiline docblocks with line length ≤ 72 in comments, no one-use temporaries, `GLib.` prefix and no `using`, `Gee.ArrayList`/`Gee.HashMap` via `.get()`/`.set()`/`.add()`.

## Rationale

- **Current:** We use Ollama-native endpoints: `/api/chat` and `/api/embed`. These are not part of the OpenAI API standard.
- **Target:** Use OpenAI-style endpoints: `/v1/chat/completions` and `/v1/embeddings`, following the same pattern as **`Call.Models`**: `is_openai = true` and `url_endpoint = "v1/chat/completions"` (or `"v1/embeddings"`). Then `Call.Base.build_url()` strips `/api` from the connection URL and appends the endpoint, so the same connection (e.g. `http://localhost:11434/api`) works for both Ollama-native and v1 endpoints.
- **Benefits:** One request/response shape for chat and embed across Ollama and other providers; easier to add or switch backends later; aligns with existing use of `v1/models` (see plan 1.17).

## Phased approach

- **Phase 1 (this plan):** Add **separate classes** in the **same namespace** (`OLLMchat.Call`, `OLLMchat.Response`) that represent the OpenAI API request/response shapes and perform the HTTP calls. These classes are **available** (e.g. for tests or direct use) but **not integrated** into Session, agent flow, history, or UI. Existing `Call.Chat`, `Call.Embed`, `Response.Chat`, `Response.Embed` remain unchanged and continue to be used by the app.
- **Phase 2 (later):** Integrate the OpenAI call path into Session/agent (e.g. choose OpenAI vs Ollama per connection, map OpenAI responses into existing UI/history if desired). Not in scope for Phase 1.

## New classes (OpenAI data standards, same namespace)

All new types live under existing namespaces: `OLLMchat.Call` and `OLLMchat.Response`.

### Call layer (OLLMchat.Call)

- **`Call.ChatCompletions`** — New class (separate from `Call.Chat`). Same pattern as **`Call.Models`**: `is_openai = true`, `url_endpoint = "v1/chat/completions"`. Builds request body in OpenAI form (model, messages, stream, max_tokens, temperature, top_p, response_format, tools). Handles SSE streaming and non-streaming. Returns OpenAI-shaped response types (see below). Subclasses `Call.Base`; implements its own request serialization and response parsing for the OpenAI spec.
- **`Call.Embeddings`** — New class (separate from `Call.Embed`). Same pattern as **`Call.Models`**: `is_openai = true`, `url_endpoint = "v1/embeddings"`. Request: model, input (string or array), optional dimensions, optional encoding_format. Returns `Response.EmbeddingList` (or equivalent) with `data[]` and `usage`.

### Response layer (OLLMchat.Response)

New classes that mirror the **OpenAI API response shapes** (so we can serialize/deserialize to and from the exact JSON). Existing `Response.Chat` and `Response.Embed` stay as-is for the Ollama pipeline.

**Chat completions:**

- **`Response.ChatCompletion`** — Top-level response: `id`, `object`, `created`, `model`, `choices` (array of `Response.ChatCompletionChoice`), optional `usage` (`Response.Usage`).
- **`Response.ChatCompletionChoice`** — One choice: `index`, `message` (`Response.ChatCompletionMessage`), `finish_reason`.
- **`Response.ChatCompletionMessage`** — `role`, `content`, optional `tool_calls` (array; structure matches OpenAI).
- **`Response.Usage`** — `prompt_tokens`, `completion_tokens`, `total_tokens` (optional fields as per API).

For streaming, either a dedicated type for a single SSE chunk (e.g. `Response.ChatCompletionChunk` with `choices[].delta`) or parse each SSE line into a known type: `Response.ChatCompletionChunk` (choices → `ChatCompletionChunkChoice` with delta → `ChatCompletionDelta`, finish_reason; delta has content?, role?, tool_calls? → `ToolCallDelta`). Use `Json.gobject_from_data(typeof(Response.ChatCompletionChunk), payload, -1)` in `exec_stream()` and read from the typed object; still accumulate into `Response.ChatCompletionMessage`; plan can specify “streaming returns same message type built from deltas”.

**Embeddings:**

- **`Response.EmbeddingList`** (or `Response.Embeddings` to avoid clash with call name) — Top-level: `object`, `data` (array of `Response.EmbeddingDatum`), `model`, optional `usage` (`Response.Usage`).
- **`Response.EmbeddingDatum`** — `embedding` (array of doubles), `index` (int). Matches OpenAI `data[].embedding` and `data[].index`.

Naming: if `Response.Embeddings` is used for the list, the call class can stay `Call.Embeddings`; both in same namespace is fine. Alternatively name the response type `Response.EmbeddingList` to avoid confusion with the call.

### No new namespace

All of the above live in `OLLMchat.Call` and `OLLMchat.Response`. No `OLLMchat.OpenAI` or similar.

## Current vs OpenAI: summary

| Area        | Current (Ollama)              | OpenAI standard                         |
|------------|--------------------------------|-----------------------------------------|
| Chat URL   | `api/chat`                     | `v1/chat/completions`                    |
| Embed URL  | `api/embed`                    | `v1/embeddings`                         |
| Chat req   | `model`, `messages`, `options` (snake_case), `think`, `keep_alive`, `format` (obj or string) | `model`, `messages`, `max_tokens`, `temperature`, `top_p`, `stream`, `response_format`, etc. |
| Chat resp  | Top-level `message`, `done`, `done_reason`, `eval_count`, `total_duration`, … | `choices[0].message`, `choices[0].finish_reason`, `usage.prompt_tokens` / `completion_tokens` |
| Stream     | NDJSON chunks: `message`, `done`, … | SSE `data:` lines: `choices[0].delta`, `choices[0].finish_reason` |
| Embed req  | `model`, `input`, `dimensions`, `options`, `keep_alive` | `model`, `input`, `dimensions`, `encoding_format` |
| Embed resp | `embeddings` (array of arrays), `total_duration`, `prompt_eval_count` | `data[].embedding`, `data[].index`, `usage` |

---

## 1. Chat: API calls and differences

### 1.1 Endpoint and URL

- **Current:** `Call.Chat` sets `url_endpoint = "chat"`, `is_openai = false` → `{base}/api/chat` (e.g. `http://localhost:11434/api/chat`).
- **Target (new classes):** Follow **`Call.Models`**: `Call.ChatCompletions` sets `is_openai = true`, `url_endpoint = "v1/chat/completions"`. Then `build_url()` strips `/api` from the connection URL and appends the endpoint → `http://localhost:11434/v1/chat/completions`. Same connection URL works as for Models.

### 1.2 Request body (differences)

| Current (Ollama /api/chat)     | OpenAI /v1/chat/completions   | Notes |
|-------------------------------|------------------------------|--------|
| `model`                       | `model`                      | Same |
| `messages`                    | `messages`                   | Same array of `{ role, content }` (and tool_calls / tool results). Content can be string or array of parts (e.g. text + image_url). |
| `stream`                      | `stream`                     | Same |
| `options` (nested object with `temperature`, `top_p`, `num_ctx`, …) | Top-level `temperature`, `top_p`, `max_tokens`, … | OpenAI uses flat params; no `num_ctx` (Ollama-specific; context size is via Modelfile/temp model). |
| `format` (string "json" or object) | `response_format` (e.g. `{ type: "json_object" }` or `{ type: "json_schema", json_schema: {...} }`) | Name and shape differ. |
| `think`                       | —                            | Ollama-only; thinking models may expose reasoning via message content or vendor-specific fields. For OpenAI compat we may omit or map if server supports. |
| `keep_alive`                  | —                            | Ollama-only; not in OpenAI. Omit for v1. |
| `tools`                       | `tools`                      | Same idea (array of tool definitions). |
| —                             | `tool_choice`                | OpenAI supports "none" / "auto" / specific tools. We don’t use today; can add later. |
| —                             | `stop`, `seed`, `presence_penalty`, `frequency_penalty` | We can add where needed. |

**Planned request changes:**

- Add a path (e.g. connection flag or call-level flag) to send chat requests to `v1/chat/completions` with OpenAI-shaped JSON.
- Map our existing `Call.Options` (temperature, top_p, etc.) and `format` / `format_obj` into OpenAI top-level fields and `response_format`.
- Omit or gate Ollama-only fields (`think`, `keep_alive`, `options.num_ctx`) when using the OpenAI endpoint; document that custom context size still requires Modelfile/temp model when talking to Ollama.

### 1.3 Response body (non-streaming)

- **Current:** Single object: `model`, `created_at`, `message` (role, content, thinking?, tool_calls?), `done`, `done_reason`, `total_duration`, `load_duration`, `prompt_eval_count`, `prompt_eval_duration`, `eval_count`, `eval_duration`.
- **OpenAI:** `id`, `object`, `created`, `model`, `choices`: `[{ index, message: { role, content, tool_calls? }, finish_reason }]`, `usage`: `{ prompt_tokens, completion_tokens, total_tokens }`.

**Planned response handling (Phase 1):**

- Parse the response into the **new** OpenAI-shaped types: `Response.ChatCompletion`, `Response.ChatCompletionChoice`, `Response.ChatCompletionMessage`, `Response.Usage`. No conversion to existing `Response.Chat` or `Message` in Phase 1; that belongs in Phase 2 (integration).

### 1.4 Response body (streaming)

- **Current:** NDJSON chunks with `message` (role, content, thinking?, tool_calls?), `done`, `done_reason`, and timing fields. `Response.Chat.addChunk()` expects this shape (e.g. `chunk.get_object_member("message")`).
- **OpenAI:** SSE stream; each `data:` line is a JSON object with `choices`: `[{ index, delta: { content?, role?, tool_calls? }, finish_reason? }]`, `model`, `created`. Content and tool_calls are incremental (deltas).

**Planned streaming (Phase 1):**

- In the **new** `Call.ChatCompletions` class: consume SSE, parse each `data:` line as a chunk object with `choices[].delta`. Accumulate deltas into a single `Response.ChatCompletionMessage` (and optional `Response.Usage` if provided in a final chunk). Expose the accumulated message (and finish_reason) to the caller. No use of existing `Response.Chat.addChunk()` in Phase 1; the new call returns OpenAI-shaped types only.

### 1.5 API calls: new classes (chat)

- **New class `Call.ChatCompletions`** (separate from `Call.Chat`), same URL pattern as **`Call.Models`**:
  - `is_openai = true`, `url_endpoint = "v1/chat/completions"`.
  - Request body in OpenAI form: model, messages, stream, max_tokens, temperature, top_p, response_format, tools (no think, keep_alive, nested options).
  - Non-streaming: parse response into `Response.ChatCompletion` (choices[0].message, usage).
  - Streaming: read SSE, accumulate `choices[0].delta` into `Response.ChatCompletionMessage`, return that (and finish_reason when present).
- **Existing `Call.Chat`** unchanged; continues to use `/api/chat` and `Response.Chat` for the current app flow.

---

## 2. Embed: API calls and differences

### 2.1 Endpoint and URL

- **Current:** `Call.Embed` sets `url_endpoint = "embed"`, `is_openai = false` → `{base}/api/embed`.
- **Target (new class):** Follow **`Call.Models`**: `Call.Embeddings` sets `is_openai = true`, `url_endpoint = "v1/embeddings"` → `build_url()` yields `http://localhost:11434/v1/embeddings` with the same connection.

### 2.2 Request body (differences)

| Current (Ollama /api/embed) | OpenAI /v1/embeddings | Notes |
|-----------------------------|------------------------|--------|
| `model`                     | `model`                | Same |
| `input` (string or array of strings) | `input` (string or array of strings) | Same |
| `dimensions`                | `dimensions`           | Same (optional) |
| `options` (e.g. num_ctx)    | —                      | Ollama-only; omit for v1. |
| `keep_alive`                | —                      | Ollama-only; omit for v1. |
| `truncate`                  | —                      | Ollama-specific; omit for v1. |
| —                           | `encoding_format`      | e.g. "float" / "base64"; we can omit and use default. |

### 2.3 Response body (differences)

- **Current:** `model`, `embeddings`: array of arrays of numbers, `total_duration`, `load_duration`, `prompt_eval_count`. Our `Response.Embed` and `exec_embed()` normalize embeddings and return them in the same structure.
- **OpenAI:** `object`, `data`: array of `{ embedding: number[], index }`, `model`, `usage`: `{ prompt_tokens, total_tokens }`. No nanosecond timings.

**Planned response handling (Phase 1):**

- Parse into the **new** types: `Response.EmbeddingList` (or `Response.Embeddings`) with `data`: array of `Response.EmbeddingDatum` (each with `embedding`, `index`), and optional `usage`. Callers that need a flat list of vectors can read `data[].embedding`; normalization (if desired) can be done in `Call.Embeddings` before returning or left to the caller. No conversion to existing `Response.Embed` in Phase 1.

### 2.4 API calls: new classes (embed)

- **New class `Call.Embeddings`** (separate from `Call.Embed`), same URL pattern as **`Call.Models`**:
  - `is_openai = true`, `url_endpoint = "v1/embeddings"`.
  - Request body: `model`, `input` (string or array), optional `dimensions`, optional `encoding_format`.
  - Parse response into `Response.EmbeddingList` (or `Response.Embeddings`): `data[]` of `Response.EmbeddingDatum`, `model`, optional `usage`.
- **Existing `Call.Embed`** unchanged; continues to use `/api/embed` and `Response.Embed` for the current app flow.

---

## 3. What we need to add (checklist) — Phase 1

- [x] **New response types (OpenAI shapes)**  
  Added: `Response.Usage`, `Response.ChatCompletionMessage`, `Response.ChatCompletionChoice`, `Response.ChatCompletion`, `Response.EmbeddingDatum`, `Response.EmbeddingList`. JSON serialization/deserialization to match the OpenAI API.

- [x] **New `Call.ChatCompletions`**  
  Added in `OLLMchat.Call`. Request JSON for `v1/chat/completions` (model, messages, stream, max_tokens, temperature, top_p, response_format, tools). Non-streaming: `exec()` returns `Response.ChatCompletion`. Streaming: `exec_stream()` implemented with SSE read loop and typed chunk parsing.

- [x] **New `Call.Embeddings`**  
  Added in `OLLMchat.Call`. Sends to `v1/embeddings` (model, input, optional dimensions, encoding_format); `exec_embeddings()` returns `Response.EmbeddingList`.

- [x] **SSE handling**  
  `Call.ChatCompletions.exec_stream()` implemented: `connection.soup.send_async()`, line-by-line SSE read, parse each `data:` line into `Response.ChatCompletionChunk`, accumulate deltas into `Response.ChatCompletionMessage` (content, role, tool_calls, finish_reason). Chunk types: `ChatCompletionChunk`, `ChatCompletionChunkChoice`, `ChatCompletionDelta`, `ToolCallDelta`, `ToolCallDeltaFunction`. `Call.Base.parse_error_from_json` made protected for stream error body parsing.

- [ ] **Tests**  
  Add or extend tests that call `Call.ChatCompletions` and `Call.Embeddings` against local Ollama (or mock) to verify request/response and (when implemented) streaming.

- [x] **No integration in Phase 1**  
  Session, agent, history, UI continue to use existing `Call.Chat` and `Call.Embed` only. The new classes are available for direct use or future wiring (Phase 2).

---

## 4. Files added

**Response (one class per file):** `Response/Usage.vala`, `Response/ChatCompletionMessage.vala`, `Response/ChatCompletionChoice.vala`, `Response/ChatCompletion.vala`, `Response/EmbeddingDatum.vala`, `Response/EmbeddingList.vala`, `Response/ToolCallDeltaFunction.vala`, `Response/ToolCallDelta.vala`, `Response/ChatCompletionDelta.vala`, `Response/ChatCompletionChunkChoice.vala`, `Response/ChatCompletionChunk.vala`.

**Call:** `Call/ChatCompletions.vala`, `Call/Embeddings.vala`.

**Build:** All of the above added to `libollmchat/meson.build` in `ollmchat_ollama_src`. **Changed:** `Call/Base.vala` — `parse_error_from_json` made protected for use from `ChatCompletions.exec_stream()`.

**Unchanged:** `Call.Chat`, `Call.Embed`, `Response.Chat`, `Response.Embed`, Session, agent, history, UI.

---

## 5. Summary table: current vs OpenAI

| Call      | Current endpoint   | Current request highlights     | OpenAI endpoint           | OpenAI request highlights        |
|----------|--------------------|---------------------------------|---------------------------|----------------------------------|
| Chat     | `api/chat`         | options (snake_case), think, keep_alive, format | `v1/chat/completions`     | Flat params, response_format, no think/keep_alive |
| Chat     | —                  | Response: message, done, eval_* | —                         | choices[0].message, finish_reason, usage |
| Embed    | `api/embed`         | input, dimensions, options, keep_alive | `v1/embeddings`           | input, dimensions, (encoding_format) |
| Embed    | —                  | embeddings[][], total_duration  | —                         | data[].embedding, data[].index, usage |

Phase 1 adds **separate** Call and Response classes in the same namespace (`OLLMchat.Call`, `OLLMchat.Response`) that implement the OpenAI API shapes; existing behaviour is unchanged. Integration into Session/agent is a later phase.

---

## 6. Outstanding

- **Tests:** Add or extend tests that call `Call.ChatCompletions.exec()` and `Call.Embeddings.exec_embeddings()` against local Ollama (or mock) to verify request/response. Add tests for `Call.ChatCompletions.exec_stream()` to cover SSE parsing and delta accumulation.

- **Phase 2 (later):** Integrate OpenAI call path into Session/agent (e.g. connection type or flag to use v1 endpoints, map OpenAI responses to existing UI/history if desired). Not in scope for Phase 1.

---

## 7. Code that would need changing (Phase 2) — by method

Phase 2 integration is split by **method** (embed, generate, chat), not by library. Each plan covers **all libraries** that use that method, so one plan can deal with all usages of that section.

| Plan | Method | Summary |
|------|--------|---------|
| **1.24.1** | **Embed** | All embed usages: libollmchat Client, libocvector (Database, VectorBuilder, DocumentationVectorBuilder, Search), examples (config/dimension). Single response type: `Response.Embed` with optional `prompt_tokens`/`total_tokens`. |
| **1.24.2** | **Generate** | All generate usages: libollmchat Client (and any other lib that uses generate). Optional migration to v1 completions. |
| **1.24.3** | **Chat** | All chat usages: libollmchat (Client, Agent, History, Tools), libollmchatgtk (ChatWidget, ChatView), libocvector (VectorBase), liboctools (EditMode, Child/Factory, ReadFile/Tool), examples (oc-test-cli), ollmapp (ollmchat-cli). |

**Phased rollout:** Do **1.24.1 (embed)** first (all libraries), then **1.24.2 (generate)** if desired, then **1.24.3 (chat)**. Each plan is self-contained for that method.

**Sub-plans:** `1.24.1-openai-api-embed.md` (embed), `1.24.2-openai-api-generate.md` (generate), `1.24.3-openai-api-chat.md` (chat). Plans 1.24.4–1.24.6 (liboctools, examples, ollmapp) are stubs that point into 1.24.1 / 1.24.3.

---

**Response/Call type definitions:** For embed we keep `Response.Embed` (add `prompt_tokens`/`total_tokens`); v1 JSON is mapped into it. For chat, Phase 2 can add mappers or an adapter that turns `Response.ChatCompletion`/`Response.ChatCompletionMessage` into `Response.Chat` where needed.
