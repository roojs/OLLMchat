# 8.1 — Bin wire protocol (`libocrpc`)

> **Do not update `docs/plans/1.0-summary.md` for this plan.**

**Status:** in progress (Phase 1 done; Phase 2 next)

**Legend:** **✔️** applied · **⏳** not yet applied · promote **✔️** → **✅** when verified

## At a glance

| ✔️ | Item | Where |
| --- | ---- | ----- |
| ✔️ | Phase 1 codec (`Stream`, `Serializable`, `bin-test` smoke) | [Phase 1](#phase-1--codec-done) |
| ✔️ | Default **`string[]`** + scalar **`string`** (compact / BOXED) | `libocrpc/Bin/Serializable.vala` |
| ✔️ | No payload helpers on **`Stream`** | `libocrpc/Bin/Stream.vala` |
| ✔️ | Extra smoke: **`TestPaths`**, 130-char string, 40k BOXED | `tests/rpc/bin-test.vala` |
| ✔️ | Wire spec: two-form string length, **`string[]`**, BOXED scalar | `docs/bin-rpc-protocol.md` |
| ✔️ | **`write_reg_gtype`**, **`read_gtype`**, **`write_gtype`** `type_byte` param | [§6.1](#61-libocrpcbinstreamvala--write_reg_gtype-symmetric-to-read_reg_gtype) |
| ✔️ partial | **`TestListBag`** class (override pattern; **no** round-trip test) | [§6.2](#62-testsrpcbin-testvala--testlistbag-geearraylist-object-array) |

### Gee.ArrayList — two paths

| Path | Where on wire | Bin approach | Status | Fences |
| ---- | ------------- | ------------ | ------ | ------ |
| ⏳ **A** | **`Response.result`** root arrays (`is_array`, `fetch_files`, …) | Phase 3 envelope — root object array (§15) | ⏳ | [Phase 3](#phase-3--transport-hookup-after-phase-2) |
| ⏳ **B** | List **property** on a wire GObject (`items`, `children`, …) | Override **`bin_write_prop`** / **`bin_read_prop`** — **`switch (prop.name)`** | ✔️ class · ⏳ test | [§6.2](#62-testsrpcbin-testvala--testlistbag-geearraylist-object-array) |

**🚫** Default codec does **not** handle **`Gee.ArrayList`** (throws). **`string[]`** / **`GStrv`** is **not** **`Gee.ArrayList`** — default codec, no override.

Detail: [Gee.ArrayList — bin strategy](#geearraylist--bin-strategy).

### Phase 2 backlog

| ✔️ | § | Item |
| --- | - | ---- |
| ⏳ | [§1](#1-libocrpcbinstreamvala--static-type-registration) | Static **`Stream.register`** |
| ⏳ | [§2](#2-extend-rpc_register-on-each-wire-type) | Extend **`rpc_register()`** on wire types |
| ⏳ | [§3](#3-ollmfilesdfilevala--binserializable--graph-omits) | **`ollmfilesd/File`** — **`Bin.Serializable`** + omits |
| ⏳ | [§4](#4-libocfilesv2filevala--client-binserializable) | Client **`File`** — **`Bin.Serializable`** |
| ⏳ | [§5](#5-libocfilesv2projectmanagervalva--keep-json-registration-phase-2) | **`ProjectManager`** — keep JSON registration |
| ✔️ | [§6.1](#61-libocrpcbinstreamvala--write_reg_gtype-symmetric-to-read_reg_gtype) | **`write_reg_gtype`** / **`read_gtype`** |
| ⏳ | [§6.2](#62-testsrpcbin-testvala--testlistbag-geearraylist-object-array) | **`TestListBag`** + copy pattern to wire types |

### Phase 3 backlog

| ✔️ | § | Item |
| --- | - | ---- |
| ⏳ | [§1](#1-libocrpctransportconnectionvala--own-bin) | **`Connection`** owns **`bin`** |
| ⏳ | [§2](#2-libocrpctransportconnectionvala--on_input_ready-receive-hook) | **`on_input_ready`** receive hook |
| ⏳ | [§3](#3-libocrpcclientvala--own-bin) | **`Client`** owns **`bin`** |
| ⏳ | [§4](#4-libocrpcclientvala--dispatch_line-receive-hook) | **`dispatch_line`** receive hook |

**Wire specification:** [`docs/bin-rpc-protocol.md`](../bin-rpc-protocol.md) (version 3.0) — canonical byte layout; update the spec when the wire format changes, not this plan.

**Related:**

- [`2.10.4.27-ACTIVE-rpc-client-cleanup-and-wire-registration.md`](2.10.4.27-ACTIVE-rpc-client-cleanup-and-wire-registration.md) — wire type registration pattern
- [`8.0-libollmchat-local-gguf-backend.md`](8.0-libollmchat-local-gguf-backend.md) — orthogonal **8.x** workstream

---

## Purpose

Implement the binary payload codec described in [`bin-rpc-protocol.md`](../bin-rpc-protocol.md) in **`libocrpc`**, namespace **`OLLMrpc.Bin`**.

- **🔷** Types: **`Stream`**, **`Serializable`** — **🚫** no separate **`Registry`** or **`Object`** base class.
- **🔷** **`Stream`** — per-connection I/O + JIT wire-name tokens + **`write`** / **`parse`**; **process-wide static** alias → GType catalog via **`Stream.register`** (same call sites as **`rpc_register()`**).
- **🔷** **`Connection`** / **`Client`** own **`bin`** for the channel lifetime — **🚫** not one stream per message.
- **🔷** Primary API: **`write(Serializable)`** outbound, **`parse()`** inbound — root type from wire only.
- **🔷** Default **`bin_write_prop`** / **`bin_read_prop`** — scalars, **`string[]`** (`GStrv`), and nested **`Serializable`** objects.
- **🔷** Override **`bin_write_prop`** / **`bin_read_prop`** — **`Gee.ArrayList`**, other **`T[]`**, **`uint8[]`**, graph/runtime omits — see [Gee.ArrayList — two paths](#geearraylist--two-paths) at top.
- **✔️ 🔷** Phase 1: codec in **`libocrpc`** — **done** (see **Phase 1**).
- **⏳ 🔷** Phase 2: audit the **two live RPC sites**; port wire types from JSON workarounds to **`Bin.Serializable`**; consolidate registration — **before** transport hookup.
- **⏳ 🔷** Phase 3: attach **`bin`** to **`Connection`** / **`Client`**; hook read/write loops; optional **`Debugger`**.
- **🚫** Domain types in **`libocrpc`** — sample types only in **`tests/rpc/bin-test.vala`**.

Proposed Vala uses **early return** and **inverted guards** — **🚫** no **`else`** where the happy path can return first ([`reducing-nesting`](../coding-standards.md#reducing-nesting)).

---

## RPC layer today (JSON)

**ℹ️** Server **`OLLMrpc.Transport.Connection`:**

- Owns **`GLib.SocketConnection`** + **`GLib.IOChannel`** read loop.
- **`write(GLib.Object)`** — **`Json.gobject_serialize`** + NDJSON line.
- **`on_input_ready`** — parse line → **`Request.dispatch(params_node)`** → **`Json.gobject_deserialize`** into **`CallParam`**.

**ℹ️** Client **`OLLMrpc.Client`:**

- Owns **`DataInputStream`** / **`DataOutputStream`** on the socket.
- **`call(Request)`** — serialize request as JSON line; **`dispatch_line`** deserializes **`Response.result`** via process-wide **`OLLMrpc.types`**.

**ℹ️** Registration today ([`2.10.4.27`](2.10.4.27-ACTIVE-rpc-client-cleanup-and-wire-registration.md)):

- Process-wide **`OLLMrpc.register(name, gtype)`** before **`Client`** exists.
- Per-handler **`Request.register`** on server for params types.

**Dual stack until Phase 3 cutover:** keep **`OLLMrpc.register`**, **`rpc_register()`**, **`Json.gobject_*`**, **`Response.result_type`** / **`is_array`**, and **`Request.register`** (handler routing) in place while Phase 2 adds **`Bin.Serializable`** and extends **`rpc_register()`** with static **`Bin.Stream.register`**. Remove JSON payload paths only when Phase 3 hooks **`bin`** on the wire.

---

## Bin integration model

### Ownership

```text
Process-wide (static)
  └── Bin.Stream.register(alias, gtype)   ← alias → local GType; same call sites as OLLMrpc.register / rpc_register()

Connection (server) / Client (client)
  └── Bin.Stream bin                        ← in/out streams + JIT wire-name tokens + write/parse
```

- **🔷** Create **`bin`** once when the channel opens; clear on disconnect.
- **🔷** **Send:** caller invokes **`bin.write(obj)`** when outbound RPC carries a GObject body.
- **🔷** **Receive:** RPC read loop calls **`bin.parse()`** on the hot path (Phase 3).

### Type registration

- **🔷** **`Bin.Stream.register(alias, gtype)`** — **static**, process-wide (alias → local GType for decode).
- **🔷** Each wire type's existing **`rpc_register()`** also calls **`Bin.Stream.register`** — **same places** as **`OLLMrpc.register`** today:
  - **`libocfiles/V2/ProjectManager`** constructor (domain types)
  - **`OLLMrpc.Client`** static construct (**`Error`**, **`Notification`**)
  - **`ollmfilesd`** startup — add **`rpc_register()`** on server wire types and call before listen (server has no **`OLLMrpc.register`** today)
- **🔷** Client and server register the **same wire alias strings**; each maps to its **own** local **`typeof`**.
- **🚫** No **`register_wire_types(Bin.Stream bin)`** — no per-connection type catalog.
- **🚫** No type registration on **`connect()`** / **`start()`** — types are registered before the channel opens.

### Phases

| Phase | Work |
| ----- | ---- |
| **1** | **`Stream`**, **`Serializable`** in **`libocrpc`** — **done** |
| **2** | Fix **`Stream.register`** → static; extend **`rpc_register()`**; audit wire types; **`bin_*`** overrides; **keep JSON** |
| **3** | **`Connection`** / **`Client`** own **`bin`**; read-loop hooks; envelope cutover |

**🚫** Do **not** attach **`bin`** to **`Connection`** / **`Client`** until Phase 2 wire types are audited.

**🚫** Phase 3 does **not** remove JSON-RPC framing until envelope design is settled.

---

## Vala API (`OLLMrpc.Bin`)

### `Stream` (public)

| Method | Role |
| ------ | ---- |
| **`static register(alias, gtype)`** | Process-wide wire alias → GType (from each type's **`rpc_register()`**) |
| **`write(Serializable obj)`** | Outbound root object |
| **`parse()`** | Inbound root object; type from wire |

Also used by **`Serializable`**: **`write_tag`** / **`read_tag`**, **`write_gtype`** / **`parse_object`**.

### `Serializable` (override points, `bin_` prefix)

| Method | Role |
| ------ | ---- |
| **`bin_write`** | Walk properties, write **`TOKEN_END`** |
| **`bin_read`** | **`bin_pre`**, read tags until **`TOKEN_END`**, **`bin_post`** |
| **`bin_pre`** | Inbound hook before property decode (default empty) |
| **`bin_post`** | Inbound hook after property decode (default empty) |
| **`bin_write_prop`** | Per-prop encode — default: scalars, **`string[]`**, nested **`Serializable`**; override for other lists / omits |
| **`bin_read_prop`** | Per-prop decode — default: scalars, **`string[]`**, nested **`Serializable`**; override for other lists / omits |

- **🚫** No private **`bin_*`** helpers on the interface.
- **🚫** No nested **`switch`** in prop encode/decode.
- **🚫** Varint / LEB128 / zigzag.
- **🔷** Prop walk uses **`get_property`** / **`set_property`** / **`find_property`**.

Wire layout details: [`bin-rpc-protocol.md`](../bin-rpc-protocol.md).

---

## ✔️ Phase 1 — codec (done)

✔️ **`libocrpc/Bin/namespace.vala`**, **`Stream.vala`**, **`Serializable.vala`**

✔️ **`libocrpc/meson.build`** — **`Bin/*`** in **`ocrpc_src`**

✔️ **`tests/rpc/bin-test.vala`** + meson target **`test-rpc-bin`** (requires **`tests=true`**)

### ✔️ Phase 1 extensions

✔️ **`Serializable.vala`** — default **`string[]`**; scalar **`string`** compact length + **`BOXED`**; array guard post-switch; inlined read/write (no **`Stream`** payload helpers)

✔️ **`Stream.vala`** — removed **`write_compact_uint`** / **`write_string_payload`** / etc.

✔️ **`bin-test.vala`** — **`TestPaths`**; 130-char string; 40k **`BOXED`** round-trip

✔️ **`docs/bin-rpc-protocol.md`** — two-form string length; **`string[]`**; large scalar → **`BOXED`**

**⏳ 💩** Phase 1 shipped instance **`register`** — Phase 2 §1 moves alias maps to static (see **Phase 2**).

Sources are canonical — no duplicate fences here.

---

## ⏳ Gee.ArrayList — bin strategy

↑ Summary: [Gee.ArrayList — two paths](#geearraylist--two-paths) under **At a glance**.

| Path | Where on wire | Bin approach | Status | Fences |
| ---- | ------------- | ------------ | ------ | ------ |
| ⏳ **A** | **`Response.result`** + **`is_array`** (`fetch_files`, `vector_metadata`, …) | Phase 3 envelope — root object array (§15); element types in Phase 2 | ⏳ | [Phase 3](#phase-3--transport-hookup-after-phase-2) |
| ⏳ **B** | List **property** on wire GObject (`FolderFiles.items`, `Tree.elements`, …) | **`bin_write_prop`** / **`bin_read_prop`** override — **`switch (prop.name)`**, **`default:`** → **`base`** | ✔️ class · ⏳ test | [§6.2](#62-testsrpcbin-testvala--testlistbag-geearraylist-object-array) |

### ✔️ Path B — object-element list wire layout

| Step | Write | Read |
| ---- | ----- | ---- |
| 1 | **`write_tag`** | verify tag |
| 2 | **`write_gtype(T, OBJECT \| 0x80)`** | type byte **`0xD0`** → **`read_gtype()`** |
| 3 | compact count | compact count |
| 4 | **`child.bin_write(ctx)`** per element | **`GLib.Object.new`** + **`bin_read`** per element → **`Gee.ArrayList<T>`** |

### ⏳ Path B — scalar-element lists

| | |
| - | - |
| Same | **`switch (prop.name)`** shape as object lists |
| Type byte | **`GLib.Type.INT \| 0x80`** (etc.) |
| Payload | compact count + per-element scalar inline in **`case`** body |
| Spec | [`bin-rpc-protocol.md`](../bin-rpc-protocol.md) §15 |

### ⏳ Audit — where lists appear

| What | JSON / RPC today | Bin action | Status |
| ---- | ---------------- | ---------- | ------ |
| **`Response.result`** + **`is_array`** | Root JSON array | Path **A** — Phase 3 envelope | ⏳ |
| ✔️ | **`CallParam.args`**, param **`string[]`** | Serialized | Default codec — **no** override | ✔️ |
| **`VectorMetadata.children`** | **`serialize_property`** omit | **`bin_*_prop`** omit (§3) | ⏳ |
| **`Folder.children`** / **`project_files`** | On wire via JSON | **Audit:** omit vs Path **B** override | ⏳ |
| **`Tree.elements`** | Not RPC wire type today | Only if promoted to wire bag | ⏳ |

**✔️ 🚫** No list encode/decode helpers on **`Stream`** beyond **`write_gtype`** / **`read_gtype`** / **`write_reg_gtype`** (§6.1). List logic lives in owning type **`case`** bodies.

---

## ⏳ Phase 2 — wire-type audit (do this next)

Backlog: [Phase 2 backlog](#phase-2-backlog) at top. List strategy: [Gee.ArrayList — bin strategy](#geearraylist--bin-strategy).

### Two RPC sites today

| Site | Role | Registration today |
| ---- | ---- | ------------------ |
| **`ollmfilesd/Application.vala`** | Server — **`Request.register`** maps handler → param type | No **`OLLMrpc.register`**; serializes results with **`Json.gobject_serialize`** |
| **`libocfiles/V2/ProjectManager.vala`** | Client — **`OLLMrpc.register`** before **`new Client()`** | Six result types + **`Error`** / **`Notification`** on **`Client` static construct** |

**Wire aliases (both ends — same strings, local `typeof` per side):**

- `Daemon`
- `Folder`
- `File`
- `FileAlias`
- `FileWithHistory`
- `VectorMetadata`
- `Error`
- `Notification`

**`Request.register` stays** after bin cutover — handler routing, not result typing.

**Becomes redundant only after Phase 3 cutover:**

- Process-wide **`OLLMrpc.register()`** / **`OLLMrpc.types`**
- Per-class **`rpc_register()`** statics on domain types
- **`Response.result_type`** + **`is_array`** for client deserialize typing
- **`Client.dispatch_line`** `types.get(result_type)` branch
- Most **`Json.Serializable` `serialize_property` / `deserialize_property`** omits on wire types

### Bin migration work

| ✔️ | Work | § / path |
| --- | ---- | --------- |
| ✔️ partial | List properties — Path **B** | [§6.2](#62-testsrpcbin-testvala--testlistbag-geearraylist-object-array) |
| ✔️ | **`string[]`** — default codec | default **`bin_*_prop`** |
| ⏳ | Graph/runtime omits | [§3](#3-ollmfilesdfilevala--binserializable--graph-omits) |
| ⏳ | Root array results — Path **A** | [Phase 3](#phase-3--transport-hookup-after-phase-2) |
| ⏳ | Client wire types — **`Bin.Serializable`** | [§4](#4-libocfilesv2filevala--client-binserializable) |
| ⏳ | Extend **`rpc_register()`** + static **`Stream.register`** | [§1](#1-libocrpcbinstreamvala--static-type-registration), [§2](#2-extend-rpc_register-on-each-wire-type) |

**Graph/runtime omit props (§3):** `ollmfilesd/File` — `manager`, `buffer`, `parent`; `ollmfilesd/Daemon` — `app`; `VectorMetadata` — `parent`, `children`; `FileWithHistory` — computed display props. **Audit:** `ollmfilesd/Folder` — `children` / `project_files`.

**🚫** **`Gee.HashMap`** — none on wire payload GObjects.

**Side channels unchanged:** **`Response.msg`** / **`msg_encode`** stay envelope fields.

### ⏳ 1. `libocrpc/Bin/Stream.vala` — static type registration

**⏳ Why:** Alias → GType is **process-wide** (like **`OLLMrpc.register`**), not per-connection. Per-connection **`Stream`** keeps only JIT wire-name tokens (**`names`**, **`name_to_token`**).

**⏳ Where:** Move **`alias_to_gtype`** / **`gtype_to_alias`** off the instance; make **`register`** static.

**⏳ Depends on:** Phase 1 **`Stream`** (instance **`register`** — fix in Phase 2).

#### ⏳ Remove — instance maps + instance `register`

```vala
		private Gee.HashMap<string, GLib.Type> alias_to_gtype =
			new Gee.HashMap<string, GLib.Type> ();
		private Gee.HashMap<GLib.Type, string> gtype_to_alias =
			new Gee.HashMap<GLib.Type, string> ();
```

```vala
		public void register (string alias, GLib.Type gtype) throws GLib.Error
		{
			if (this.alias_to_gtype.has_key (alias)) {
				throw new Error.REGISTRATION (
					"duplicate register of alias '%s'",
					alias
				);
			}

			this.alias_to_gtype.set (alias, gtype);
			this.gtype_to_alias.set (gtype, alias);
		}
```

#### ⏳ Replace with — static maps + static `register`

```vala
		private static Gee.HashMap<string, GLib.Type>? alias_to_gtype;
		private static Gee.HashMap<GLib.Type, string>? gtype_to_alias;

		public static void register(string alias, GLib.Type gtype) throws GLib.Error
		{
			if (alias_to_gtype == null) {
				alias_to_gtype = new Gee.HashMap<string, GLib.Type>();
				gtype_to_alias = new Gee.HashMap<GLib.Type, string>();
			}
			if (alias_to_gtype.has_key(alias)) {
				throw new Error.REGISTRATION(
					"duplicate register of alias '%s'",
					alias
				);
			}
			alias_to_gtype.set(alias, gtype);
			gtype_to_alias.set(gtype, alias);
		}
```

**ℹ️** Instance methods **`write_gtype`**, **`parse_object`**, **`read_reg_gtype`** read the static maps (not **`this.alias_to_gtype`**).

**ℹ️** Update **`tests/rpc/bin-test.vala`** — **`Stream.register(...)`** static, not **`write_bin.register`**.

---
### ⏳ 2. Extend `rpc_register()` on each wire type

**⏳ Why:** Bin alias registration lives in the **same** static methods and call sites as JSON today — no channel-setup catalog.

**⏳ Where:** Each type's existing **`rpc_register()`** + server types that lack one yet.

**⏳ Depends on:** **§1**.

#### ⏳ Remove — `Folder.rpc_register()` (client)

```vala
		public static void rpc_register()
		{
			OLLMrpc.register("Folder", typeof(Folder));
		}
```

#### ⏳ Replace with — `Folder.rpc_register()` (client)

```vala
		public static void rpc_register()
		{
			OLLMrpc.register("Folder", typeof(Folder));
			OLLMrpc.Bin.Stream.register("Folder", typeof(Folder));
		}
```

**ℹ️** Same pattern on **`Daemon`**, **`File`**, **`FileAlias`**, **`FileWithHistory`**, **`VectorMetadata`**, **`Error`**, **`Notification`**.

**ℹ️** Server (**`ollmfilesd`**): add **`rpc_register()`** on wire types (e.g. **`OLLMfilesd.File`**) mapping the **same alias strings** to server **`typeof`**; call from **`OllmfilesdApplication`** startup before listen.

**🚫** Do **not** remove **`OLLMrpc.register()`** in Phase 2 — dual stack until Phase 3.

---
### ⏳ 3. `ollmfilesd/File.vala` — `Bin.Serializable` + graph omits

**⏳ Why:** Port JSON **`serialize_property`** omits (`manager`, `buffer`, `parent`) to **`bin_write_prop`**; inbound mirror in **`bin_read_prop`**.

**⏳ Where:** Class declaration + two override methods.

**⏳ Depends on:** Phase 1 **`Serializable`**.

#### ⏳ Add — implements clause

```vala
	public class File : GLib.Object, Json.Serializable, OLLMrpc.Bin.Serializable
```

#### ⏳ Add — `bin_write_prop` / `bin_read_prop` (graph omits)

```vala
	public override void bin_write_prop(
		OLLMrpc.Bin.Stream ctx,
		GLib.ParamSpec prop
	) throws GLib.Error
	{
		switch (prop.name) {
			case "manager":
			case "buffer":
			case "parent":
				return;
			default:
				base.bin_write_prop(ctx, prop);
				return;
		}
	}

	public override void bin_read_prop(
		OLLMrpc.Bin.Stream ctx,
		GLib.ParamSpec prop,
		uint8 type_byte
	) throws GLib.Error
	{
		switch (prop.name) {
			case "manager":
			case "buffer":
			case "parent":
				return;
			default:
				base.bin_read_prop(ctx, prop, type_byte);
				return;
		}
	}
```

**ℹ️** Repeat the same omit pattern on **`ollmfilesd/Daemon`** (`app`), **`OLLMvector2/SQT/VectorMetadata`** (`parent`, `children`), and audit **`ollmfilesd/Folder`** `children` / `project_files`.

---
### ⏳ 4. `libocfiles/V2/File.vala` — client `Bin.Serializable`

**⏳ Why:** Client **`File`** has no **`Json.Serializable`** today; needs **`Bin.Serializable`** + same shape overrides as server for round-trip.

**⏳ Where:** Class declaration; add overrides as needed for client-only transient props.

**⏳ Depends on:** **§3** omit pattern.

#### ⏳ Add — implements clause

```vala
	public class File : GLib.Object, OLLMrpc.Bin.Serializable
```

**ℹ️** Same for **`Folder`**, **`FileAlias`**, **`FileWithHistory`**, **`SQT.VectorMetadata`** under **`libocfiles/V2/`**.

---
### ⏳ 5. `libocfiles/V2/ProjectManager.vala` — keep JSON registration (Phase 2)

**⏳ Why:** Dual stack — JSON path stays live until Phase 3 attaches **`bin`**. Constructor **`rpc_register()`** block unchanged; Phase 2 extends each **`rpc_register()`** body (§2), not this call site.

**⏳ Where:** Constructor — **no change** to the call list in Phase 2.

**⏳ Depends on:** **§2**.

**ℹ️** Constructor keeps:

```vala
			OLLMrpc.Daemon.rpc_register();
			Folder.rpc_register();
			File.rpc_register();
			FileAlias.rpc_register();
			FileWithHistory.rpc_register();
			SQT.VectorMetadata.rpc_register();
```

Phase 3 creates **`this.rpc.bin`** on **`connect()`** — **no** extra registration there.

---
### ⏳ 6. `bin_write_prop` / `bin_read_prop` — list overrides (`Gee.ArrayList`, …)

**⏳ Why:** Default walk covers scalars, **`string[]`**, and nested **`Serializable`** objects only. **`Gee.ArrayList`**, other **`T[]`**, **`uint8[]`**, and list wrappers (**`FolderFiles.items`**, …) need per-type overrides — **`switch (prop.name)`** on write and read, **`default:`** → **`base.bin_*_prop`**.

**⏳ Where:** Every wire type with a list prop or list-shaped wrapper. Audit **`ollmfilesd/Application.vala`** handlers → param/result bag types.

**⏳ Depends on:** **§3**, **§4**, **§6.1** (**`write_reg_gtype`** + **`write_gtype`** type-byte param).

**ℹ️** Wire layouts: [`bin-rpc-protocol.md`](../bin-rpc-protocol.md) §15.

**ℹ️** **`string[]`** (**`CallParam.args`**, **`FolderParams.paths`**, …) stays in the **default** codec — **no** override case.

**ℹ️** **`VectorMetadata.children`** — omit (§3), not a list encode.

**ℹ️** **`ollmfilesd/Folder`** **`children`** / **`project_files`** — audit: omit vs encode **`FolderFiles.items`** / **`ProjectFiles`** flat list; copy the **`TestListBag`** pattern from **§6.2** once decided.

**ℹ️** Root array **results** (**`Response.result`** = **`File[]`**, …) are Phase 3 **envelope** work — separate from property overrides on bag types.

**ℹ️** Encode/decode bodies are **inline in the `case`** — **🚫** no nested **`switch`** on wire type inside the prop handler.

---
#### ✔️ 6.1 `libocrpc/Bin/Stream.vala` — `write_reg_gtype` (symmetric to `read_reg_gtype`)

**✔️ Why:** **`write_gtype`** inlines **`TOKEN_REG_TYPE`** (`0xFF` `0xFE` + `reg_id` + alias) today; **`read_reg_gtype`** already exists for the read side. Factor the write half out — **one** internal method, same name pattern. §15 object arrays then call **`write_gtype(typeof, GLib.Type.OBJECT | 0x80)`** — **🚫** no new public helper.

**✔️ Where:** **`Stream.vala`** — add **`write_reg_gtype`** and **`read_gtype`**; replace **`write_gtype`** and **`parse_object`**.

**✔️ Depends on:** Phase 1 **`Stream`**.

##### ✔️ Add — before `write_gtype`

```vala
		/**
		 * Introduce a type alias on the wire ({@link TOKEN_REG_TYPE}).
		 */
		internal void write_reg_gtype (GLib.Type object_type) throws GLib.Error
		{
			if (this.gtype_to_alias[object_type] == null) {
				throw new Error.REGISTRATION (
					"Unregistered class type schema: %s",
					object_type.name ()
				);
			}

			if (this.name_to_token.has_key (
				this.gtype_to_alias[object_type]
			)) {
				return;
			}

			var new_reg_id = (uint) this.names.length;

			this.out_stream.put_byte (0xFF);
			this.out_stream.put_byte (0xFE);
			if (new_reg_id < 128) {
				this.out_stream.put_byte ((uint8) new_reg_id);
			} else {
				this.out_stream.put_byte (
					(uint8) (0x80 | ((new_reg_id >> 8) & 0x7F))
				);
				this.out_stream.put_byte (
					(uint8) (new_reg_id & 0xFF)
				);
			}

			this.out_stream.put_byte (
				(uint8) uint.min (
					this.gtype_to_alias[object_type].length,
					255
				)
			);
			size_t written;
			this.out_stream.write_all (
				((uint8[]) this.gtype_to_alias[object_type])[
					0:uint.min (
						this.gtype_to_alias[object_type].length,
						255
					)
				],
				out written
			);

			this.names += this.gtype_to_alias[object_type];
			this.name_to_token.set (
				this.gtype_to_alias[object_type],
				(uint16) new_reg_id
			);
		}
```

##### ✔️ Remove — `write_gtype`

```vala
		public void write_gtype (GLib.Type object_type) throws GLib.Error
		{
			if (this.gtype_to_alias[object_type] == null) {
				throw new Error.REGISTRATION (
					"Unregistered class type schema: %s",
					object_type.name ()
				);
			}

			if (!this.name_to_token.has_key (this.gtype_to_alias[object_type])) {
				var new_reg_id = (uint) this.names.length;

				this.out_stream.put_byte (0xFF);
				this.out_stream.put_byte (0xFE);
				if (new_reg_id < 128) {
					this.out_stream.put_byte ((uint8) new_reg_id);
				} else {
					this.out_stream.put_byte (
						(uint8) (0x80 | ((new_reg_id >> 8) & 0x7F))
					);
					this.out_stream.put_byte ((uint8) (new_reg_id & 0xFF));
				}

				this.out_stream.put_byte (
					(uint8) uint.min (
						this.gtype_to_alias[object_type].length,
						255
					)
				);
				size_t written;
				this.out_stream.write_all (
					((uint8[]) this.gtype_to_alias[object_type])[
						0:uint.min (
							this.gtype_to_alias[object_type].length,
							255
						)
					],
					out written
				);

				this.names += this.gtype_to_alias[object_type];
				this.name_to_token.set (
					this.gtype_to_alias[object_type],
					(uint16) new_reg_id
				);
			}

			this.out_stream.put_byte ((uint8) GLib.Type.OBJECT);
			var reg_id = (uint) this.name_to_token[
				this.gtype_to_alias[object_type]
			];
			if (reg_id < 128) {
				this.out_stream.put_byte ((uint8) reg_id);
				return;
			}

			this.out_stream.put_byte (
				(uint8) (0x80 | ((reg_id >> 8) & 0x7F))
			);
			this.out_stream.put_byte ((uint8) (reg_id & 0xFF));
		}
```

##### ✔️ Replace with — `write_gtype` — `type_byte` param; delegate intro to `write_reg_gtype`

```vala
		public void write_gtype (
			GLib.Type object_type,
			uint8 type_byte = (uint8) GLib.Type.OBJECT
		) throws GLib.Error
		{
			if ((type_byte & 0x7F) != (uint8) GLib.Type.OBJECT) {
				throw new Error.PROTOCOL (
					"write_gtype type_byte 0x%02X is not object",
					type_byte
				);
			}

			this.write_reg_gtype (object_type);

			if (this.gtype_to_alias[object_type] == null) {
				throw new Error.REGISTRATION (
					"Unregistered class type schema: %s",
					object_type.name ()
				);
			}

			this.out_stream.put_byte (type_byte);
			var reg_id = (uint) this.name_to_token[
				this.gtype_to_alias[object_type]
			];
			if (reg_id < 128) {
				this.out_stream.put_byte ((uint8) reg_id);
				return;
			}

			this.out_stream.put_byte (
				(uint8) (0x80 | ((reg_id >> 8) & 0x7F))
			);
			this.out_stream.put_byte ((uint8) (reg_id & 0xFF));
		}
```

##### ✔️ Add — after `write_gtype`

```vala
		/**
		 * Read {@code reg_id} after an object type byte; return registered gtype.
		 */
		public GLib.Type read_gtype () throws GLib.Error
		{
			var reg_b = this.in_stream.read_byte ();
			var reg_id = (uint) reg_b;
			if ((reg_b & 0x80) != 0) {
				reg_id = ((uint) (reg_b & 0x7F) << 8)
					| this.in_stream.read_byte ();
			}

			if (reg_id >= this.names.length) {
				throw new Error.PROTOCOL (
					"unknown wire name token %u",
					reg_id
				);
			}
			if (this.alias_to_gtype[this.names[reg_id]] == 0) {
				throw new Error.REGISTRATION (
					"Unrecognized type alias: %s",
					this.names[reg_id]
				);
			}

			return this.alias_to_gtype[this.names[reg_id]];
		}
```

##### ✔️ Remove — `parse_object`

```vala
		public Serializable parse_object () throws GLib.Error
		{
			var reg_b = this.in_stream.read_byte ();
			var reg_id = (uint) reg_b;
			if ((reg_b & 0x80) != 0) {
				reg_id = ((uint) (reg_b & 0x7F) << 8)
					| this.in_stream.read_byte ();
			}

			if (reg_id >= this.names.length) {
				throw new Error.PROTOCOL (
					"unknown wire name token %u",
					reg_id
				);
			}
			if (this.alias_to_gtype[this.names[reg_id]] == 0) {
				throw new Error.REGISTRATION (
					"Unrecognized type alias: %s",
					this.names[reg_id]
				);
			}

			var obj = (Serializable) GLib.Object.new (
				this.alias_to_gtype[this.names[reg_id]]
			);
			obj.bin_read (this);
			return obj;
		}
```

##### ✔️ Replace with — `parse_object` — call `read_gtype`

```vala
		public Serializable parse_object () throws GLib.Error
		{
			var element_type = this.read_gtype ();

			var obj = (Serializable) GLib.Object.new (element_type);
			obj.bin_read (this);
			return obj;
		}
```

**ℹ️** **`write_reg_gtype`** / **`read_reg_gtype`** — **`TOKEN_REG_TYPE`** intro (§5). **`write_gtype`** / **`read_gtype`** — type byte + **`reg_id`** (§6, §14, §15).

---

---

#### ✔️ 6.2a `tests/rpc/bin-test.vala` — `TestListBag` class

##### ✔️ Add — class (after `TestPaths`)

```vala
	public class TestListBag : GLib.Object, OLLMrpc.Bin.Serializable
	{
		public string label { get; set; default = ""; }
		public Gee.ArrayList<TestPair> items {
			get; set; default = new Gee.ArrayList<TestPair> ();
		}

		public override void bin_write_prop (
			OLLMrpc.Bin.Stream ctx,
			GLib.ParamSpec prop
		) throws GLib.Error
		{
			switch (prop.name) {
				case "items":
					var val = GLib.Value (prop.value_type);
					this.get_property (prop.name, ref val);
					var list = (Gee.ArrayList<TestPair>) val;
					ctx.write_tag (prop.name);
					ctx.write_gtype (
						typeof (TestPair),
						(uint8) GLib.Type.OBJECT | 0x80
					);
					if (list.size < 128) {
						ctx.out_stream.put_byte ((uint8) list.size);
					} else {
						ctx.out_stream.put_byte (
							(uint8) (0x80 | ((list.size >> 8) & 0x7F))
						);
						ctx.out_stream.put_byte (
							(uint8) (list.size & 0xFF)
						);
					}
					foreach (var child in list) {
						child.bin_write (ctx);
					}
					return;
				default:
					base.bin_write_prop (ctx, prop);
					return;
			}
		}

		public override void bin_read_prop (
			OLLMrpc.Bin.Stream ctx,
			GLib.ParamSpec prop,
			uint8 type_byte
		) throws GLib.Error
		{
			switch (prop.name) {
				case "items":
					if ((type_byte & 0x7F) != GLib.Type.OBJECT
						|| (type_byte & 0x80) == 0) {
						throw new OLLMrpc.Bin.Error.PROPERTY (
							"prop '%s' expected object array",
							prop.name
						);
					}
					if (ctx.read_gtype () != typeof (TestPair)) {
						throw new OLLMrpc.Bin.Error.PROPERTY (
							"prop '%s' expected TestPair elements",
							prop.name
						);
					}
					var count = (uint) ctx.in_stream.read_byte ();
					if ((count & 0x80) != 0) {
						count = ((count & 0x7F) << 8)
							| ctx.in_stream.read_byte ();
					}
					var list = new Gee.ArrayList<TestPair> ();
					for (var i = 0; i < count; i++) {
						var child = (TestPair) GLib.Object.new (
							typeof (TestPair)
						);
						child.bin_read (ctx);
						list.add (child);
					}
					var val = GLib.Value (prop.value_type);
					val = list;
					this.set_property (prop.name, val);
					return;
				default:
					base.bin_read_prop (ctx, prop, type_byte);
					return;
			}
		}
	}
```

#### ⏳ 6.2b `tests/rpc/bin-test.vala` — `TestListBag` round-trip in `main`

##### ⏳ Add — round-trip in `main` (before final `return 0`)

```vala
			mem = new GLib.MemoryOutputStream.resizable ();
			out_stream = new GLib.DataOutputStream (mem);
			write_bin = new OLLMrpc.Bin.Stream (null, out_stream);
			write_bin.register ("TestListBag", typeof (TestListBag));
			write_bin.register ("TestPair", typeof (TestPair));

			var list_src = new TestListBag () {
				label = "bag",
				items = new Gee.ArrayList<TestPair> (),
			};
			list_src.items.add (new TestPair () {
				name = "one",
				count = 1,
			});
			list_src.items.add (new TestPair () {
				name = "two",
				count = 2,
			});
			write_bin.write (list_src);
			out_stream.close ();

			bytes = mem.steal_as_bytes ();
			in_base = new GLib.MemoryInputStream.from_bytes (bytes);
			in_stream = new GLib.DataInputStream (in_base);
			read_bin = new OLLMrpc.Bin.Stream (in_stream, null);
			read_bin.register ("TestListBag", typeof (TestListBag));
			read_bin.register ("TestPair", typeof (TestPair));

			var list_dst = read_bin.parse () as TestListBag;
			if (list_dst == null) {
				GLib.printerr ("list bag parse returned null\n");
				return 1;
			}
			if (list_dst.label != "bag") {
				GLib.printerr ("list bag label mismatch\n");
				return 1;
			}
			if (list_dst.items.size != 2) {
				GLib.printerr ("list bag items size mismatch\n");
				return 1;
			}
			if (
				list_dst.items.get (0).name != "one"
				|| list_dst.items.get (0).count != 1
				|| list_dst.items.get (1).name != "two"
				|| list_dst.items.get (1).count != 2
			) {
				GLib.printerr ("list bag element mismatch\n");
				return 1;
			}
```

**ℹ️** Wire types with graph omits **and** list props merge all **`case`** branches in **one** **`switch (prop.name)`** per type (§3 + §6.2).

**ℹ️** Scalar-element arrays (**`int[]`**, **`Gee.ArrayList<int>`**, …) use the §15 short-list layout — same **`switch (prop.name)`** shape; type byte **`GLib.Type.INT | 0x80`**, compact count, then per-element scalar payloads inline in the **`case`** body.

**⏳ ℹ️** Copy **§6.2** to each audited wire type (**`ollmfilesd/Vector/Tree.elements`**, **`Folder.children`** once audit picks encode, …) — change prop name, element **`typeof`**, and **`Gee.ArrayList<T>`** type only.

---

## ⏳ Phase 3 — transport hookup (after Phase 2)

**🚫** Do **not** start Phase 3 until Phase 2 wire types implement **`Bin.Serializable`** and static **`Stream.register`** via **`rpc_register()`**.

Backlog:

- **`Connection.on_input_ready`** / **`Client.dispatch_line`** receive hooks
- **`Request.param`** / **`Response.result`** bin cutover on the wire
- Remove JSON payload paths ( **`OLLMrpc.register`**, **`rpc_register()`**, **`result_type`** / **`is_array`** ) once envelope is stable
- Optional **`Bin.Debugger`**

### ⏳ 1. `libocrpc/Transport/Connection.vala` — own `bin`

**⏳ Why:** Server channel holds one **`Bin.Stream`** for the connection lifetime.

**⏳ Where:** **`Connection`** field + end of successful **`start()`**.

**⏳ Depends on:** Phase 2 static **`Stream.register`** (via **`rpc_register()`**).

#### ⏳ Add — field on `Connection`

```vala
		public Bin.Stream? bin { get; private set; }
```

#### ⏳ Add — end of successful `start()` setup

```vala
			var in_stream = new GLib.DataInputStream(this.stream.get_input_stream());
			var out_stream = new GLib.DataOutputStream(this.stream.get_output_stream());
			this.bin = new Bin.Stream(in_stream, out_stream);
```

**🚫** Phase 3 step 1 does **not** hook **`on_input_ready`** to **`bin.parse()`** until envelope design is ready.

---
### ⏳ 2. `libocrpc/Transport/Connection.vala` — `on_input_ready` receive hook

**⏳ Why:** Replace JSON params deserialize with **`bin.parse()`** on the hot path.

**⏳ Where:** **`on_input_ready`** after line parse (envelope TBD).

**⏳ Depends on:** **§1**, Phase 2 param types, envelope design.

**🚫** No **Remove** / **Replace with** fences here until the envelope is defined — then paste verbatim **Remove** of the JSON params decode block from **`on_input_ready`** and **Replace with** the bin decode block (same rule as [`guide-to-writing-plans.md`](../guide-to-writing-plans.md)).

---
### ⏳ 3. `libocrpc/Client.vala` — own `bin`

**⏳ Why:** Client holds one **`Bin.Stream`** for the connection lifetime.

**⏳ Where:** Field + **`connect()`** / **`disconnect()`**.

**⏳ Depends on:** Phase 2 static **`Stream.register`**.

#### ⏳ Add — field

```vala
		public Bin.Stream? bin { get; private set; }
```

#### ⏳ Add — after `this.output` assigned in `connect()`

```vala
			this.bin = new Bin.Stream(this.input, this.output);
```

#### ⏳ Add — clear in `disconnect()`

```vala
			this.bin = null;
```

---
### ⏳ 4. `libocrpc/Client.vala` — `dispatch_line` receive hook

**⏳ Why:** Replace **`Json.gobject_deserialize`** + **`types.get(result_type)`** with **`bin.parse()`**.

**⏳ Where:** **`dispatch_line`** result decode branch.

**⏳ Depends on:** **§3**, Phase 2 result types, envelope design.

**🚫** No **Remove** / **Replace with** fences here until the envelope is defined — then paste verbatim **Remove** of the JSON result decode block from **`dispatch_line`** and **Replace with** the bin decode block.

---
## Suggested order

1. ✔️ ~~Phase 1 — **`Stream`** + **`Serializable`**~~ — done
2. ✔️ ~~Phase 1 extensions — **`string[]`**, BOXED scalar, bin-test extras, wire spec~~ — done
3. ✔️ ~~Smoke: **`test-rpc-bin`**~~ — done
4. ⏳ **Gee.ArrayList** — [two paths](#geearraylist--two-paths) at top; approve **§6.2** fences
5. ⏳ **Phase 2** — static **`Stream.register`**; extend **`rpc_register()`**; **`Bin.Serializable`** on server + client wire types; **`bin_*`** overrides; **keep JSON**
6. ⏳ **Phase 3** — attach **`bin`** on **`Connection`** / **`Client`**; hook read loops; cut JSON payloads

---

## Rejected (do not implement)

- **🚫** Standalone **`Stream.to_output`** / **`from_input`**
- **🚫** Separate **`Bin.Registry`** class
- **🚫** **`bin_registry`** field beside **`bin`**
- **🚫** **`register_wire_types(Bin.Stream bin)`** — use **`rpc_register()`** + static **`Stream.register`**
- **🚫** Per-instance type catalog (**`bin.register(alias, gtype)`** on a connection)
- **🚫** **`write_short_list_count`**, **`write_string_payload`**, and similar **payload** helpers on **`Stream`**
- **💩** **`write_object_array_element_type`**, **`read_object_array_element_type`**, and other narrative **four-word** “helper” names on **`Stream`** — use **`write_gtype`** / **`read_gtype`** and **`write_reg_gtype`** / **`read_reg_gtype`** ([`method-names-new-methods`](../coding-standards.md#method-names-and-new-methods))
- **🚫** **`write_prop`** / **`read_prop`** without **`bin_`** prefix
- **🚫** Nested **`switch`** in prop encode/decode
- **🚫** Varint / LEB128 / zigzag
- **🚫** **`parse(GLib.Type)`** — root type from wire only
- **🚫** Domain **`FileNode`** / **`DirectoryNode`** in **`libocrpc`**
- **🚫** New **`Stream`** per message
- **🚫** Trailing **`## Concrete code proposals`** chapter duplicating phase sections

---

## LLM notes

- **💩** Server **`Connection`** may need **`DataInputStream`** wrapper — **`IOChannel`** path differs from **`Client`**.
- **💩** **`ollmfilesd`** wire types need new **`rpc_register()`** statics — server has no **`ProjectManager`**-style block today.
- **💩** **`Stream`** constructor accepts nullable in/out streams for memory-only tests (**`null`** for unused direction).
- **💩** Put fences in the **phase section** that owns the work — **not** a second proposals chapter at the bottom.