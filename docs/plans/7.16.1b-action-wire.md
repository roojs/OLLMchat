# 7.16.1b — Wire `Action.*` into `Details`

**Status:** **in progress** — extraction + build landed in **7.16.1** merge; **`run_action`** dispatch and **`Details`** trim **remaining**

**Parent:** [`7.16-iterative-task-execution-loop.md`](7.16-iterative-task-execution-loop.md)

**Depends on:** [`7.16.1-action-copy.md`](7.16.1-action-copy.md) ✅

**Blocks:** [`7.16.2-session-markers-replay.md`](7.16.2-session-markers-replay.md)

**Pointer:** `docs/coding-standards.md` — Checklist for all plans · **Layout:** `docs/guide-to-writing-plans.md`

> **Note:** `docs/plans/1.0-summary.md` is not updated until **7.16.1** (copy + wire) is done and archived.

---

## Purpose

- **🔷** Wire Action classes into live execution end-to-end.
- **🔷** **`Details.run_exec`** → **`yield this.run_action ()`** (or equivalent dispatch).
- **🔷** Trim duplicated bodies from **`Details`** once wired ( **`run_post_exec`**, **`post_exec_prompt`**, **`run_exec`** loop).

---

## Done (merged with 7.16.1 — `11d40d40`)

### Build

- **✅** Action sources in **`liboccoder/meson.build`** — order: **`Base`**, **`PostExamMerge`**, **`ExamSequence`**, **`RunTools`**, **`RefOnly`**, **`WriteExec`**, then **`Task/ResultParser.vala`**.

### `Action.Base` / visibility

- **✅** **`Action.Base : OLLMchat.Agent.Base`** — **`fill_model`**, **`chat_call`**, **`replace_chat(task.chat())`** available on actions.
- **✅** **`PostExamMerge.run`** uses **`this.fill_model()`** / **`this.chat_call`** (not **`this.task.*`** delegates).

### Extraction wiring (behaviour-preserving move from **`ResultParser`**)

- **✅** **`Task.Tool.run`** → **`WriteExec.extract_result`** or **`RefOnly.extract_result`** by skill tools.
- **✅** **`Details.run_post_exec`** → **`PostExamMerge.extract(parser)`** only (LLM loop still on **`Details`**).
- **✅** **`Skill.Runner`** replay hydration — same Action extract dispatch.
- **✅** **`ResultParser`** — executor methods removed; **`document`** public read-only for Action parsers.

See [`7.16.1-action-copy.md`](7.16.1-action-copy.md) **Design revisions** for the approved **`Base`** extraction API.

---

## Remaining

- **⏳** **`Details.run_action`** dispatch — pick **`ExamSequence`**, **`RunTools`**, **`RefOnly`**, or **`WriteExec`** from **`build_run_queue`** path (see parent **Architecture**).
- **⏳** **`Details.run_exec`** → delegate to chosen Action **`run()`**; stop duplicating the foreach / post-exec gate on **`Details`**.
- **⏳** **`Details.run_post_exec`** → delegate to **`PostExamMerge.run()`** (drop duplicate LLM retry loop on **`Details`**).
- **⏳** Remove **`Details.post_exec_prompt`** once **`PostExamMerge`** is sole owner.
- **⏳** Acceptance: paths **A / B / C / D** behaviour parity with pre-wire baseline (live + replay).

---

## Scope notes

- **ℹ️** Extraction-only wiring is **complete** — do not re-move parse logic back to **`ResultParser`** unless fixing a bug.
- **ℹ️** Ephemeral **`new Action.X(task)`** for extract is **intentional**; full **`run_action`** will reuse the same Action types without a separate parse-only layer.
- **🚫** Do not add **`Iterate.vala`** here ( **7.16.3** ).

---

## LLM notes

- **🔷** Next implementation step: **`run_action`** sketch + single-path wire (suggest **C** or **D** first — smallest dispatch table).
- **🚫** Do not add **`Iterate.vala`** here.
