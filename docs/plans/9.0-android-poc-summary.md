# 9.0 Android chat POC — **ACTIVE** summary (start here)

> **Do not update `docs/plans/1.0-summary.md` for this plan.**

> **Status honesty:** **✅** only when the user confirms done. Agent work stays **✔️** or **⏳** until then — see `docs/guide-to-writing-plans.md` (**✅** vs **✔️**). Do not remove backlog items or rewrite **Suggested order** without user sign-off.

**Status:** in progress — Phase 3 device verify + mobile polish

**Pointer:** `docs/guide-to-writing-plans.md` — checklist; Vala follows `docs/coding-standards.md`.

**Package:** `org.roojs.ollmchat.androidpoc`  
**Build:** `scripts/android/build-chat-poc-apk.sh` → `scripts/android/adb-install-chat-poc.sh`  
**Golden rule:** Android-only edits (`ollmapp/android/`, Android meson, `android/icons/`). Shared code needs explicit approval.

**Related:** [`9.3-ACTIVE-android-settings-maindialog.md`](9.3-ACTIVE-android-settings-maindialog.md) · [`done/9.1-DONE-android-chat-shell.md`](done/9.1-DONE-android-chat-shell.md) · [`done/9.2-DONE-android-tls-migration.md`](done/9.2-DONE-android-tls-migration.md) · [`bugs/done/2026-06-17-FIXED-android-chat-poc-device-issues.md`](../bugs/done/2026-06-17-FIXED-android-chat-poc-device-issues.md)

---

## At a glance

- **✅** TLS, config persist, cold boot, chat send/receive (basic)
- **✅** Agent dropdown, input hide while replying, send bar (device, cache-first boot)
- **✅** Model dropdown — icons, button ellipsize, full-width list popover
- **✔️** Tools subset — `web_fetch`, `session_fetch`, `google_search` / `web_search` registered (device verify deferred)

---

## Purpose

- **🔷** Usable chat POC on device — stable boot, send/receive, mobile layout
- **🔷** Settings: Connections + Models only
- **🔷** Models tab reuses `ModelsPage` / `ModelRow` pattern
- **🔷** History: full-screen overlay below header — header stays; history replaces chat body; dismiss via history toggle, New chat, or selecting a session (no dimmed backdrop, no tap-outside)

---

## Done ✅

- **✅** Shell scaffold — [`done/9.1-DONE-android-chat-shell.md`](done/9.1-DONE-android-chat-shell.md)
- **✅** TLS on device — [`docs/android-tls.md`](../android-tls.md)
- **✅** Config at `files/etc/ollmchat/config.2.json`
- **✅** Boot / TLS / startup crash — [`bugs/done/2026-06-17-FIXED-android-chat-poc-device-issues.md`](../bugs/done/2026-06-17-FIXED-android-chat-poc-device-issues.md)
- **✅** About + header icons — [`bugs/done/2026-06-17-FIXED-android-icon-theme-gsettings.md`](../bugs/done/2026-06-17-FIXED-android-icon-theme-gsettings.md)
- **✅** `History.Manager` mkdir crash — shared fix (bug doc §8)
- **✅** Connections settings tab — `ConnectionRow` list
- **✅** Hide `ChatInput` while assistant is running
- **✅** Agent dropdown — matches `Window.vala` (`ListStore` + `SignalListItemFactory`)
- **✅** History full-screen overlay below header (`Gtk.Stack` in `AndroidMainWindow.vala`)
- **✅** Model dropdown icons — `document-properties-symbolic`, `weather-fog-symbolic` in `android/icons/manifest`; capability symbols in `ModelUsageFactory`
- **✅** Model dropdown button ellipsize — `ModelUsageFactory.max_chars` on closed button; full names + wide popover in list (`ChatBar` popup width)

---

## Backlog ⏳

Single list — status + provenance only. Implementation detail lives in **Code map** and per-item proposals when we implement.

### Chat bar (model dropdown)

_(none)_

### Chat rendering

- **🔷** `⏳` GtkSourceView font not monospace on device
  - Code blocks / SourceView text renders in a proportional or wrong font on Android; desktop uses `resources/style.css` `.source-view { font-family: monospace; }` — likely needs Android font fallback or explicit `Pango.FontDescription`

### Tools (Android POC)

Tools button is empty because Android never calls `OLLMtools.Registry.fill_tools()` — not a `ChatBar` bug.

**Could work now (minimal registry):**

| Tool | Verdict | Notes |
|------|---------|-------|
| `web_fetch` | ✅ | HTTP/TLS on device |
| `session_fetch` | ✅ | Current session / history refs |
| `google_search` | ⚠️ | Only if user configures API key (Custom Search; no public free tier) |

**Disable for POC (no project workspace):**

| Tool | Verdict | Notes |
|------|---------|-------|
| `read_file` | 🚫 | Needs `ProjectManager` / workspace paths |
| `write_file` | 🚫 | Same |
| `edit_mode` | 🚫 | Same |

**Not on device:**

| Tool | Verdict | Notes |
|------|---------|-------|
| `run_command` | 🚫 | No useful shell on Android |
| `codebase_search` | 🚫 | Vectors + projects — out of scope |
| Wrapped `.tool` defs | 🚫 | Out of scope |
| MCP (HTTP / stdio) | 🚫 | HTTP could work on device; blocked on **MCP management UI** — desktop still hand-edits `mcp.json` ([`docs/mcp-settings.md`](../mcp-settings.md)); Android waits on that

**Later (not this phase):**

- **🔷** `run_command` (or equivalent) via **RPC** on hosted `ollmfilesd` — needs RPC connected first; remote execution on your machine, not the phone
- **🔷** **MCP** — after desktop gets settings UI for servers (today: edit `mcp.json` only); then HTTP MCP on Android is plausible; stdio MCP less so on device

### Session lifecycle

- **🔷** `⏳` Cold start opens fresh `EmptySession`
  - User expects last chat back — sidebar may list history but boot does not restore
- **🔷** `⏳` Verify saves after `force-stop`
  - Check `files/.../history/` after send + kill (`Session.save_async`)
- **🔷** `⏳` Decide: new chat on restart vs restore last `fid`
- **💩** `⏳` Pause/stop flush + resume restore
  - Android-only — needs approval for shared `Manager` / `ChatWidget`

### Settings

- **🔷** `✔️` Models tab — android `OllmchatWindow` + android `MainDialog` + shared `ModelsPage` (device verify pending)
  - **ℹ️** Plan: [`9.3-ACTIVE-android-settings-maindialog.md`](9.3-ACTIVE-android-settings-maindialog.md)

### Out of scope 🚫

- **🚫** Desktop window / `Initialize` / `MainDialog` refactor
- **🚫** Projects, vectors, full tools registry (POC: small enabled subset only — see **Tools** above)
- **🚫** MCP — needs desktop MCP settings UI first (see **Tools** / **Later**)
- **🚫** RPC-backed remote command execution

---

## Suggested order

1. Tools — register minimal subset (`web_fetch`, `session_fetch`, `google_search` / `web_search`; disable file/command tools) **✔️** (device verify deferred)
2. Models settings tab — [`9.3`](9.3-ACTIVE-android-settings-maindialog.md) **✔️** (device verify pending)
3. Session lifecycle (verify saves → product decision → optional restore)
4. Tool settings config (API key / engine ID for `google_search`)

---

## Quick commands

```bash
rm -rf .pixiewood/android/app/src/main/jniLibs   # if symlink step fails
scripts/android/build-chat-poc-apk.sh
scripts/android/adb-install-chat-poc.sh
adb shell cat /storage/emulated/0/Android/data/org.roojs.ollmchat.androidpoc/files/etc/ollmchat/config.2.json
adb shell am force-stop org.roojs.ollmchat.androidpoc && adb logcat -c
adb shell am start -n org.roojs.ollmchat.androidpoc/org.gtk.android.ToplevelActivity
```

---

## Code map

- **Shell:** `ollmapp/android/OllmchatWindow.vala`, `android/MainDialog.vala`, `AndroidApplication.vala`, `AndroidStartup.vala` (see plan 9.3)
- **Agent dropdown (ref):** `ollmapp/Window.vala` `setup_agent_dropdown()`
- **Tools (ref):** `liboctools/Registry.vala`, desktop `Window.vala` / `Application.vala` registration path
- **Chat (shared):** `libollmchatgtk/ChatWidget.vala`, `ChatBar.vala`, `List/ModelUsageFactory.vala`
- **Models settings (ref):** `ollmapp/SettingsDialog/ModelsPage.vala`, `ModelRow.vala`
- **Icons:** `android/icons/manifest`

---

## Concrete code proposals

**⏳ Deferred** — add hunks when implementing each backlog group; golden rule for shared vs Android-only.
