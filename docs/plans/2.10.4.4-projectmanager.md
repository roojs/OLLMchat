# 2.10.4.4 — `ProjectManager` (client and server)

**Status:** PROPOSAL — implementation plan (no coding in this doc).

**Depends on:** [`2.10.4.3`](done/2.10.4.3-DONE-daemon-and-rpcclient.md) ✅ (`RpcClient`, `Daemon.*`)

**Files in play:**

| File | Role |
| ---- | ---- |
| `libocfiles/RpcClient.vala` | Connect + hello only (**not in Meson** — stays unwired) |
| `libocfiles/RpcClientBoot.vala` | Ensure daemon running (**not in Meson** — stays unwired) |
| `libocfiles/V2/ProjectManager.vala` | **Client** — copy of PM, stripped + RPC relay |
| `ollmfilesd/ProjectManager.vala` | **Server** — full PM (DB, scan, disk) |
| `ollmfilesd/Listen.vala` | Holds server `ProjectManager`; RPC dispatch |

Do **not** edit shipping `libocfiles/ProjectManager.vala`.

---

## Premise: one daemon, many lightweight clients

- **Daemon:** one `OLLMfiles.ProjectManager` — SQLite, FAISS queues, filesystem scan, canonical `projects` / caches.
- **Each UI window:** its own **`V2/ProjectManager`** + its own **`RpcClient`** (separate socket sessions are fine).
- Old design shared one fat in-process PM; V2 PM is thin — holds `active_project` / `active_file`, Gtk signals, and **`RpcClient`** only.
- App constructs **`new ProjectManager()`** per window (when cutover); each instance bootstraps RPC on construct.

---

## Phase 1 — Bootstrapping (`RpcClientBoot`, not `RpcClient`)

**Goal:** Client can always reach a running `ollmfilesd` before any `ProjectManager.*` call.

Today `RpcClient.connect()` only connects to the socket; it does **not** start the daemon. Spawn/pid/socket policy lives in **`RpcClientBoot`** so it does not overlap `RpcClient` properties (`protocol`, `client_name`, pending calls, streams).

**Note:** **`RpcClient.vala`** and **`RpcClientBoot.vala`** are delivered as source but **not wired into Meson** until `ollmfilesd` has a build target and cutover tests exist (same as 2.10.4.3 mockup).

**`RpcClientBoot` (implemented in tree, unwired):**

1. **`ensure_daemon()`** loop (pid file + socket under `~/.local/share/ollmchat/`):
   - Socket accepts connection → done.
   - Else pid alive but socket dead → SIGTERM, short grace, remove stale socket/pid, respawn.
   - Else → remove stale socket/pid, spawn `ollmfilesd` (detached, or `--foreground` + held `Subprocess` for tests).
   - After each spawn, poll socket for **`startup_wait`** (seconds; default 5).
   - Repeat up to **`max_attempts`** (default 3); then fail with an error the UI can show (*could not start or reach the filesystem daemon*).
2. **`RpcClient.connect()`:** `RpcClientBoot.ensure_daemon` → Unix connect + **`Daemon.hello`** (wired in `RpcClient.vala`).
3. Optional: `client_name` includes window id later; v1 `"ollmchat"` OK.

**Out of scope for phase 1:** `ProjectManager` RPC methods; V2 file edits; Meson wiring; **systemd** (no `prefer_systemd` on boot).

**Systemd (later, not RpcClientBoot):** optional global setting in the app to install/enable a **user** unit and start it — separate from package install and from boot logic (external daemon may already own the socket; boot still probes socket + pid only).

**Test (when wired):** no `ollmfilesd` running → boot + `RpcClient.connect()` leaves daemon up and hello succeeds.

---

## Phase 2 — Client `libocfiles/V2/ProjectManager.vala`

**Goal:** Same public API surface as today where callers care, but **no direct filesystem / DB / scan** — only RPC + local UI state.

### Constructor (client bootstrap)

```text
V2/ProjectManager()
  → rpc = new RpcClient ()
  → yield rpc.connect()     // ensure_daemon + hello inside
  → (no SQ.Database — db stays null on client)
```

- Remove or no-op server-only construction: `DeleteManager` wired to local DB, `db` usage in ctor.
- **`delete_manager`**, **`file_cache`**, **`tree_cache`**, **`scanning`:** drop or keep empty stubs until `File` / `Folder` plans; do not mirror full daemon graphs.

### What stays on the client (no RPC)

| Piece | Why |
| ----- | --- |
| `active_project`, `active_file` | Per-window UI state |
| Gtk signals | Re-emit after RPC success |
| `buffer_provider`, `git_provider` | Local / no-op bases for now |
| `disable_initial_scan` | Passed as `params.skip_scan` on activate |

### What becomes `rpc.call("ProjectManager.{method}", …)`

Replace body of any method that touches **disk, SQLite, `saveToDB`, `read_dir`, `DeleteManager` on server data**, or builds canonical project tree:

| V2 method | RPC | Strip from V2 body |
| --------- | --- | ------------------ |
| `load_projects_from_db` | `ProjectManager.load_projects_from_db` *(wire name = Vala name)* | DB query, `projects.append` from SQL — **result type `Rpc.ProjectList`** *(not added yet; V2 mockup parses `{ "projects": [ … ] }`)* |
| `create_project` | `ProjectManager.create_project` | local `Folder` create + `saveToDB` |
| `remove_project` | `ProjectManager.remove_project` | local `saveToDB` |
| `activate_project` | `ProjectManager.activate_project` | `read_dir`, `saveToDB` on activate |
| `get_folder_at_path` | `ProjectManager.get_folder_at_path` or defer | DB / cache walk |
| `restore_active_state` | orchestrate several RPCs | direct DB / activate |
| `activate_file` | → `File.activate` in [2.10.4.6](2.10.4.6-file.md) | `saveToDB` |
| `on_file_metadata_change` | → `File.metadata.set` | `saveToDB` |
| `on_file_contents_change` | → `File.write` | disk write |
| `convert_fake_file_to_real` | → `File.register` | Gio file create + DB |
| `check_active_file_changed` | → `File.changed.check` | disk stat |
| `write_buffer_to_disk` | → `File.write` | disk |
| `reload_file_from_disk` | → `File.read` | disk |

**Phase 2 tranche (PM-only):** implement RPC relay for **`load_projects_from_db`**, **`create_project`**, **`remove_project`**, **`activate_project`** first — enough for settings + window open.

**Client `projects` list:** after `load_projects_from_db` RPC, rebuild **`OLLMfiles.ProjectList`** from the wire payload. **Later:** add **`OLLMfiles.Rpc.ProjectList`** (+ thin `ProjectEntry` rows) as the declared `result` type; daemon serializes it, client deserializes and appends `Folder`s into `this.projects`. **V2 mockup (now):** no new Rpc classes — inline JSON stand-in only.

### Phase 2 does not edit

- `ollmfilesd/*` (except any import fix if build requires — prefer phase 3)
- Shipping `libocfiles/ProjectManager.vala`

---

## Phase 3 — Server `ollmfilesd/ProjectManager.vala` + dispatch

**Goal:** Incoming `ProjectManager.{method}` on the wire runs the **real** method on the daemon’s `ProjectManager` instance.

### Daemon process layout

```text
main
  → open DB, ProjectMigrate.migrate_all
  → project_manager = new OLLMfiles.ProjectManager(db)
  → yield project_manager.load_projects_from_db()
  → listen = new Listen(project_manager: ...)
  → listen.start()
```

One **`ProjectManager`** for all connected clients (shared DB). Per-window state (`active_project` on server) may need **`client_id` later** — v1 can use last-writer or single-client assumption for testing.

### `Listen` wiring

- Property **`ProjectManager`** on `Listen` (name matches wire object — dispatch uses `registry.ProjectManager`).
- Value: the server `OLLMfiles.ProjectManager` instance (relay target).

**Relay pattern** (same idea as `Rpc.Daemon` today):

- For each supported wire method, handler receives **`Rpc.Request`**:
  - Read **`request.param`**
  - Call **`this.project_manager.{method}(...)`** (real Vala)
  - **`request.session.reply(request, result)`** or **`reply_error`**
- Handlers can live:
  - in **`construct`** of a small `Rpc.ProjectManagerRelay` wrapper, **or**
  - as **`rpc_{method}`** signals on a GObject that holds `unowned ProjectManager manager`

**Lightweight vs heavy:**

| Style | Example |
| ----- | ------- |
| Trivial in relay ctor | parse params, one line call, serialize result |
| Heavy | `activate_project` — already async on server; relay `yield`s and returns revision / flags |

Do **not** duplicate business logic in relay — only param mapping + result JSON.

### Phase 3 tranche (match phase 2 client)

Wire handlers for:

- `ProjectManager.load_projects_from_db`
- `ProjectManager.create_project`
- `ProjectManager.remove_project`
- `ProjectManager.activate_project`

### Phase 3 does not edit (yet)

- `libocfiles/V2/ProjectManager.vala` beyond what phase 2 needs
- Vector, `Folder`, `File` server relays ([2.10.4.5](2.10.4.5-folder.md), [2.10.4.6](2.10.4.6-file.md))

---

## Phase 4 — End-to-end test (both files + RpcClient)

1. Start nothing → app/test constructs V2 `ProjectManager` → RpcClient spawns daemon → hello.
2. `load_projects_from_db` / list projects — round trip.
3. `create_project` / `remove_project` / `activate_project` — round trip.
4. Two V2 `ProjectManager` instances (two RpcClients) — both talk to one daemon; document behaviour of shared `active_project` on server until `client_id` exists.

---

## Methods map (design reference)

| `ProjectManager.{method}` | Server today | Client V2 | Phase |
| ------------------------- | ------------ | --------- | ----- |
| `load_projects_from_db` | DB → `projects` | RPC + hydrate list | 2–3 |
| `create_project` | disk + DB | RPC | 2–3 |
| `remove_project` | DB | RPC | 2–3 |
| `activate_project` | scan + DB | RPC | 2–3 |
| `get_folder_at_path` | cache + DB | RPC | later |
| `restore_active_state` | composite | RPC orchestration | later |
| `snapshot`, `index`, … | daemon | RPC | later |

---

## Events (later)

Daemon emits `event.project.*`; each window’s `RpcClient.notification` → V2 PM updates UI. Not required for phase 2–3 tranche.

---

## Open questions (decide before code)

1. **Client/server type sharing** — shared wire package vs duplicate types TBD; first PM tranche may decide. **Alternative (in tree):** `libocfiles/JsonOverlay.vala` — types add `JsonOverlay` and call `overlay_jobject()` / `overlay_jtext()` to merge RPC JSON into an existing instance instead of a shared domain interface.
2. **`ensure_daemon` spawn command** — path to `ollmfilesd` binary, args (`--foreground` for tests), pid/socket recovery limits (done in boot; tune defaults if needed).
3. **Server `active_project`** with multiple windows — last activate wins vs per-session id.
4. **Exact wire method names** — use full Vala names (`load_projects_from_db` not `list`) — **recommended** per 2.10.4.1.
5. **`Listen.ProjectManager` property** vs lowercase `project_manager` — wire requires **`ProjectManager`** prefix; property name on `Listen` must match dispatch (`ProjectManager`).

---

## Meson (when implementing)

- `libocfiles`: `RpcClient.vala` + optionally `V2/ProjectManager.vala` behind flag.
- `ollmfilesd`: `ProjectManager.vala` + relay; link `libocfiles` server-side deps as today.

---

## Summary

| Phase | Where | What |
| ----- | ----- | ---- |
| **1** | `RpcClientBoot` (+ later `RpcClient`) | Ensure daemon running, then connect + hello |
| **2** | `V2/ProjectManager` | Remove FS/DB/scan; ctor starts RPC; PM methods → `call` |
| **3** | `ollmfilesd/ProjectManager` + `Listen` | Relay `Request` → real methods; shared server PM |
| **4** | All | Tranche test: load / create / remove / activate |

**Only two domain files** for PM logic: **`libocfiles/V2/ProjectManager.vala`** and **`ollmfilesd/ProjectManager.vala`**, plus **`RpcClientBoot`** / **`RpcClient`** for bootstrap (phase 1; both unwired in Meson until cutover).

---

## Follow-up (after 2.10.4.6 cutover)

**[`2.10.4.7-active-state-client-only.md`](2.10.4.7-active-state-client-only.md)** — move active project/file out of daemon + SQLite `is_active`; per-window UI ownership; config-driven restore ([`1.3.9`](1.3.9-agent-configuration-and-window-state.md)). Wire `activate_*` in this tranche is **temporary** for cutover tests.
