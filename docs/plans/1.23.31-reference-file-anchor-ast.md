# Plan 1.23.31: File references — anchor and AST checking

**Source:** [1.23.20 DONE](done/1.23.20-DONE-post-testing-changes.md) §2 (deferred from 1.23.18).

**Coding standards:** `.cursor/rules/CODING_STANDARDS.md`.

## Status

TODO — not started.

## Goal

References of the form **`file_path#anchor`** (absolute or project-relative file + hash):

- **Today:** `Details.validate_references()` checks the **file exists** (and directory rules). It does **not** verify that **`#anchor`** resolves in that file.
- **Here:** After file exists, validate that the anchor identifies a real **markdown section** (GFM heading slug) and/or **AST symbol** (method, class, etc.) in that document, consistent with how `link_content` / injection resolves `path#anchor`.

## Scope

- **`liboccoder/Task/Details.vala` — `validate_references()`** — for `scheme == file` and non-empty `link.hash`, load or consult project index / AST / markdown headings for `resolved_path` and reject if anchor missing (with helpful message, e.g. available headings/symbols).
- Align with existing resolution paths used when building reference blocks for refinement/exec.

## Related

- **1.23.20** (done): Bare `#anchor` (user_request) and bare file path validation; path#anchor was explicitly deferred here.
- **1.23.30:** Reference content length (separate).
