# 1.1. Restore Message Duplication Bug

## Overview

Fix bug in session restore that is duplicating messages and displaying them in not quite the right order.

## Status

✅ **DONE** - Bug fix completed. Loading works fine now.

## Implementation Details

### Problem
- Messages are being duplicated when restoring sessions
- Messages are not in the correct order when displayed

### Investigation Needed
- Check `load_messages()` in ChatWidget
- Check message filtering logic in Session loading
- Verify message ordering in session.messages
- Check for duplicate message creation during restore

### Files to Review
- `libollmchatgtk/ChatWidget.vala` - `load_messages()` method
- `libollmchat/History/Session.vala` - Message loading logic
- `libollmchat/History/SessionPlaceholder.vala` - Message filtering
