# 2.10.4.6 — `File` (client and server)

**Status:** **IN PROGRESS** — client strip-down done; server `File.*` relays **phased** in [`2.10.4.9-DONE-v2-caller-cutover.md`](../2.10.4.9-DONE-v2-caller-cutover.md) §1b (reverted from tree until approved); tests pending.

**Depends on:** [`2.10.4.5`](2.10.4.5-DONE-folder.md) ✅

**Parent:** [`2.10.4.1`](2.10.4.1-ollmfilesd-rpc-api.md)

---

## Progress

| Step | Status |
| ---- | ------ |
| Client RPC stubs on `V2/File.vala` | ✅ |
| Strip daemon-only methods from client `File` | ✅ §1 |
| Strip daemon-only from client `FileBase` | ✅ §2 |
| Server `ollmfilesd` `File.*` relays | ⏳ **T12** — not implemented; client stubs call wire methods that have no daemon handler yet |
| PM delegates to `File.{method}` (optional tidy) | ⏳ with **T11–T12** |
| Test read/write/register/delete | ⏳ |

---

## Client — `libocfiles/V2/File.vala`

RPC via `this.manager.rpc`. Namespace **`OLLMfiles`**.

**Client RPC stubs (done):**

| Method | Wire | Notes |
| ------ | ---- | ----- |
| `read()` | `File.read` | ✅ {@link Copyable} + buffer {@code read_async} |
| `write()` | `File.write` | fire-and-forget |
| `check_changed()` | `File.changed.check` | |
| `register()` | `File.register` | fake `id == -1`; partial field overlay |
| `delete()` | `File.delete` | |

**Not on `File` (client-only, no RPC):**

- `File.activate` — per-window tab: {@link ProjectManager.activate_file}; drop daemon wire in [`2.10.4.7`](2.10.4.7-FUTURE-active-project-file-outside-db.md).
- `cursor_line`, `cursor_offset`, `scroll_position`, `last_viewed` — per-window / agent config ([`2.10.4.7`](2.10.4.7-FUTURE-active-project-file-outside-db.md), [`1.3.9`](1.3.9-agent-configuration-and-window-state.md)); **no** `File.metadata.set` on daemon.

**Client-only (no RPC):**

- `File.buffer`, `buffer_provider`, `create_buffer` — after `File.read` returns content
- `File.new_fake` (id &lt; 0) until **`File.register`** succeeds
- `cursor_line`, `cursor_offset`, `scroll_position`, `last_viewed` — per-window; agent config at restore ([`1.3.9`](1.3.9-agent-configuration-and-window-state.md))

**Not on V2 client:** `ProjectManager.tree_factory` / `tree_cache` — tree-sitter AST lives on the daemon; see **AST on wire** below.

---

## 1. Daemon only — delete from client `File` ✅

Removed from `libocfiles/V2/File.vala`. Full bodies remain in `ollmfilesd/File.vala`.

| Method / constructor | Removed | Notes |
| -------------------- | ------- | ----- |
| `File.new_from_info` | ✅ | Daemon scan only |
| `check_updated` | ✅ | Use {@link check_changed} RPC; PM {@code check_active_file_changed} |
| `approve` | ✅ removed client | `FileHistory.approve` RPC — relay TBD |
| `revert` | ✅ removed client | `FileHistory.revert` RPC — relay TBD |
| `is_documentation` | ✅ | `Indexer` on daemon only |
| `saveToDB` in `get_selected_code` / `get_cursor_position` | ✅ | Cursor client-only ([`2.10.4.7`](2.10.4.7-FUTURE-active-project-file-outside-db.md)) |
| `icon_name` disk `query_info` | ✅ | `ContentType.guess` from path only |

**Kept on client `File`:** constructor, `new_fake`, buffer helpers (`contents`, `line_count`, …), display props, `to_summary` stub, RPC stubs (§ above).

**Borderline:** `new_fake` still stat's path locally for `is_text` / `last_modified` until `register` / `read` RPC — OK for tool paths outside project.

---

## 2. Daemon only — delete from client `FileBase` ✅

Removed from `libocfiles/V2/FileBase.vala`. Full bodies on `ollmfilesd/FileBase.vala`.

| Method | Removed | Notes |
| ------ | ------- | ----- |
| `mtime_on_disk` | ✅ | Daemon scan |
| `init_db` | ✅ | Daemon startup |
| `query` | ✅ | Daemon SQL |
| `compare` | ✅ | Daemon `read_dir` |
| `copy_db_fields_to` | ✅ | Client uses {@link Copyable.copy_from} |
| `saveToDB` | ✅ | Daemon / RPC |
| `removeFromDB` | ✅ | {@link File.delete} RPC |
| `get_target_info` | ✅ | Daemon alias scan |

**Kept on client `FileBase`:** wire properties, display helpers, `to_summary` abstract.

---

## Server — `ollmfilesd`

`OLLMfiles.File` + disk I/O; **`DeleteManager.remove`** for delete path.

**Daemon-internal:** `FileHistory.cleanup_old_backups`; `VectorMetadata.cleanup_all_deleted` after delete.

---

## Methods — today’s code → wire

| `File.{method}` | Today | Notes |
| --------------- | ----- | ----- |
| `get` | `ProjectManager.get_file_from_active_project` | Resolve path → `filebase` — object TBD (`File` vs `ProjectManager`) |
| `read` | buffer load / `reload_file_from_disk` | `params.path`; returns `filebase` + `content` |
| `write` | `write_buffer_to_disk`, `on_file_contents_change` | `params.content` — queues scan/index in daemon |
| `activate` | `ProjectManager.activate_file` only | **Not** on client `File` — per-window state; interim `File.activate` RPC from PM; remove in [`2.10.4.7`](2.10.4.7-FUTURE-active-project-file-outside-db.md) |
| `metadata.set` | `on_file_metadata_change` (shipping) | **Dropped** — cursor/scroll client-only ([`2.10.4.7`](2.10.4.7-FUTURE-active-project-file-outside-db.md)) |
| `changed.check` | `check_active_file_changed` | external change vs buffer |
| `register` | `convert_fake_file_to_real` | fake → real row |
| `delete` | `DeleteManager.remove` | `params.path` |
| `approve` | `File.approve` (in-process) | ⏳ `FileHistory.approve` RPC — relay TBD |
| `revert` | `File.revert` (in-process) | ⏳ `FileHistory.revert` RPC — relay TBD |
| `index` | per-file index queue | with `BackgroundScan` |
| `ast.lookup` | `ProjectManager.tree_factory` + `Tree.parse` + `lookup_path` | **daemon only** — returns line range, not a `Tree` object |

```json
→ {"method":"File.read","params":{"path":"/home/alan/proj/foo/src/Ap.php"}}
→ {"method":"File.write","params":{"path":"...","content":"..."}}
→ {"method":"File.ast.lookup","params":{"path":"...","ast_path":"..."}}
```

**Open:** top-level `params.path` only vs nested `params.file`; whether `get` stays on `ProjectManager.get_folder_at_path`.

---

## Events

| Event | Was |
| ----- | --- |
| `event.file.added` | `new_file_added` |
| `event.file.changed` | external / peer-window change — route by per-client project watch; see [`2.10.4.8`](2.10.4.8-per-client-project-notifications.md) |

**Client:** restore `V2/ProjectManager.file_contents_changed` when 2.10.4.8 is implemented (not on local `File.write` success).

---

## AST on wire (daemon — not V2 client)

**Decision:** `OLLMfiles.Tree` (tree-sitter path → line range for **one file**) is **backend-only**. V2 `ProjectManager` has **no** `tree_factory` / `tree_cache`.

Today (shipping, in-process) callers use `project_manager.tree_factory(file)` then `tree.parse()` / `lookup_path()`:

| Caller | Use |
| ------ | --- |
| `liboctools/ReadFile/Request.vala` | `read_file` tool + `ast_path` → line range |
| `libocfiles/FileChange.vala` | Agent edits anchored to AST path |
| `liboccoder/Task/ResolveLink.vala` | Task refs `[[file#ast]]` |

**Not used by editor UI** (`SourceView`, `Window`) — no frontend direct access.

**Target after cutover:**

- Daemon keeps `tree_factory` + cache on server `ProjectManager` (or per-file cache on `File`).
- Wire: e.g. **`File.ast.lookup`** — `params.path`, `params.ast_path` → result `{ start_line, end_line, comment_start }` (exact shape TBD).
- **`File.read`** with `ast_path` may fold into lookup + read slice on server, or client calls lookup then read by lines.
- Tools / task code call **RPC** and consume **results** — they do not hold or parse `Tree` locally.
- Vector `Indexer` already uses `new Tree(file)` on daemon side — same process.

**Shipping `libocfiles/ProjectManager.tree_factory`:** keep until tools migrate to RPC; remove from client when cutover complete.

---

## Deferred

- `File.read.chunk` — large files
- `File.git.blame` — git UI
- `File.ast.lookup` — wire name TBD; replaces client `tree_factory` for ReadFile, FileChange, ResolveLink

---

## Test

See **[`2.10.4.10`](2.10.4.10-rpc-tests.md)** — RPC round-trip harness (read/write/register/delete, PM tranche, isolated `data_dir` under build). UI keeps old `File` until cutover.
