# 2.10.4.3 — `RpcClient` + `Daemon` (client and server)

**Status:** **DONE** — mockup delivered (Meson / foreground daemon test not wired).

**Delivered:** `libocfiles/RpcClient.vala`; `ollmfilesd/Rpc/Daemon.vala` (`Daemon.hello`, `Daemon.shutdown`); `ollmfilesd/Listen.vala`, `ollmfilesd/Session.vala`; RPC wire types (`Request`, `Response`, `CallParam`, `RpcErrorCode`, `Error`, `Notification`).

**Parent:** [`2.10.4.0`](../2.10.4.0-libocfiles-v2-and-rpc-layout.md) · [`2.10.4.1`](2.10.4.1-DONE-ollmfilesd-rpc-api.md)

**Next:** [`2.10.4.4-projectmanager.md`](../2.10.4.4-projectmanager.md)

---

## Client — `libocfiles/RpcClient.vala`

Top-level (not `V2/`). All V2 objects use this.

- Unix socket `~/.local/share/ollmchat/ollmfilesd.sock`, NDJSON
- `call("{Object}.{method}", params)` → `result` or error
- On connect: **`Daemon.hello`**
- Notification lines *(hook stub OK)*

---

## Server — `ollmfilesd` / `Daemon`

`Listen`, `Session`, `Rpc.Daemon`.

| `method` | Params | Result |
| -------- | ------ | ------ |
| `Daemon.hello` | `protocol`, `client` | `protocol`, `server`, `ready` |
| `Daemon.shutdown` | `force` | `ok` |

```json
→ {"method":"Daemon.hello","params":{"protocol":1,"client":"ollmchat"}}
→ {"method":"Daemon.shutdown","params":{"force":false}}
```

**Notification:** `event.Daemon.ready` after listen *(naming TBD in 2.10.4.1)*.

---

## Daemon startup (not RPC — lives here, not in `ProjectManager` plan)

Runs before / outside client calls:

| Step | Today |
| ---- | ----- |
| Open DB | `SQT/Database` |
| Migrate | `ProjectMigrate.migrate_all` — **never** `Daemon.migrate` on wire |
| Load projects | `ProjectManager.load_projects_from_db` — then clients call `ProjectManager.*` |

---

## Test / Meson

Foreground daemon; hello → shutdown. Add `RpcClient.vala` to `libocfiles`; `V2/` still out of build.
