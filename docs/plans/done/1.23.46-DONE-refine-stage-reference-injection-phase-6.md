# Plan 1.23.46: Refine / execute templates + skill renames — Phase 6 (`task_execution.md`, standard executor)

**Status:** **Complete** — [`resources/skill-prompts/task_execution.md`](../../../resources/skill-prompts/task_execution.md) updated in the repo per the checklist and notes below. Further work belongs in **[Plan 1.23.40](1.23.40-DONE-refine-stage-reference-injection.md)** or a new plan.

**Parent:** [Plan 1.23.40](1.23.40-DONE-refine-stage-reference-injection.md) — overview, four-template matrix, **§ Conventions**, terminology, skill catalog, file index.

**Coding standards (full):** [`.cursor/rules/CODING_STANDARDS.md`](../../../.cursor/rules/CODING_STANDARDS.md) and [`docs/code-documentation.md`](../../../docs/code-documentation.md) (Valadoc).

**Phase:** 6 of 8 — **standard** execution prompt (**`task_execution.md`**). The **write** executor template (**`task_execution_write.md`**) + **`gresources.xml`** `<file>` + full body are **[Phase 7](1.23.47-DONE-refine-stage-reference-injection-phase-7.md)** (complete). Parser / **`WriteChange`** / **`exec_extract`** are **[Phase 8](1.23.48-DONE-refine-stage-reference-injection-phase-8.md)** (complete).

**Cross-refs (review):** [Phase 4](1.23.44-DONE-refine-stage-reference-injection-phase-4.md) — pre-exec references, **`build_exec_runs`**, per-run **`Tool.references`** / **`exam_reference`**, **`link_content`** at **`EXECUTION`**. [Phase 5](1.23.45-DONE-refine-stage-reference-injection-phase-5.md) — **`executor_prompt`** chooses **`task_execution_write.md`** vs **`task_execution.md`**; inline **`## Reference Contents`** in **`Tool.run()`** (no **`reference_contents()`**). **This phase** updated only the **standard** wrapper **`task_execution.md`** so it matches that behavior and the parent **§ Templates** row 3.

**Plan rule:** If it is not in a code fence, it is not an approved change.

---

## Coding standards checklist

Copied from **`.cursor/rules/CODING_STANDARDS.md`** — “Checklist for all plans”. If a bullet conflicts with **§ Conventions** in the [parent plan](1.23.40-DONE-refine-stage-reference-injection.md), **the standards file wins**.

- **Nullable types**: Design new APIs/properties to avoid nullable types where possible (default objects / flags).
- **Null checks**: Avoid generic null checks; only where the design explicitly requires null (see standards for the narrow exceptions).
- **String interpolation**: No `@"..."` except multi-line usage/help/documentation; use `+` concatenation otherwise.
- **Temporary variables**: No one-use temporaries; no trivial aliases (allowed exception: long property chains **4+** steps).
- **Brace placement**: Line breaks before `{` for namespace/class/method; **inline** `{` for `if`/`for`/`while`/`switch`; never a one-line `if` with body on the same line.
- **`this.` prefix**: Use for instance member access in new/modified Vala.
- **GLib prefix & `using`**: Fully qualify `GLib.*`; do not add `using` for new code.
- **Property initialization**: Prefer `get; set; default = …` (or field defaults), not constructor assignment for those defaults.
- **Line length & breaking**: Break long method calls, concatenations, docblocks, and comments (docblock/comment line length per full standards).
- **StringBuilder**: Avoid `GLib.StringBuilder` unless building strings in loops with **hundreds** of iterations; use `+` and `string.joinv()`.
- **String building in loops**: Never accumulate with `+=` inside a loop; use `string.nfill()` / `replace()`, `string.joinv()`, etc.
- **ArrayList for strings**: Do not use `Gee.ArrayList<string>` only to join; use `string[]` with **`string[] name = {}`** only for empty init (not `new string[0]`).
- **Loops and nesting**: In loops, use **`continue`** to skip cases and keep the body flat; avoid **`else`** and nested **`if`** inside loops.
- **Character looping**: Do not iterate characters unless there is truly no alternative; prefer string methods and `GLib.Regex`.
- **File info try/catch**: Wrap `query_info()`-style calls only when file existence is unknown — not right after a successful read of the same path.
- **Try/catch scope**: Wrap only the minimal expression(s) that throw; keep setup outside `try`.
- **Underscore prefix**: No leading `_` on variables, fields, or properties.
- **`get_*` methods**: Prefer properties or verb-less/action names over `get_foo()` (see standards for exceptions).
- **Docblocks**: Multiline `/** … */` with `@param` / `@return` where relevant; not one-liners for real behavior.
- **Debug / warning messages**: No timestamps or monotonic time in `GLib.debug()` / `GLib.warning()`; no class/method prefixes in the message (file/line come from the log).
- **Gee.HashMap**: Use `.set()` / `.get()` / `.has_key()` / `.unset()` — not `[]`.
- **Gee.ArrayList**: Use `.get()` / `.set()` / `.add()` / `.remove_at()` — not `[]`.
- **Reducing nesting**: Prefer early `return` / `continue`; avoid `else` where flow stays clear; prefer `switch` over long `if` / `else if` chains when editing.
- **SQL table aliases**: Not applicable to this plan’s fences; if you add SQL elsewhere, no table aliases (full names only).
- **Plan Vala fences**: Do **not** use comment-only placeholders (`/* … */` or empty `return "";`) where real logic belongs — either **complete, compilable Vala** in the fence or **prose-only** steps (no fake stub block).

---

## Implementation

### Phase 6

**Scope (markdown — standard executor only):** Align **`task_execution.md`** with the **standard** execution path (parent **§ Templates** row 3): tool runs, **Result summary** + optional body/skill output per **Skill definition**, reference vs tool-output injection described in [Phase 5](1.23.45-DONE-refine-stage-reference-injection-phase-5.md) / [Phase 4](1.23.44-DONE-refine-stage-reference-injection-phase-4.md). **Delivered** prose for **per-run** scope (examination split, shared references), **`## Specific Document or Code to consider for this task`** when present, and **Skill definition** as the only authority for extra sections or fenced output — without duplicating the **write** / **Change details** contract (**`task_execution_write.md`** is **[Phase 7](1.23.47-DONE-refine-stage-reference-injection-phase-7.md)** only).

**Bundling:** **`task_execution.md`** is listed under **`<gresource prefix="/skill-prompts">`** in **`resources/gresources.xml`**. This phase did **not** add a new resource filename; edits were **content-only** in **`task_execution.md`** (same bundling rules as [Phase 3](1.23.43-DONE-refine-stage-reference-injection-phase-3.md) § Bundling).

**Not in Phase 6:** **`task_execution_write.md`**, **`gresources.xml`** rows for the write template, or Vala — those belong to **[Phase 7](1.23.47-DONE-refine-stage-reference-injection-phase-7.md)** (write prompt + bundle) and **[Phase 5](1.23.45-DONE-refine-stage-reference-injection-phase-5.md)** / **[Phase 8](1.23.48-DONE-refine-stage-reference-injection-phase-8.md)** (code).

### Tree check (historical — addressed in shipped prompt)

Review target files: **`resources/skill-prompts/task_execution.md`**, **`liboccoder/Task/Tool.vala`**, **`liboccoder/Task/ResultParser.vala`**.

- **Run shape:** Replaced “one tool call” wording with **one execution slice** (tool optional, references optional).
- **Input composition:** Documented assembly as tool output + shared **## Reference Contents** + optional **`## Specific Document or Code to consider for this task`**.
- **Result summary:** **`## Result summary`** remains aligned with `ResultParser.exec_extract()`.

### Delivered in `task_execution.md` (summary)

- **`## This execution run`** and per-slice input wording; **Skill definition** as sole authority for body sections and fenced skill output (no invented “findings” sections or placeholder fences).
- **Do / Don’t:** follow-up and gaps **only** in **## Result summary**; no separate follow-up sections; links in examples (including AST `#namespace-…` and line-range `#L23-55` style anchors).
- **Example output:** two shapes (completion signal vs partial); examples assume **## Result summary** only unless a skill explicitly requires more.

### Phase 6 implementation checklist (content-only)

| Deliverable | Done |
|-------------|:----:|
| `task_execution.md`: replace strict single-tool-run wording with per-run wording (tool or references, or both) | ✅ |
| `task_execution.md`: align "What you receive" and "Tool Output and/or Reference information" with `Tool.run()` assembled input | ✅ |
| `task_execution.md`: document optional examination section title without introducing write-path rules | ✅ |
| Keep completion signal and output format contract aligned with parser / Runner for standard executor | ✅ |
| Skill-definition-only body / fenced output; examples and follow-up-in-summary rules | ✅ |

### Approved prompt deltas (applied — archive)

**Replace** (section heading + body)

```md
## Tool calls

This run is for **one** tool call. The Runner executed a single tool; the input below includes that tool's output and any reference content for this run. (The task may have further tool calls in later runs.) Use the content provided to produce your result summary and any body sections the skill requires.
```

```md
## This execution run

This run is for **one execution slice** of the task. Depending on the task shape, it may include:

- one tool call output,
- reference content only (no tool call), or
- both tool output and reference content.

The task may have additional runs after this one (for example, one run per examination reference, or multiple tool calls). Use only the content provided for this run when producing the result summary and required body sections.
```

**Replace** (bullet under `## What you receive`)

```md
- **Tool Output and/or Reference information** — Reference content (resolved References for this run) and/or tool output (this run's or the task's tool runs). When the task had tool calls: tool output plus any reference content. When the task had **no** tool calls: reference content only (one run per reference or one combined run if the skill sets `execute-combined`). You interpret this content and produce Result summary + body sections.
```

```md
- **Tool Output and/or Reference information** — Input assembled for this execution run: tool output (if a tool ran), shared reference contents (if provided), and optionally a focused examination section titled **`## Specific Document or Code to consider for this task`**. Any component may be empty except the overall section. Interpret only this run's provided input and produce Result summary + required body sections.
```

Subsequent edits (skill output rules, examples, follow-up-in-summary, link styles) landed in the same file after this fence set; **see current** [`task_execution.md`](../../../resources/skill-prompts/task_execution.md).

### Notes for reviewer

- This phase did **not** change parser/runtime Vala; it updated prompt copy so the model’s instructions match execution behavior in Phases 4, 5, and 8.
- Placeholders and trailing layout unchanged: **`{what_is_needed}`**, **`{skill_definition}`**, **`{project_description}`**, **`{executor_input}`**, **`{executor_previous_analysis}`**, **`{executor_retry_issues}`**.
