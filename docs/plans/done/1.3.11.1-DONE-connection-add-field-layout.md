# 1.3.11.1 — ConnectionAdd field layout

> **Do not update `docs/plans/1.0-summary.md` for this plan.**

**Status:** DONE

**Parent:** `docs/plans/1.3.11-config-pane-field-layout.md`

**Pointer:** `docs/guide-to-writing-plans.md` — **Checklist for plans**

**Files changed:** `ollmapp/SettingsDialog/ConnectionAdd.vala`

---

## Purpose

- **🔷** **`Adw.ActionRow`** put title + subtitle on the left and the control on the right.
- **🔷** Help text was scrunched under the label on narrow widths.
- **🔷** Fix: title stays left; control + **`dim-label`** description stack in the **suffix** column (right).

---

## Layout (normative)

```
Host                    [ entry field          ]
                        URL of the Ollama…
```

- **✅** No **`subtitle`** on the row.
- **✅** Vertical **`Gtk.Box`** suffix: control, then wrapped label.
- **✅** Label: **`css_classes = {"dim-label"}`**, **`xalign = 1.0`**, **`max_width_chars = 45`**.

---

## Shipped in `ConnectionAdd()` constructor

- **✅** Host row — description under **`host_entry`**
- **✅** API Key row — description under **`api_key_entry`**
- **✅** Logic inlined in constructor (no new private helper method)

---

## Concrete code proposals

**⏳** None — implemented. See tree at **`ollmapp/SettingsDialog/ConnectionAdd.vala`** (Host / API Key row blocks in **`ConnectionAdd()`**).
