# 2.10.4.30 — Startup overlay + daemon activity status bar

**Status:** **PROPOSED** — for review

**Pointer:** `docs/guide-to-writing-plans.md` — **Checklist for plans**; proposed Vala follows **`docs/coding-standards.md`**

**Parent:** [`2.10.4.0-summary.md`](2.10.4.0-summary.md)

**Related:**

- [`done/2.10.4.14-DONE-daemon-scan-update-notification.md`](done/2.10.4.14-DONE-daemon-scan-update-notification.md) — **`event.vector.scan_update`**
- [`done/2.10.4.22-DONE-app-rpc-daemon-startup.md`](done/2.10.4.22-DONE-app-rpc-daemon-startup.md) — RPC boot order in **`initialize_client`**
- [`2.10.4.1-ollmfilesd-rpc-api.md`](2.10.4.1-ollmfilesd-rpc-api.md) — wire inventory
- [`2.10.2-background-scan-pause-and-foreground-reload.md`](2.10.2-background-scan-pause-and-foreground-reload.md) — pause/resume on bar (later)
- [`2.10.4.8-per-client-project-notifications.md`](2.10.4.8-per-client-project-notifications.md) — **`event.file.*`** (separate concern)
- [`9.0-android-poc-summary.md`](9.0-android-poc-summary.md) — Android chat POC; **no** local **`ollmfilesd`**

---

## Purpose

- **🔷** Extend the **startup blocking overlay** (today **`CheckingConnectionDialog`**) so it stays up through **Ollama verification** and **`ollmfilesd` RPC connect**, with **wording that changes per step**, and dismisses only when the **main shell is stable** (agent dropdown populated, chat shell wired).
- **🔷** Repurpose **`VectorScanBanner`** into a **non-blocking daemon-activity status bar** for work that happens **after** the overlay dismisses — project list load, project activate / filesystem scan, vector index queue.
- **🔷** Produce an explicit **inventory** of what status text we can drive **today** vs what needs **new daemon notifications**.
- **🔷** Add a formal **`disable_filesd`** switch so builds that **cannot** spawn **`ollmfilesd`** (Android) or **do not need** project workspace (chat-only) skip daemon boot cleanly — document what breaks without the daemon.
- **🔷** Final polish tranche for the **2.10.4 RPC migration** — UI feedback for daemon boot and early project work that is invisible today.

---

## Current behaviour (gaps)

| Phase | What runs | User sees today |
| ----- | --------- | --------------- |
| **`Initialize.run`** | **`config.check_connections()`** (Ollama) | **`CheckingConnectionDialog`**: *"Verifying connection to server…"* |
| **`Initialize.run`** | Model verify, required-model pull | Same dialog **closes** after Ollama check; model work has **no** overlay |
| **`initialize_client`** | **`yield rpc.connect(Daemon.hello)`** — spawn **`ollmfilesd`**, socket, hello | **Nothing** until failure → **`tool_error_banner`** |
| **`initialize_client`** | **`yield load_projects_from_db`** | **Nothing** |
| **`initialize_client`** | Tools, agents, **`agent_dropdown.wire()`** | Header **empty** until wire completes; then agents appear |
| **Session / Coding agent** | **`restore_active_state`** → **`activate_project`** (RPC fire-and-forget) | **Nothing** while daemon **`read_dir`** runs |
| **Daemon background** | **`BackgroundScan`** vector queue | **`event.vector.scan_update`** → **`VectorScanBanner`** only |

**ℹ️** **`AgentFactory.initialize_widget`** has a **`FIXME`** (*initialize first and show…*) — same gap when the coding-agent pane opens.

**ℹ️** **Android** ([`ollmapp/android/OllmchatWindow.vala`](../../ollmapp/android/OllmchatWindow.vala)) already **never** calls **`rpc.connect`** — chat-only boot with a stub **`OLLMfiles.ProjectManager`** ([`AndroidToolTypes.vala`](../../ollmapp/android/AndroidToolTypes.vala)). There is **no** formal flag yet; behaviour is implicit per platform build.

---

## `disable_filesd` — flag and implications

### Android — your assumption is correct

- **🔷** The Android POC **cannot** usefully spawn **`ollmfilesd`** on-device (no packaged daemon binary, no Unix socket boot path, sandbox limits).
- **🔷** Android **does not need** local **`ollmfilesd`** for the current POC — chat + **`web_fetch`** / **`session_fetch`** / **`google_search`** only ([`9.0-android-poc-summary.md`](9.0-android-poc-summary.md) **Tools** table).
- **✅** Today: **`initialize_client`** on Android skips daemon entirely — LLM connect → agents → chat. **No** **`ProjectManager`** RPC client.

**ℹ️** **Later** Android could connect to a **remote** **`ollmfilesd`** on the dev machine ([`9.0`](9.0-android-poc-summary.md) *Later* bullet) — that is **not** `disable_filesd`; it is a different transport target.

### Proposed flag shape

| Mechanism | Where | Default |
| --------- | ----- | ------- |
| **Meson** **`files_daemon`** | **`meson_options.txt`** — `false` for Android APK target | `true` on Linux desktop |
| **Compile-time** | **`libocfiles`** / **`ollmapp`** — when `false`, **🚫** link **`ClientBoot`** spawn path; optional stub PM | — |
| **Runtime** (💩 optional) | **`config.2.json`** key e.g. **`files_daemon.enabled`** | Desktop `true` |

**🔷** Minimum for this tranche: **Meson / platform** flag so **`initialize_client`** branches like Android today — **not** a user-facing Settings toggle unless requested.

**💩** Env override **`OLLM_DISABLE_FILESD=1`** for dev chat-only desktop runs — confirm before build.

### Overlay / status bar when `disable_filesd`

| Layer | With daemon | Without daemon |
| ----- | ----------- | -------------- |
| Overlay step 3–4 | *Starting / connecting filesystem daemon…* | **Skipped** |
| Overlay step 5 | *Loading workspace…* (`load_projects_from_db` + wire) | *Preparing chat…* (agents + tools only) |
| Status bar (Layer 2) | Project load, scan, index | **Hidden / unused** — no daemon RPC, no **`scan_update`** |

### What works without `ollmfilesd`

| Feature | Works? | Notes |
| ------- | ------ | ----- |
| LLM chat (**`chatter`** agent) | **✅** | Same as Android POC |
| History / sessions | **✅** | Local SQLite under app data dir |
| **`web_fetch`**, **`session_fetch`**, **`google_search`** | **✅** | No project paths required |
| Settings (connections, models) | **✅** | Unchanged |
| Agent dropdown | **✅** | Only agents registered without PM — today **`chatter`** only on Android |

### What does **not** work without `ollmfilesd`

Everything that goes through **`libocfiles.ProjectManager`** RPC or daemon-owned **`files.sqlite`**:

| Feature | Broken because |
| ------- | -------------- |
| **Project list** / create / remove | **`ProjectManager.load_projects_from_db`**, **`create_project`**, … — daemon DB |
| **Activate project** / file tree | **`activate_project`** → daemon **`read_dir`** |
| **Coding assistant** (**`CodeAssistant`** / **`SourceView`**) | **`AgentFactory`** needs real PM — **`restore_active_state`**, editor buffers via **`File.*`** |
| **Skill runner** (Conductor) | Same — project-scoped skills |
| **File tools** — **`read_file`**, **`write_file`**, **`edit_mode`** | Need workspace paths + daemon I/O |
| **`codebase_search`** | Daemon FAISS + vector pipeline |
| **`run_command`** / sandbox (future **`Bubble.*`**) | Daemon execution + overlay scan |
| **Approvals** / pending file history | **`Folder.fetch_pending_approvals`**, daemon SQL |
| **File-change banner** | Compares buffer to daemon-backed file state |
| **Vector / scan status bar** | No **`event.vector.scan_update`** source |
| **Session `project_path` restore** | **`AgentDropdown.session_selection`** → **`activate_project`** — no-op or error without PM |

**🔷** In short: **without the file daemon you lose the project workspace** — not just “project manager UI”, but **all** coding-agent, file-tree, vector, and filesystem-backed tool surfaces. Pure chat remains fine.

### Desktop today vs target

| | Desktop **`Window.vala`** today | Target with **`disable_filesd`** |
| - | ------------------------------- | -------------------------------- |
| **`rpc.connect`** | **Required** — failure → **`tool_error_banner`**, **return** | **Skipped** when flag set |
| **`project_manager`** | Real **`OLLMfiles.ProjectManager`** + RPC | **Null** or no-op stub (mirror Android) |
| Agents registered | **`chatter`** + **`CodeAssistant`** + **SkillRunner** | **`chatter`** only (💩 unless remote daemon added later) |
| Tools | Full **`tools_registry.fill_tools(pm)`** | Subset registration (Android pattern) |

### Touch points (flag)

| File | Change |
| ---- | ------ |
| **`meson_options.txt`** / **`ollmapp/meson.build`** | **`files_daemon`** option; Android build sets **`false`** |
| **`ollmapp/Window.vala`** | Branch **`initialize_client`** — daemon overlay steps + **`rpc.connect`** only when enabled |
| **`libocfiles/ProjectManager.vala`** | **💩** **`files_daemon`** compile guard or stub interface — avoid linking spawn on Android |
| **`ollmapp/android/OllmchatWindow.vala`** | Align with shared overlay API; keep implicit **`disable_filesd`** |

**🚫** Do **not** fail boot when **`disable_filesd`** and user picks **Coding assistant** — hide or disable that agent when PM unavailable (same as Android today: only **`chatter`** in dropdown).

---

## Target UX (two layers)

```text
┌─────────────────────────────────────────────────────────────┐
│  LAYER 1 — Startup overlay (modal, blocks interaction)      │
│  Steps advance in order; dismiss when shell stable            │
├─────────────────────────────────────────────────────────────┤
│  1. Connecting to LLM server…          (Ollama / OpenAI)    │
│  2. Verifying models…                  (optional sub-step)  │
│  3–4. (files_daemon only) Starting / connecting daemon…     │
│  5. Preparing workspace…  OR  Preparing chat… (no daemon)   │
└─────────────────────────────────────────────────────────────┘
                              ↓ dismiss
┌─────────────────────────────────────────────────────────────┐
│  LAYER 2 — Status bar (files_daemon only)                   │
│  Non-blocking; reveals under header banners                 │
├─────────────────────────────────────────────────────────────┤
│  • Loading project list…                                    │
│  • Opening project: <name>…                                 │
│  • Scanning project files: 450 / ~1200   (filesystem ⏳)     │
│  • Indexing: file.vala — 3/12       (scan_update today ✅)  │
└─────────────────────────────────────────────────────────────┘
```

---

## Status inventory — what we can show

### A. Client-local (no new wire) — **available now**

These are **`yield`** boundaries or explicit calls in **`ollmapp/Window.vala`** / **`libocfiles/ProjectManager.vala`**. Label text is set **before** the call and cleared **after** (or on error).

| Step | Trigger (client) | Suggested label | Layer |
| ---- | ---------------- | --------------- | ----- |
| Ollama probe | **`config.check_connections()`** | *Connecting to LLM server…* | Overlay |
| Model verify | **`initialize_model`**, **`ensure_model_usage`** | *Verifying chat model…* | Overlay |
| Required models | **`ensure_required_models`** | *Checking tool models…* | Overlay |
| Daemon boot | **before** **`yield rpc.connect(hello)`** | *Starting filesystem daemon…* | Overlay |
| Daemon hello | **during** **`yield rpc.connect(hello)`** | *Connecting to filesystem daemon…* | Overlay |
| Project list (window boot) | **before/after** **`yield load_projects_from_db`** | *Loading project list…* | Bar (or overlay step 5) |
| Tool / agent wire | **before** **`agent_dropdown.wire()`** | *Preparing agents…* | Overlay step 5 |
| Overlay dismiss | **after** **`setup_chat_widget`** + first **`agent_dropdown.wire()`** | — | Overlay hides |
| Project list (agent pane) | **`AgentFactory.initialize_widget`** → **`load_projects_from_db`** | *Loading project list…* | Bar |
| Activate project (sent) | **`activate_project`** — RPC **`call.begin`** | *Opening project: \<basename\>…* | Bar |
| Filesystem scan progress | **`event.project.filesystem_scan_update`** (Phase 3) | *Scanning project files: \<done\> / ~\<total\>* | Bar |
| Activate project (RPC done) | **`rpc.call.end`** on activate | Clear if no scan event pending | Bar |
| Vector index queue | **`event.vector.scan_update`** | *Indexing: \<file\> — X/Y* (today's text) | Bar ✅ |

**🚫** Client cannot know filesystem scan **progress** today — only coarse start (RPC sent) or end (RPC reply if awaited).

**💩** Optionally await **`activate_project`** RPC on the status-bar path so the bar clears when the server replies — still useful as a **fallback** if notifications are missed; does not replace throttled progress events.

---

### B. Daemon push notifications — **on the wire today**

| `method` | Payload | Status use |
| -------- | ------- | ---------- |
| **`event.vector.scan_update`** | **`message`**: `"<queue_size> <path>"` | Vector index progress + completion (**`queue_size == 0`**) ✅ |

**ℹ️** Broadcast to **all** clients — no per-window routing ([`2.10.4.8`](2.10.4.8-per-client-project-notifications.md)).

---

### C. Daemon push notifications — **not on the wire (gaps)**

| Desired label | Why missing | Plan |
| ------------- | ----------- | ---- |
| *Scanning project files: done / ~total* | **`Folder.read_dir`** — no RPC emit | **Phase 3** — **`event.project.filesystem_scan_update`** (**🔷** user requirement) |
| *Scanning project files — done* | Same | Final notification **`done == 0`** (mirror **`scan_update`**) or **`done >= total`** sentinel |
| *Opening vector database…* | **`open_vector_db`** on daemon startup only | **🚫** not needed on client boot if daemon already warm |
| *Building index queue…* | **`Codebase.start`** / **`queueProject`** — no events | **`event.vector.queue_update`** (💩) or reuse **`scan_update`** at queue build |
| External file changed | File watcher | **`event.file.changed`** — [`2.10.4.8`](2.10.4.8-per-client-project-notifications.md) |

**🚫** Do **not** invent **`vector.status`** or generic RPC tracing on the status bar for this tranche — see [`2.10.4.1`](2.10.4.1-ollmfilesd-rpc-api.md) LLM reference.

**💩** Debug mode: append last RPC **`method`** name to the bar (developer-only) — not requested; confirm before build.

---

## Phase 1 — Startup overlay ✔️

**🔷** Single overlay for the whole boot sequence — **`OLLMapp.BusyDialog`** (`ollmapp/BusyDialog.vala`) with public **`status_label`**; callers use **`present`/`close`** and set **`status_label.label`**.

### Step labels (ordered)

1. *Connecting to LLM server…*
2. *Verifying models…* (skip sub-step if fast / unchanged connection)
3. *Starting filesystem daemon…* / *Connecting to filesystem daemon…* (split if spawn vs socket distinguishable in **`ClientBoot`**)
4. *Loading workspace…* — covers **`load_projects_from_db`** + agent registration + **`agent_dropdown.wire()`**

### Dismiss rule

- **🔷** Hide overlay when **`initialize_client`** reaches **post-`wire()`** milestone (agents visible, **`new_chat_button.sensitive = true`**).
- **🔷** On **`rpc.connect`** failure — overlay closes, existing **`tool_error_banner`** (or alert) unchanged.
- **🚫** Do **not** wait for session restore or coding-agent **`restore_active_state`** — that is **Layer 2**.

### Touch points

| File | Change |
| ---- | ------ |
| **`ollmapp/Initialize.vala`** | Pass overlay / callback into **`run`** for Ollama + model steps (today show/hide around **`check_connections`** only) |
| **`ollmapp/Window.vala`** | Own overlay for **`initialize_client`** daemon + workspace steps |
| **`libocrpc/Client.vala`** / **`ClientBoot.vala`** | **💩** Optional stage signal (*spawning* vs *connecting*) — only if labels need split without guessing |

---

## Phase 2 — Repurpose status bar

**🔷** Rename **`VectorScanBanner`** → **`DaemonActivityBanner`** (or keep class, broaden API).

### API sketch (plan only — no code until approved)

- **`set_indeterminate(string label)`** — pulse bar, no fraction (unknown duration: project list, waiting for scan complete).
- **`set_progress(string label, double fraction)`** — vector index (reuse today’s math).
- **`clear()`** — hide revealer (queue empty / step done).

### Wiring

| Source | Handler |
| ------ | ------- |
| **`Window.initialize_client`** | Bar: *Loading project list…* around **`load_projects_from_db`** |
| **`ProjectManager.activate_project`** (client) | Bar: *Opening project…* on send; **⏳** clear on RPC end or filesystem event |
| **`rpc.notification`** | **`event.vector.scan_update`** — keep parse logic; update labels to *Indexing…* |
| **`rpc.notification`** | **`event.project.filesystem_scan_update`** — *Scanning… done / ~total* (Phase 3) |
| **`AgentFactory.initialize_widget`** | Bar during second **`load_projects_from_db`** + **`restore_active_state`** |

**🔷** When coding agent is active and bar is idle — **hide** (same as today after vector queue drains).

---

## Phase 3 — Filesystem scan progress (daemon)

**🔷** **`read_dir`** can take noticeable time on large projects — show **count-based progress** on the status bar. **🚫** Do **not** stream the file tree or file payloads over RPC — **counts + optional current path only**.

### Design (user requirement)

- **🔷** **Total (denominator)** — approximate count from **SQLite before** the disk walk starts.
  - After **`load_files_from_db`** in **`activate_project`**, query **`filebase`** rows under the project root (`delete_id = 0`, path prefix match — same style as **`Folder.load_children`**).
  - **ℹ️** First open / empty DB → total **`0`** → bar runs **indeterminate** until the first throttle emit, or show *Scanning project files…* without fraction.
  - **🔷** Total is a **hint** — disk may find **more** entries than the DB had; UI must not break when **`done > total`** (clamp bar fraction at **`1.0`**, show *450 / ~400* or bump displayed total to **`max(total, done)`**).
- **🔷** **Done (numerator)** — increment on the daemon as the tree walk proceeds (each folder **`read_dir`** visit and/or each entry handled in **`read_dir_update`** / enumeration — pick **one** consistent rule in implementation).
- **🔷** **Throttle** — **`broadcast`** at most every **`100`** counted steps, plus **mandatory** emits at **scan start** and **scan complete**.
- **🚫** No per-file RPC **`File.*`** traffic for progress — notification lines only.

### Wire shape

Mirror **`event.vector.scan_update`** — reuse **`OLLMrpc.Notification.message`** (no new fields):

| `method` | `object_type` | `message` format |
| -------- | ------------- | ---------------- |
| **`event.project.filesystem_scan_update`** | **`Project`** | `"<done> <total> <current_path>"` |

Examples:

```json
{"jsonrpc":"2.0","method":"event.project.filesystem_scan_update","object-type":"Project","message":"0 1842 /home/alan/proj"}
```

```json
{"jsonrpc":"2.0","method":"event.project.filesystem_scan_update","object-type":"Project","message":"500 1842 /home/alan/proj/src/Foo.vala"}
```

Scan finished (same sentinel pattern as vector — **`done == 0`**):

```json
{"jsonrpc":"2.0","method":"event.project.filesystem_scan_update","object-type":"Project","message":"0 0 "}
```

**💩** Include **project root path** as third token (not only current file) so multi-client broadcast can filter later — optional for this tranche; **`2.10.4.8`** routing is future.

### Daemon implementation sketch

| Piece | Responsibility |
| ----- | -------------- |
| **`ProjectManager`** (or scan helper on PM) | Per-project scan session: **`total_estimate`**, **`done`**, **`project_path`**, **`last_emit_done`** |
| **`activate_project`** | Before **`read_dir`**: set **`total_estimate`** from SQL **`COUNT(*)`**; emit start **`0 total path`**; clear session after scan |
| **`Folder.read_dir`** | Increment **`done`** at agreed hook(s); if **`done - last_emit_done >= 100`**, emit update and set **`last_emit_done = done`** |
| **`OllmfilesdApplication.broadcast`** | Same as **`BackgroundScan.emit_scan_update`** |

**ℹ️** **`pm.scanning`** map already tracks in-flight folders — session **`done`** counter is separate (UI progress, not concurrency guard).

### Client implementation sketch

| Piece | Responsibility |
| ----- | -------------- |
| **`Window.vala`** **`rpc.notification`** | Handle **`event.project.filesystem_scan_update`** alongside vector |
| **Status bar** | Parse **`done`**, **`total`**, path; label *Scanning project files: \<done\> / ~\<total\>*; **`set_progress`** with **`min(done/total, 1.0)`** when **`total > 0`** |
| **Completion** | **`done == 0`** → **`clear()`** unless vector index immediately follows |

Register row in [`2.10.4.1-ollmfilesd-rpc-api.md`](2.10.4.1-ollmfilesd-rpc-api.md) **`event.*`** when implemented.

**🚫** Do not add a separate **`filesystem_scan_complete`** method — one event type + empty sentinel is enough (matches **`2.10.4.14`**).

---

## Suggested order

0. **🔷** `⏳` **`disable_filesd`** — Meson flag + **`initialize_client`** branch (Android alignment; chat-only boot).
1. **🔷** `⏳` Phase 1 — overlay steps through Ollama + daemon (when enabled) + workspace; dismiss at agent wire.
2. **🔷** `⏳` Phase 2a — bar for **`load_projects_from_db`** + **`scan_update`** (relable only); **🚫** when **`disable_filesd`**.
3. **🔷** `⏳` Phase 2b — bar for **`activate_project`** (client-local start label).
4. **🔷** `⏳` Phase 3 — **`event.project.filesystem_scan_update`** (DB total estimate, throttled **`done`**, no tree on wire).
5. **ℹ️** [`2.10.2`](2.10.2-background-scan-pause-and-foreground-reload.md) — click-to-pause on bar **after** this plan.

---

## Acceptance (review)

- **🔷** Boot shows **one** overlay with **changing text** through LLM check and **`ollmfilesd`** connect (daemon steps **skipped** when **`disable_filesd`**).
- **🔷** Overlay stays until **agent dropdown** is populated.
- **🔷** After dismiss, bar shows **project list load**, **filesystem scan progress** (throttled), and **vector index** activity (**files_daemon** builds only).
- **🔷** **`disable_filesd`** build reaches chat without **`tool_error_banner`** daemon failure.
- **🔷** Filesystem scan shows **done / ~total** on the bar; emits throttled (~every 100 steps); **🚫** no file tree on the wire.

---

## LLM implementer guardrails

- **🚫** No new RPC request methods for status — use notifications + client hooks only.
- **🚫** No per-client routing in this tranche — broadcast **`scan_update`** behaviour unchanged.
- **🚫** Do not block **`initialize_client`** on vector indexing or filesystem scan completion.
- **🚫** Do not merge overlay and bar into one widget — two layers per **Target UX**.
- **🚫** Do not require **`ollmfilesd`** when **`disable_filesd`** — no spawn attempt, no error banner for missing daemon.
- **🚫** Do not register **Coding assistant** / **SkillRunner** / file tools without a live PM + daemon.
- **🚫** Filesystem scan notifications — **counts only**; no **`Folder[]`**, no per-file RPC for progress.
