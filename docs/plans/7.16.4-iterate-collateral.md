# 7.16.4 — Iterate collateral (prompts, skills, progress, docs, cleanup)

**Status:** proposed

**Parent:** [`7.16-iterative-task-execution-loop.md`](7.16-iterative-task-execution-loop.md)

**Depends on:** [`7.16.3-action-iterate-wire.md`](7.16.3-action-iterate-wire.md)

**Blocks:** nothing (closes **7.16** feature)

**Pointer:** `.cursor/rules/CODING_STANDARDS.md` — Checklist for all plans · **Layout:** `docs/guide-to-writing-plans.md`

> **Note:** `docs/plans/1.0-summary.md` is not updated until this sub-plan is done and archived.

---

## Purpose

- **🔷** Align prompts, parser, skills, docs, and progress labels with **task iteration** loop shipped in **7.16.3**.
- **🔷** Remove legacy multi-tool refine + merge + old replay branches.
- **🔷** Polish UX copy and hide obsolete sessions in UI.

---

## Refinement and parser

- **🔷** **`ResultParser.extract_refinement`**: reject **`proposed_tools.size > 1`**.
- **🔷** **`task_refinement.md`**: drop “encourage multiple tool calls”; max **one** fence; further tools via task iteration.
- **🔷** **`ResultParser`**: parse iteration output (optional tool + required summary) — may overlap **7.16.3**; finish edge cases here.

```vala
if (task.proposed_tools.size > 1) {
    this.issues += "\nRefinement allows at most one tool call; use task iteration for more.";
    return;
}
```

---

## Prompts

- **🔷** Rename **`task_post_exec.md`** → **`task_iteration.md`**.
- **🔷** Role: task **iteration** (not “merge N runs”).
- **🔷** Placeholders: **`{tool_call_history}`** / **`{execution_log}`**, **`{task_definition}`**, retry headers.
- **🔷** **`task_execution.md`**: one cycle only; drop “more runs after this one”.
- **💩** `⏳` Pick **Summarizing** vs **Task iteration** vs **Iterate** for stage column after dogfood.

---

## Skills

- **🔷** Update **`resources/skills/*.md`** path **B** refinement sections (e.g. **`analyze_codebase`** — one search at refine).
- **ℹ️** **A / C / D** skill files: no change on happy path.
- **ℹ️** **`plan_create`** / **`implement_code`**: write path unchanged.

---

## Docs

- **⏳** **`docs/task-and-skills-flow.md`** — iteration loop, not multi-tool merge table.
- **⏳** **`docs/skills-format.md`** — one tool at refine; iteration owns more.

---

## Progress — `PhaseEnum.to_human`

- **⏳** Implement draft in parent **Progress status — `to_human` draft** (phase 3 + phase 4 note):
  - **`TASK_ITERATING`**, **`TASK_ITERATION`**, **`TASK_ITERATION_RETRY`**, **`TASK_ITERATION_CAP`**
  - **`from_string("task_iteration")`**
  - **`POST_EXAM_MERGE`** or rename **Post review** for exam merge
- **💩** **`Iterating (n)`** may compose markup on **`Details`** when **`cycle_index > 0`**.

---

## Remove / simplify

- **🔷** Multi-tool refinement prompts (**path B**).
- **🔷** **`run_post_exec`** merge of N refinement tools; single-run copy shortcut for multi-tool refine.
- **🔷** **`tool_runs_combined`** one-shot merge for path **B**.
- **🔷** Legacy **`on_replay`** multi-tool cursor paths (`Runner.vala` ~978–1090).
- **🔷** New replay: **🚫** handle **`post_exec`** wire id.
- **🚫** Dual replay hydrators.

---

## Session UI

- **💩** `⏳` Hide skill sessions with **`schema_version`** below min in session list (not deleted on disk).

---

## Slim `Details` (**💩** optional tail)

- **💩** `⏳` After collateral stable: move any remaining fat out of **`Details`**; keep data + refine + **`to_markdown`** + progress hooks.

---

## Acceptance (full **7.16**)

- **🔷** Second tool fence at refine → validation error.
- **🔷** Two-cycle search: history shows first query; second call differs; final **`out_doc`**.
- **🔷** **A (no tools):** two exam refs → merge — same as today.
- **💩** **A (has tools):** only if that gate was built.
- **🔷** **C / D:** success path unchanged.
- **🔷** **`plan_create`** / approval still work.
- **🔷** Old session: no false replay hydration.
- **🔷** New session: **`schema_version`** + replay with markers.

---

## LLM notes

- **🚫** Do not reintroduce **`has_task_complete()`** tail phrase.
- **🚫** No parallel tools within one cycle.
